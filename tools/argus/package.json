{
  "name": "@athena/argus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency OpenTelemetry collector for Claude Agent SDK / Claude Code sessions: ingest, aggregation, persistence and a JSON API",
  "license": "GPL-3.0-or-later",
  "engines": {
    "node": ">=20.11"
  },
  "bin": {
    "argus": "./bin/argus.mjs"
  },
  "files": [
    "bin",
    "src"
  ],
  "scripts": {
    "start": "node bin/argus.mjs",
    "dev": "node --watch bin/argus.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "demo": "node scripts/demo-emit.mjs"
  }
}
