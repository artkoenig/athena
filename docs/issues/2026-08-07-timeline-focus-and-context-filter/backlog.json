{
  "version": 1,
  "issue": "docs/issues/2026-08-07-timeline-focus-and-context-filter",
  "workflow": "agile-loop",
  "increments": [
    {
      "id": "filter-core",
      "title": "The context panel gets a working block filter",
      "goal": "Give the context panel head a dropdown that lists the block kinds and top-level fields the shown request actually contains, and let the reader hide and show them, against the record the panel already holds.",
      "criteria": [
        "The context panel head carries a dropdown built as a `<details>` popover, with no framework and no new dependency, listing every block kind and every top-level field the currently shown request actually contains, one checkbox each, with the number of blocks behind that entry (e.g. `tool_result 12`).",
        "The list is in two labelled groups: **Blocks** first, holding the block kinds present (`assistant`, `other`, `raw`, `system`, `thinking`, `tool_result`, `tool_use`, `user`) sorted alphabetically; then **Fields**, holding the body's remaining top-level keys (`max_tokens`, `metadata`, `model`, `tools`, …) sorted alphabetically. Entries are labelled by the kind name and the field key verbatim, so `system` and `tools` read as one list.",
        "The dropdown holds two buttons, **all on** and **all off**. *All on* makes every entry visible. *All off* hides every entry the currently shown request contains.",
        "Unchecking an entry removes its blocks from the list below; checking it puts them back. Neither triggers a fetch: the filter is applied to the record the panel already holds, and `/api/content/at` is not called again.",
        "The selection is one page-wide state, held as the set of *hidden* entries. It survives a lane change, a scrub, a live refresh and a session change; it is not persisted and a page reload starts with everything visible. An entry never hidden is visible, which is what makes an entry that appears for the first time — a `thinking` block in a later request, a field an earlier body did not carry — arrive visible, including after *all off* was pressed on a request that did not contain it.",
        "An entry the current request does not contain is not listed, and its hidden state is remembered: scrubbing to a request that carries it again shows it still unchecked and its blocks still hidden.",
        "The derivation and the markup are pure functions in `public/context.js` — no `document`, no `fetch`, no `location`, the contract `test/independence.test.mjs` guards — and only the click wiring lives in `app.js`.",
        "The filter's markup carries no `data-lane` attribute and its `<summary>` carries no `data-block`, so opening the dropdown neither toggles the lane selection nor a block's expansion.",
        "An open dropdown survives a repaint: a live refresh that re-renders `#lane-panel` does not snap it shut, the same way `state.expanded` keeps expanded blocks open today.",
        "`npm --prefix tools/argus-ui test` passes after this increment, and each criterion above has at least one test that goes red when its behaviour is broken or removed."
      ],
      "status": "done",
      "note": "Accepted in review round 1: the context panel's filter is built as pure functions in public/context.js with only the click and change wiring in app.js, and the whole suite runs 230 cases at exit 0.",
      "steps": []
    },
    {
      "id": "filter-honesty",
      "title": "The panel head names visible and total, and the empty case says so",
      "goal": "Make the filtered panel tell the truth about how much it is hiding: the head names visible-of-total blocks and characters, and a panel with everything hidden shows a placeholder instead of an empty list.",
      "criteria": [
        "With nothing hidden, the head line reads as it does today (`210.114 chars · 34 blocks`). With something hidden it names both numbers — visible of total — for blocks and for characters, so the filter can never make a context look smaller than it is. The totals stay readable off `data-chars` and `data-blocks`; the visible counts arrive in data attributes of their own.",
        "With every entry hidden the panel keeps its `data-state=\"ready\"` and shows a placeholder in place of the rows, saying every kind is hidden and where to turn them back on.",
        "The head's new numbers and the placeholder are derived and rendered by pure functions in `public/context.js`, under the contract `test/independence.test.mjs` guards; no new wiring is needed in `app.js` for either.",
        "The cases the previous increment wrote to pin today's behaviour — that a partly filtered panel leaves the head's totals untouched, and that an all-hidden panel renders an empty block list with no placeholder — are updated to the new behaviour in the same change, so no case is left red, skipped or asserting the superseded shape.",
        "`npm --prefix tools/argus-ui test` passes over the whole suite after this increment, and each criterion above has at least one test that goes red when its behaviour is broken or removed."
      ],
      "status": "done",
      "note": "Accepted in review round 1 with no findings: contextCounts is a pure export of public/context.js, the head names visible-of-total chars and blocks with data-visible-chars/data-visible-blocks only while a block is actually hidden, an all-hidden record renders one placeholder in place of the rows with data-state=\"ready\" intact, app.js needed no new wiring, and the whole argus-ui suite ran 238 cases at exit 0.",
      "steps": []
    },
    {
      "id": "tools-box",
      "title": "The tools box under the timeline goes, its tick marks stay",
      "goal": "Delete the redundant tool list panel and the per-call parameter ballast it forced into page state, while leaving every other tool signal on the timeline exactly as it is.",
      "criteria": [
        "`public/tools.js` and `test/tools.test.mjs` are deleted, and `app.js` imports neither `laneToolInput` nor `renderToolPanel`.",
        "`renderLanePanel()` paints only the context panel into `#lane-panel`.",
        "Everything else about tools on the timeline is untouched: the `data-kind=\"tool\"` tick marks on the lane track, the `data-tools` count in each lane's meta, the `tool call` legend entry, and the incremental `/api/events?event=claude_code.tool_result` poll with its `sinceSeq` watermark.",
        "`toolCallOf()` keeps only what a mark needs — `seq`, `timeMs`, `spanId` — and no longer derives or holds `name`, `preview`, `text`, `chars` or `truncated`. `TOOL_PARAM_CHARS` and the `paramText()` helper go with it. `mergeToolMarks()` keeps its contract: duplicates dropped by `seq`, the watermark returned as the highest `seq` actually held, the input array untouched.",
        "The context panel comes through the deletion whole: it is still built from one input builder, the filter still renders, hides and reopens, the head still names visible-of-total blocks and characters when something is hidden, and an all-hidden record still shows its placeholder with `data-state=\"ready\"`.",
        "Every case outside the deleted file that asserts the old tool-mark shape or the tool panel's presence — whole-object assertions on `toolCallOf()`'s return among them — is updated or removed in the same change, so no case is left red, skipped or asserting the superseded shape.",
        "`npm --prefix tools/argus-ui test` passes over the whole suite after this increment, and each criterion above has at least one test that goes red when its behaviour is broken or removed."
      ],
      "status": "done",
      "note": "Accepted in review round 1: public/tools.js and test/tools.test.mjs are gone, renderLanePanel() assigns only the context panel, toolCallOf() returns just seq/timeMs/spanId with TOOL_PARAM_CHARS and paramText() removed, mergeToolMarks() keeps its contract, and the tick marks, data-tools count, legend and incremental tool poll are untouched; the whole argus-ui suite ran 223 cases at exit 0. Round 0 was rejected for one defect only: the README still promised the deleted tool list, which round 1 removed.",
      "steps": []
    },
    {
      "id": "tabs-out",
      "title": "The six technical tabs go out of the code",
      "goal": "Remove the pre-timeline tab interface — its views, its state, its wiring, its per-refresh fetches, and everything left over that only it could produce — and land the whole issue with both suites green.",
      "criteria": [
        "`DETAIL_VIEWS` and `renderDetailViews()` are gone from `public/timeline.js`, and no tab nav is rendered anywhere.",
        "Gone from `public/app.js`: `renderTabBody()`, `renderOverviewTab()`, `renderTodosTab()`, `renderTracesTab()`, `renderWaterfall()`, `renderSpanInspector()`, `renderEventsTab()`, `renderMetricsTab()`, `renderRawTab()`, `loadTabData()`, the helpers only they used (`kpi`, `formatValue`, `spanKind`, `spanNote`, `todoStatusChip`, `SPAN_KINDS`), the `#tab-body` container, and the state fields `tab`, `trace`, `selectedTraceId`, `selectedSpanId`, `events`, `eventFilters`, `metrics`, `facets`.",
        "Gone from the wiring in `wireEvents()`: the handlers for `[data-tab]`, `[data-trace]`, `[data-span]` and `[data-event-seq]`, the `change` handler for `event-filter` and `event-errors`, and the `event-search` branch of the `input` handler. The 15-second interval no longer repaints a tab body.",
        "`renderDetail()` no longer builds the `counts` object, and no refresh requests `/api/traces`, `/api/facets` or `/api/metrics`. The `/api/events` request that feeds the tool marks stays.",
        "No unused import or dead helper is left behind in `app.js` or `timeline.js`, and the tests that pinned the removed views are removed with them rather than left skipped.",
        "Nothing survives elsewhere in the interface that only a removed view could produce: the stylesheet keeps no rule for a tab nav, a tab body or any of the six views' markup, and no selector is left that no renderer can emit.",
        "The interface's own documentation stops promising the removed views: the README no longer describes Overview, Tasks, Traces, Events, Metrics or Attributes as things a reader can open, and no other page under `tools/argus-ui` still names a tab. The previous increment was rejected in review for exactly this kind of stale prose, so it is part of the deletion rather than a follow-up.",
        "The `change` and `input` listeners the context filter and the rest of the page still need survive the removal: the filter's checkboxes, the *all on* and *all off* buttons and the lane selection all still work, and the live refresh still repaints the timeline and the lane panel with an open dropdown left open.",
        "`src/server.mjs` is unchanged: it proxies `/api/*` blindly and knows no route by name.",
        "`npm --prefix tools/argus-ui test` is green over the whole suite, and each criterion above has at least one test that goes red when its behaviour is broken or removed.",
        "`./test.sh` is green."
      ],
      "status": "todo",
      "note": "Re-cut after tools-box: two criteria added, both learned from that increment rather than from the issue. The reviewer rejected its first round solely for a README sentence that still promised the deleted panel, so removing the six tabs' README bullets is now named as part of this increment; and the same deletion had to take the stylesheet rules only the deleted module could produce, so the dead-CSS rule is named here too. Criterion 8 also now says the live refresh must still repaint the timeline and lane panel with the dropdown left open, which is the regression the filter increments would be most likely to lose to this deletion.",
      "steps": [
        {
          "label": "research:tabs-out.0",
          "at": "2026-08-07T16:37:56.621Z",
          "return": {
            "plan": "Delete the six pre-timeline tabs from tools/argus-ui: their two renderers in\npublic/timeline.js, their nine renderers and six private helpers in\npublic/app.js, their eight state fields, their wiring, their per-refresh\nfetches, the stylesheet rules only they could produce, and the README bullets\nthat promise them. Nothing else in the interface changes: the timeline, the\nlane panel, the context filter, the top stat strip, the session list, the\nsetup dialog, public/context.js, public/format.js, public/index.html and\nsrc/server.mjs are untouched.\n\nLine numbers below are as of HEAD (726327e) and shift as edits land; the\nselector or identifier is the real address.\n\n=== public/timeline.js ===\n\nDelete the `DETAIL_VIEWS` const with its doc comment (lines 32-40) and the\n`renderDetailViews()` export with its doc comment (lines 504-516, the last\nthing in the file). Leave everything else. The `import { esc, fmtClock,\nfmtDur, fmtNum } from './format.js'` line stays as it is: all four are still\nused by `renderTimeline()` and the lane meta after `renderDetailViews()` goes\n(fmtNum survives in the lane-meta title and label).\n\n=== public/app.js ===\n\nImports.\n- Drop `renderDetailViews,` from the `./timeline.js` import (line 23).\n- Drop `fmtDur` from the `./format.js` import (line 10). `fmtDur` is used\n  nowhere in app.js once the tab renderers go; every other name on that line\n  (esc, fmtNum, fmtCost, fmtClock, fmtAgo, isLive, shortId) still has a live\n  caller — fmtClock survives in paintCursor(), fmtNum and fmtCost in\n  renderStats() and renderSessionList().\n\nState literal.\n- Delete `tab: null` with the two-line comment above it (lines 38-40).\n- Delete `trace`, `selectedTraceId`, `selectedSpanId`, `events`,\n  `eventFilters`, `metrics`, `facets` (lines 63-69).\n- Keep every other field, `contextHidden` and `contextFilterOpen` above all.\n\nrenderDetail() (lines 168-220).\n- Delete the whole `const counts = { ... }` object and its comment (176-183).\n  Keep `const errors = ...`; the head's error chip still uses it, and\n  `session.counts.apiErrors` / `session.counts.toolFailures` are reads of the\n  payload, not of the deleted object.\n- Delete the `${renderDetailViews({ selected: state.tab, counts })}` line (214).\n- Delete the `<div id=\"tab-body\"></div>` line (216).\n- Delete the `renderTabBody();` call (218). `renderLanePanel();` stays as the\n  last statement, and the template now ends with `<div id=\"lane-panel\"></div>`.\n\nWhole functions and constants to delete, with the section banner comments that\nhead them:\n- `renderTabBody()` (222-248)\n- `/* --- overview --- */` banner, `kpi()`, `renderOverviewTab()` (250-363)\n- `/* --- todos --- */` banner, `todoStatusChip()`, `renderTodosTab()` (365-451)\n- `/* --- traces --- */` banner, `SPAN_KINDS`, `spanKind()`, `spanNote()`,\n  `renderTracesTab()`, `renderWaterfall()`, `renderSpanInspector()`,\n  `formatValue()` (453-596)\n- `/* --- events --- */` banner, `renderEventsTab()` (598-650)\n- `/* --- metrics --- */` banner, `renderMetricsTab()` (652-706)\n- `/* --- raw --- */` banner, `renderRawTab()` (708-722)\n- `loadTabData()` (972-1000)\nThe `/* --- empty state --- */` banner at 724 and everything after it stays.\n\nrefresh() — delete the `await loadTabData();` line (1018). The four loads above\nit stay in order, and so does the try/catch that turns a failure into the\noffline indicator.\n\nselectSession() — delete the five resets of the removed fields (1042-1046) and\n`state.tab = null` with its comment (1048-1049). Everything else in the\nfunction stays, and nothing that touches `contextHidden` or `contextFilterOpen`\nmay be added: those two survive a session change on purpose and a test pins it.\n\nwireEvents(), click listener on #detail — delete the `[data-tab]`,\n`[data-trace]`, `[data-span]` and `[data-event-seq]` branches (1200-1224). The\nlistener then ends after the `[data-ctx-all]` branch, which already returns.\nThe `data-copy`, `data-cursor-live`, `data-lane`, `summary[data-block]`,\n`summary[data-ctx-filter]` and `[data-ctx-all]` branches all stay, in that\norder. Reword the comment inside the lane branch (1166): it currently reads\n\"Clicking the open lane closes it, the way the technical views work\" and must\nstop referring to views that no longer exist — say the toggle in its own terms.\n\nwireEvents(), change listener on #detail — delete the `event-filter` and\n`event-errors` blocks (1228-1235). The listener itself and its\n`input[data-ctx-entry]` branch stay: that branch is the filter's checkboxes and\ndeleting the listener would take them with it.\n\nwireEvents(), input listener on #detail — delete `let searchTimer = null;`\n(1257) and the `event-search` early return with its debounce (1264-1267). What\nis left is the `timeline-scrub` branch, which must keep working; drop its\n`return;` or keep it, either is fine once it is the only branch.\n\nboot() — the 15-second interval keeps `renderSessionList();` and loses\n`if (state.session && state.tab === 'overview') renderTabBody();` (1324). Its\ncomment about sessions ageing out of \"live\" stays true and stays.\n\n=== public/styles.css ===\n\nDelete every rule below; each is markup only a deleted renderer could emit.\nKeep the rest of the file byte-identical.\n- The tab nav: `.tabs`, `.tab`, `.tab:hover`, `.tab[aria-selected=\"true\"]`,\n  `.tab .count` (460-491).\n- The two chip tones only `todoStatusChip()` emitted: `.chip[data-tone=\"ok\"]`\n  and `.chip[data-tone=\"warn\"]` (450-458). `live` and `error` stay — the detail\n  head and the stat strip still emit them.\n- Overview's tiles: `.kpi-grid`, `.kpi`, `.kpi-value`, `.kpi-label`,\n  `.kpi-sub`, `.kpi[data-tone=\"accent\"] .kpi-value`,\n  `.kpi[data-tone=\"error\"] .kpi-value` (495-537).\n- `.panel > h3` (547-554): no surviving renderer puts an `h3` inside a panel.\n  The base `h1, h2, h3` rule (79-86) and `.modal-body h3` stay — the setup\n  dialog still emits `<h3>`.\n- Every table rule (556-607): `.table-scroll`, the bare `table`, `th`, `td` and\n  `tbody tr:last-child td` element rules, `td.num, th.num`, `td.name`, `.bad`,\n  `.good`. No `<table>` is emitted anywhere under public/ after this change.\n- Keep `.panel` itself (539-545) — the timeline panel and the context panel are\n  both `class=\"panel …\"`. Rename the section banner at 493 from\n  \"cards & tables\" to something honest about what is left, e.g. \"panels\".\n- The whole waterfall section, banner included (609-785): `.trace-picker`,\n  `.trace-pill` and its four variants, `.waterfall`, `.waterfall-axis`,\n  `.axis-ticks`, `.axis-tick`, `.span-row` and its two variants, `.span-label`,\n  `.span-label .name`, `.span-label .detail-note`, `.span-track`, `.span-bar`\n  with its six `[data-kind]` variants and `[data-error=\"true\"]`,\n  `.span-duration`, `.span-duration[data-flip=\"true\"]`, `.span-inspector`.\n- The whole events section, banner included (1235-1316): `.filters`,\n  `.filters select, .filters input`, `.filters label.toggle`, `.event-row`,\n  `.event-row:hover td`, `.event-time`, `.event-name`, `.event-summary`,\n  `.event-row[data-error=\"true\"] .event-name`, `.attr-table`, `.attr-table td`,\n  `.attr-table td:first-child`, `.attr-table td:last-child`.\n- Inside `@media (max-width: 900px)`: the `.waterfall { --label-w: 180px; }`\n  rule. The `.layout`, `.sidebar`, `.timeline` and `.stat-strip` rules in that\n  block stay.\n- `.mono` from the `code, pre, .mono { … }` group (89-94), leaving\n  `code, pre { … }`. It is already dead today — nothing under public/ emits\n  `class=\"mono\"` — and the new stylesheet guard would fail on it. The\n  `var(--mono)` custom property is a different thing and stays.\n\nThat list is exhaustive: after it, every class selector left in styles.css is\nnamed by index.html, timeline.js, context.js or what remains of app.js.\n\n=== tools/argus-ui/README.md ===\n\n- Line 3-4, the opening sentence, still sells \"sessions, tokens, cost, traces,\n  events and metrics\". Rewrite it so it names only what the page still shows —\n  sessions, the timeline, and the context behind a lane — while keeping the\n  \"updating live over Server-Sent Events\" clause.\n- Line 66-67, in the **Timeline** bullet: delete the clause \"; the views below\n  it are reachable from there\". The rest of that bullet describes the timeline\n  and the context panel and stays.\n- Lines 72-79: delete the six bullets **Overview**, **Tasks**, **Traces**,\n  **Events**, **Metrics** and **Attributes** whole. \"What it shows\" then holds\n  **Sessions** and **Timeline**.\n- Nothing else in README.md or CLAUDE.md names a tab; both were checked.\n\n=== Untouched, and deliberately ===\n\n- `src/server.mjs`: it proxies on the prefix (`pathname.startsWith('/api/')`)\n  and names no route. Do not open it.\n- `public/context.js`, `public/format.js`, `public/index.html`: index.html\n  never carried a tab nav — the nav was rendered into `#detail` by JS — so it\n  needs no edit at all.\n- `public/format.js` keeps `fmtDur`; timeline.js still uses it.\n\n=== Rejected alternatives ===\n\n- Leaving the tab renderers in place but unreachable: decision 7 of the issue\n  rejects it, and every state field and fetch would survive with them.\n- Keeping the six `/* --- section --- */` banners as empty headings: a banner\n  over nothing is the stale prose the previous increment was rejected for.\n- Keeping `.panel > h3` and the table rules \"in case a panel grows a heading\":\n  the criterion is that no selector survives that no renderer can emit, and git\n  holds them.\n- Deleting `public/format.js`'s `fmtDur` as well: timeline.js still calls it,\n  so it is live.\n",
            "moduleMap": "tools/argus-ui/public/timeline.js — pure timeline module (no document/fetch/location). Holds buildLanes, buildDensity, contextPoints, areaPolygon, activityMarks, laneKeyOf, laneByKey, spanLaneKeys, laneGeometry, toolCallOf, mergeToolMarks, liveCursor, scrubCursor, resolveCursor, renderTimeline — and the two things this increment deletes, DETAIL_VIEWS (lines 32-40) and renderDetailViews (lines 504-516, last in the file).\ntools/argus-ui/public/app.js — the whole page: the `state` literal (line 32), api(), renderStats, renderSessionList, renderDetail (168), the nine tab renderers and their helpers (222-722), loaders loadSessions/loadStats/loadSession/loadTimeline/loadLaneContext/loadTabData (845-1000), renderLanePanel (946), refresh (1003), selectSession (1040), paintCursor/scrubTo/scheduleRefresh/connectStream, wireEvents (1142) with its delegated click/change/pointerdown/input listeners on #detail, and boot() (1306) with the 15-second interval. Entry point: `boot()` at the bottom.\ntools/argus-ui/public/styles.css — the single stylesheet, in banner-comment sections. This increment empties the tab-nav rules (460-491), the kpi tiles and every table rule (495-607, keeping .panel), the whole waterfall section (609-785), the whole events section (1235-1316), two chip tones (450-458), `.mono` (89-94) and one `.waterfall` rule inside the 900px media query.\ntools/argus-ui/README.md — the user-facing page. Opening sentence (3-4), the Timeline bullet's trailing clause (66-67) and the six view bullets (72-79) are what change.\ntools/argus-ui/test/page.test.mjs — every case about app.js, asserted over its source text; helpers walk/functionSource/detailListener/callArguments at the top. Three cases deleted, two amended, twelve added.\ntools/argus-ui/test/timeline.test.mjs — the timeline module's own suite, over real return values. Three cases deleted, one added, two names dropped from its import.\ntools/argus-ui/test/server.test.mjs — the proxy's suite, driving createServer against a fake collector on port 0. One source-level case appended.\ntools/argus-ui/src/server.mjs — unchanged. Proxies on `pathname.startsWith('/api/')` and knows no route by name.\ntools/argus-ui/public/context.js, public/format.js, public/index.html — unchanged. index.html never carried the tab nav; it was rendered into #detail by app.js.",
            "environment": "Node v22.22.2 is installed and is the only runtime needed. tools/argus-ui has zero dependencies and no build step, so nothing is installed and no `npm install` is run before anything below.\n\nThe two commands the checks run, from the repository root:\n- `npm --prefix tools/argus-ui test` — the package's own script, `node --test \"test/*.test.mjs\"`, over config/context/independence/page/server/timeline. It starts HTTP servers on port 0.\n- `./test.sh` — the repository-wide gate: test-repo.sh, test-worktree.sh, the backlog recorder's suite, and the argus, argus-ui and log-parser packages. It re-runs the argus-ui suite; that duplication is accepted because both suites are named criteria of this increment.\n\nWhile working on one file: `node --test tools/argus-ui/test/page.test.mjs` (likewise timeline.test.mjs, server.test.mjs). This is not part of the checks.\n\nThere is no linter, no formatter and no type checker in this repository — no eslint, prettier or tsconfig config exists at the root or in tools/argus-ui. There is nothing else to run.",
            "testPlan": "TESTS ARE NEEDED.\n\nNothing is red today. I did not run either suite: this increment is a deletion\nof code the current suite pins, so the first red is the one the test-author\ncreates, and a baseline run would buy no fact I could not state from reading.\nThe three cases in test/timeline.test.mjs and the three in test/page.test.mjs\nnamed under \"Cases to delete\" pass today and would go red the moment the\nimplementer deletes the code — which is why the test-author deletes them in the\nsame change rather than leaving them red or skipped.\n\n== The suite, and its conventions ==\n\nFramework: `node:test` with `node:assert/strict`, no runner and no dependency.\nNode 22 is installed; nothing needs installing.\n\nTwo styles live side by side, and each file keeps its own:\n\n- test/timeline.test.mjs and test/context.test.mjs import the pure module and\n  assert over the value or the HTML string it returns. Fixtures are tiny\n  factory functions at the top of the file (`session()`, `record()`,\n  `toolMark()`, `threeRecordContent()`), and cases are grouped under\n  `// Criterion N — …` or `// Increment N — …` banner comments. Nothing is\n  faked: the modules are pure.\n- test/page.test.mjs never executes app.js. There is no DOM and no jsdom. It\n  reads `public/app.js` as text and asserts over the source with regexes, using\n  four helpers already at the top of the file: `walk(dir)` (every file under\n  public/, recursively), `functionSource(source, name)` (the text of one\n  top-level function up to the next one), `detailListener(source, type)` (the\n  body of one delegated listener on `#detail` up to the next\n  `.addEventListener(`) and `callArguments(source, name)` (the argument text of\n  one call). Every case in that file is written this way; a new case that tries\n  to run the page instead would be the odd one out.\n- test/server.test.mjs starts a fake collector with `node:http` on port 0 and\n  drives `createServer` from `../src/server.mjs`. One new source-level case is\n  added there; it reads the file rather than starting a server.\n\nCase names are lowercase sentences describing the behaviour (\"the page opens\nlive\", \"an open dropdown survives a repaint\"), never \"should\" or a function\nname. Every assertion carries a message saying what breaks if it fails, in the\nsame voice as the file it lands in.\n\nRunning one file, from the repository root:\n  node --test tools/argus-ui/test/page.test.mjs\n  node --test tools/argus-ui/test/timeline.test.mjs\n  node --test tools/argus-ui/test/server.test.mjs\n\n== Cases to delete (superseded, not skipped) ==\n\ntest/timeline.test.mjs\n- Remove `DETAIL_VIEWS,` and `renderDetailViews,` from the import at lines 8-9.\n  Keep `import * as timeline from '../public/timeline.js';` at line 24 — it is\n  unused today and the first new case below uses it.\n- Delete `the timeline names exactly six technical views, each with a label`\n  (97-104).\n- Delete `opening a session with nothing selected offers every technical view\n  and opens none` (106-114).\n- Delete `selecting one technical view marks exactly that one as selected`\n  (116-123).\n- The banner comment at 94-95 headed those three; replace it with the banner\n  for the new case below.\n\ntest/page.test.mjs\n- Delete `the page loads with no technical view open` (73-91) — it asserts\n  `tab: null` in the state literal.\n- Delete `selecting a session returns to the timeline` (93-102) — it asserts\n  `state.tab = null` in selectSession.\n- Delete `the timeline is rendered above the technical views` (104-123) — it\n  asserts `renderDetailViews(` and `id=\"tab-body\"` in renderDetail.\n- Amend `the context panel has a container of its own, between the timeline and\n  the technical views` (373-382): rename it to say the container sits below the\n  timeline, drop the `viewsIdx` lookup and the assertion that uses it, and keep\n  the `timelineIdx < panelIdx` assertion.\n- Amend `the scrub control's input reaches the scrub` (332-341): drop the third\n  assertion, the one ordering `timeline-scrub` before `event-search`. The first\n  two assertions stay unchanged.\n\n== New cases, per criterion ==\n\nCriterion 1 — DETAIL_VIEWS and renderDetailViews are gone, and no tab nav is\nrendered anywhere.\n\n1. `the timeline module exports no tab nav`\n   Level: unit. File: test/timeline.test.mjs, under a new banner comment\n   `// Increment 4 — the six technical tabs go out of the code.`\n   Input: the namespace import `timeline` already at line 24.\n   Expected: `assert.ok(!('DETAIL_VIEWS' in timeline))` and\n   `assert.ok(!('renderDetailViews' in timeline))`, each with a message saying\n   the tab nav was deleted rather than merely unreferenced.\n   Red until: the two exports are deleted from public/timeline.js.\n\n2. `no file under public renders a tab nav`\n   Level: unit (source scan). File: test/page.test.mjs, under a new banner\n   `// Increment 4 — the six technical tabs go out of the code.`\n   Input: `walk(PUBLIC)` over every file under public/, styles.css and\n   index.html included.\n   Expected: no file contains any of the strings `data-tab`, `role=\"tablist\"`,\n   `tab-body`, `class=\"tabs\"`, `aria-label=\"Technical views\"`. Collect\n   offenders into a `problems` array of `` `${relative} names ${needle}` `` and\n   `assert.deepEqual(problems, [])`, the shape the two existing scanning cases\n   in that file already use.\n   Edge covered: this is the one case that also catches a leftover `.tabs`\n   selector in the stylesheet and a stray `#tab-body` in index.html.\n\nCriterion 2 — the renderers, helpers, container and state fields are gone from\napp.js.\n\n3. `app.js declares none of the tab renderers`\n   Level: unit (source). File: test/page.test.mjs.\n   Input: app.js as text.\n   Expected: for each of `renderTabBody`, `renderOverviewTab`, `renderTodosTab`,\n   `renderTracesTab`, `renderWaterfall`, `renderSpanInspector`,\n   `renderEventsTab`, `renderMetricsTab`, `renderRawTab`, `loadTabData`, `kpi`,\n   `formatValue`, `spanKind`, `spanNote`, `todoStatusChip`, assert app.js\n   contains neither `function <name>(` nor `async function <name>(`; and assert\n   app.js does not match `/\\bSPAN_KINDS\\b/`. Gather into one `problems` array so\n   a failure names every survivor at once.\n   Note for the author: match on the declaration form, not on the bare word —\n   `kpi` as a bare word is short enough to collide with prose.\n\n4. `the page state carries no tab, trace, event or metric field`\n   Level: unit (source). File: test/page.test.mjs.\n   Input: the state literal, sliced the way the neighbouring cases do it —\n   `appJs.indexOf('const state = {')` to `appJs.indexOf('\\n};', start)`.\n   Expected: the slice matches none of `/\\btab:/`, `/\\btrace:/`,\n   `/\\bselectedTraceId:/`, `/\\bselectedSpanId:/`, `/\\bevents:/`,\n   `/\\beventFilters:/`, `/\\bmetrics:/`, `/\\bfacets:/`; and the whole of app.js\n   matches none of `/\\bstate\\.tab\\b/`, `/\\bstate\\.trace\\b/`,\n   `/\\bstate\\.selectedTraceId\\b/`, `/\\bstate\\.selectedSpanId\\b/`,\n   `/\\bstate\\.events\\b/`, `/\\bstate\\.eventFilters\\b/`, `/\\bstate\\.metrics\\b/`,\n   `/\\bstate\\.facets\\b/`.\n   Edge: the whole-file half is what catches a field deleted from the literal\n   but still written to somewhere else. `state.` prefixes are required on the\n   whole-file half so `addEventListener` and `wireEvents` cannot false-positive\n   on `events`.\n   Also assert positively, in the same case, that the literal still carries\n   `contextHidden: new Set()` and `contextFilterOpen: false` — the deletion must\n   not take the filter's state with it.\n\nCriterion 3 — the wiring is gone, and the interval no longer repaints a tab\nbody.\n\n5. `the click handler acts on no tab, trace, span or event row`\n   Level: unit (source). File: test/page.test.mjs.\n   Input: `detailListener(appJs, 'click')`.\n   Expected: the slice matches none of `/data-tab/`, `/data-trace/`,\n   `/data-span/`, `/data-event-seq/`, `/data-detail-seq/`; and still matches\n   `/data-lane/`, `/data-ctx-all/` and `/summary\\[data-ctx-filter\\]/`, so the\n   case goes red both when a removed branch survives and when a surviving\n   branch is taken out with it.\n\n6. `the change handler is the filter's alone`\n   Level: unit (source). File: test/page.test.mjs.\n   Input: `detailListener(appJs, 'change')`.\n   Expected: no `/event-filter/` and no `/event-errors/`; still matches\n   `/input\\[data-ctx-entry\\]/` and `/renderLanePanel\\(/`.\n\n7. `the input handler is the scrub's alone`\n   Level: unit (source). File: test/page.test.mjs.\n   Input: `detailListener(appJs, 'input')`.\n   Expected: no `/event-search/` and no `/searchTimer/`; still matches\n   `/timeline-scrub/` and `/scrubTo\\(/`.\n\n8. `the slow interval repaints the session list and nothing else`\n   Level: unit (source). File: test/page.test.mjs.\n   Input: `functionSource(appJs, 'boot')`.\n   Expected: matches `/setInterval\\(/` and `/renderSessionList\\(\\)/`; matches\n   neither `/renderTabBody/` nor `/state\\.tab/`.\n\nCriterion 4 — no counts object, and no refresh asks for traces, facets or\nmetrics.\n\n9. `renderDetail builds no counts object`\n   Level: unit (source). File: test/page.test.mjs.\n   Input: `functionSource(appJs, 'renderDetail')`.\n   Expected: does not match `/\\bconst\\s+counts\\b/`. Still matches\n   `/renderTimeline\\(/`, `/id=\"lane-panel\"/` and `/renderLanePanel\\(/`.\n   Note: `session.counts.apiErrors` stays in that function, so the assertion\n   must be on the declaration, never on the word `counts`.\n\n10. `no refresh asks the collector for traces, facets or metrics`\n    Level: unit (source). File: test/page.test.mjs.\n    Input: app.js as text.\n    Expected: matches none of `/\\/api\\/traces/`, `/\\/api\\/facets/`,\n    `/\\/api\\/metrics/`. Paired with the existing case `the page asks the\n    collector for the tool calls, incrementally`, which already pins that\n    loadTimeline still requests `/api/events` with `TOOL_EVENT` and `sinceSeq`\n    — leave that case exactly as it is.\n\nCriterion 5 — no unused import and no dead helper is left behind.\n\n11. `every name a module under public imports is used below the import`\n    Level: unit (source scan). File: test/page.test.mjs.\n    Input: public/app.js, public/timeline.js, public/context.js.\n    Method: for each file, take every `import { … } from '…';` statement, split\n    its braces on commas, strip `as` aliases and whitespace, and require each\n    name to occur at least twice in the whole file (the import plus one use) —\n    count with `new RegExp('\\\\b' + name + '\\\\b', 'g')`.\n    Expected: `problems` empty. It is empty today for timeline.js and\n    context.js — every name there occurs at least twice — and goes red for\n    app.js until `renderDetailViews` and `fmtDur` are dropped from its imports.\n\n12. `every function app.js declares is called somewhere in it`\n    Level: unit (source scan). File: test/page.test.mjs.\n    Input: app.js as text.\n    Method: collect every `(?:async )?function (\\w+)\\(` at the start of a line\n    and require each name to occur at least twice in the file.\n    Expected: `problems` empty. `boot` qualifies through its `boot();` call at\n    the bottom. This is the case that catches a `kpi()` or `formatValue()` left\n    behind after its only caller went.\n\nCriterion 6 — the stylesheet keeps no rule only a removed view could produce.\n\n13. `the stylesheet has no rule for the removed views`\n    Level: unit (source). File: test/page.test.mjs.\n    Input: public/styles.css as text.\n    Expected: the file matches none of this closed list, each checked\n    separately so a failure names the survivor:\n    `/^\\.tabs\\b/m`, `/^\\.tab\\b/m`, `/\\.tab .count/`, `/\\bkpi/`, `/\\.table-scroll/`,\n    `/^table\\s*\\{/m`, `/^th\\s*\\{/m`, `/^td\\s*\\{/m`, `/^tbody\\b/m`, `/\\.panel > h3/`,\n    `/\\.trace-pill/`, `/\\.trace-picker/`, `/\\.waterfall/`, `/\\.axis-tick/`,\n    `/\\.span-row/`, `/\\.span-bar/`, `/\\.span-label/`, `/\\.span-track/`,\n    `/\\.span-duration/`, `/\\.span-inspector/`, `/\\.detail-note/`, `/\\.filters/`,\n    `/\\.event-row/`, `/\\.event-time/`, `/\\.event-name/`, `/\\.event-summary/`,\n    `/\\.attr-table/`, `/data-tone=\"ok\"/`, `/data-tone=\"warn\"/`, `/\\.mono\\b/`,\n    `/\\.bad\\b/`, `/\\.good\\b/`.\n    And, positively, that it still carries `.panel {`, `.timeline`, `.lane`,\n    `.ctx-filter`, `.ctx-block`, `.chip[data-tone=\"live\"]` and\n    `.chip[data-tone=\"error\"]`, so the case goes red if the deletion overshoots.\n\n14. `no class the stylesheet styles is one no source can emit`\n    Level: unit (source scan). File: test/page.test.mjs.\n    Input: styles.css plus every other file under public/ via `walk(PUBLIC)`.\n    Method: collect every class token in styles.css with\n    `/\\.([A-Za-z][\\w-]*)/g`. Collect the tokens the other files can emit: every\n    whitespace-separated token inside a `class=\"…\"` attribute, plus every\n    single- or double-quoted string literal that is itself a bare token\n    (`/^[A-Za-z][\\w-]*$/`) — the second half is what covers\n    `${session.name ? 'session-name' : 'session-id'}`, which is emitted through\n    a ternary rather than written into a class attribute.\n    Expected: every styles.css token is in the emitted set; `problems` empty.\n    This is the criterion's \"no selector is left that no renderer can emit\"\n    clause, and it is why `.mono`, `.bad` and `.good` are on the deletion list —\n    all three are unemitted once the tabs go, and `.mono` already is today.\n\nCriterion 7 — the documentation stops promising the removed views.\n\n15. `no page under tools/argus-ui promises a removed view`\n    Level: unit (source scan). File: test/page.test.mjs, using a\n    `PROJECT = fileURLToPath(new URL('../', import.meta.url))` constant of its\n    own alongside the existing `PUBLIC` and `TEST_DIR`.\n    Input: every `*.md` file directly under the project (README.md, CLAUDE.md).\n    Expected: none of them matches `/- \\*\\*Overview\\*\\*/`, `/- \\*\\*Tasks\\*\\*/`,\n    `/- \\*\\*Traces\\*\\*/`, `/- \\*\\*Events\\*\\*/`, `/- \\*\\*Metrics\\*\\*/`,\n    `/- \\*\\*Attributes\\*\\*/`, `/\\btabs?\\b/i`, or `/the views below it/`.\n    Note for the author: the bullet form is deliberate — README.md keeps the\n    phrase \"Server-Sent Events\", and `\\btabs?\\b` does not match \"table\" or\n    \"tables\", so neither the options table nor its prose trips the case.\n\nCriterion 8 — the listeners the rest of the page needs survive.\n\n16. `a live refresh still repaints the timeline and the lane panel, dropdown\n    left open`\n    Level: unit (source). File: test/page.test.mjs.\n    Input: `functionSource(appJs, 'refresh')`,\n    `functionSource(appJs, 'renderDetail')`,\n    `functionSource(appJs, 'renderLanePanel')`.\n    Expected: refresh matches `/renderDetail\\(\\)/`; renderDetail matches\n    `/renderTimeline\\(/` and `/renderLanePanel\\(/`; renderLanePanel matches\n    `/filterOpen:\\s*state\\.contextFilterOpen\\b/`.\n    The rest of this criterion is already pinned by cases that stay untouched\n    and go red if the deletion takes a listener with it: `clicking a lane\n    selects it, and clicking it again lets go`, `selecting a lane fetches its\n    context`, `unchecking an entry repaints and fetches nothing`, `all on and\n    all off go through the pure helper against the held record, and fetch\n    nothing`, `an open dropdown survives a repaint`, `the selection survives a\n    lane change and a session change`, `the scrub control's input reaches the\n    scrub` (amended above), `a drag is registered before the next refresh can\n    fire`, `a full render repaints the panel`, `live mode follows new requests\n    into the panel`. The test-author must leave all of them in place.\n\nCriterion 9 — src/server.mjs knows no route by name.\n\n17. `the proxy knows no collector route by name`\n    Level: unit (source). File: test/server.test.mjs, appended at the end,\n    reading the file with `fs.readFileSync(fileURLToPath(new\n    URL('../src/server.mjs', import.meta.url)), 'utf8')` — that file does not\n    import `node:fs` today, so add the two imports it needs.\n    Expected: the source matches `/pathname\\.startsWith\\('\\/api\\/'\\)/` and does\n    not match `/\\/api\\/\\w/` — it may name the prefix and nothing under it.\n    This is what makes \"src/server.mjs is unchanged\" checkable: it goes red the\n    moment the deletion teaches the proxy a route.\n\n== Left untested, and why ==\n\n- That the deleted views are actually unreachable in a browser: there is no DOM\n  in this suite and adding one would be a dependency the project forbids. The\n  source-level absence of every renderer, its state and its wiring is the whole\n  of what can be checked here, and it is what the criteria are written against.\n- The rendered appearance of the stylesheet after the deletion: CSS is not\n  executed anywhere in this suite. Cases 13 and 14 check which selectors exist,\n  not what they look like.\n- The README's new opening sentence, beyond the absence checks of case 15: its\n  wording is prose and a test that pinned it would break on every edit.\n- `public/context.js`, `public/format.js` and `public/index.html`: unchanged by\n  this increment, and their own suites already cover them.\n",
            "checks": [
              "npm --prefix tools/argus-ui test",
              "./test.sh"
            ],
            "questions": [],
            "needsTests": true,
            "summary": "Delete the six pre-timeline tabs whole — DETAIL_VIEWS and renderDetailViews from timeline.js, nine renderers, six helpers, eight state fields, four wiring branches and loadTabData from app.js, the stylesheet rules and README bullets only they could produce — and pin the deletion with seventeen new cases in page.test.mjs, timeline.test.mjs and server.test.mjs while deleting the six cases that held the removed views in place."
          }
        },
        {
          "label": "tests:tabs-out.0",
          "at": "2026-08-07T16:48:28.844Z",
          "return": {
            "summary": "Wrote the failing tests for increment 4 (tabs-out) across tools/argus-ui/test/timeline.test.mjs, test/page.test.mjs and test/server.test.mjs: deleted the three superseded cases in each of the first two files, amended two more, and added the 17 new cases the plan named — 13 of them currently red against the untouched app.js/timeline.js/styles.css/README.md (`node --test tools/argus-ui/test/timeline.test.mjs`: 77 tests, 1 fail, exit 1; `node --test tools/argus-ui/test/page.test.mjs`: 63 tests, 12 fail, exit 1), the remaining 4 (imports-used, functions-called, live-refresh-survives, server-proxy-unnamed) pass today because they pin behaviour this increment does not touch or that already holds, and stand as regression guards (`node --test tools/argus-ui/test/server.test.mjs`: 9 tests, 0 fail, exit 0).",
            "cases": [
              {
                "case": "Remove DETAIL_VIEWS, and renderDetailViews, from the timeline.test.mjs import; keep the unused `import * as timeline` line.",
                "file": "tools/argus-ui/test/timeline.test.mjs",
                "testName": "",
                "expected": "The two names are dropped from the named import; `import * as timeline` stays for the new case.",
                "got": "Done — the import block now reads buildLanes, laneGeometry, renderTimeline, buildDensity, contextPoints, areaPolygon, activityMarks, MIN_CURVE_WIDTH_PCT, ACTIVITY_BUCKETS, mergeToolMarks, liveCursor, scrubCursor, resolveCursor, toolCallOf, spanLaneKeys, laneByKey; `import * as timeline from '../public/timeline.js';` is unchanged."
              },
              {
                "case": "Delete 'the timeline names exactly six technical views, each with a label'",
                "file": "tools/argus-ui/test/timeline.test.mjs",
                "testName": "",
                "expected": "Case removed rather than skipped.",
                "got": "Deleted."
              },
              {
                "case": "Delete 'opening a session with nothing selected offers every technical view and opens none'",
                "file": "tools/argus-ui/test/timeline.test.mjs",
                "testName": "",
                "expected": "Case removed rather than skipped.",
                "got": "Deleted."
              },
              {
                "case": "Delete 'selecting one technical view marks exactly that one as selected'; replace the banner comment at 94-95 with the new one",
                "file": "tools/argus-ui/test/timeline.test.mjs",
                "testName": "",
                "expected": "Case removed, banner replaced with '// Increment 4 — the six technical tabs go out of the code.'",
                "got": "Deleted and banner replaced."
              },
              {
                "case": "the timeline module exports no tab nav",
                "file": "tools/argus-ui/test/timeline.test.mjs",
                "testName": "the timeline module exports no tab nav",
                "expected": "assert.ok(!('DETAIL_VIEWS' in timeline)) and assert.ok(!('renderDetailViews' in timeline)), each with a message saying the tab nav was deleted rather than merely unreferenced.",
                "got": "Red today: AssertionError 'the tab nav was deleted from timeline.js, not merely left unreferenced' (expected true, actual false) — DETAIL_VIEWS is still exported by public/timeline.js."
              },
              {
                "case": "Delete 'the page loads with no technical view open' (asserts tab: null in the state literal)",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "",
                "expected": "Case removed rather than skipped.",
                "got": "Deleted."
              },
              {
                "case": "Delete 'selecting a session returns to the timeline' (asserts state.tab = null in selectSession)",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "",
                "expected": "Case removed rather than skipped.",
                "got": "Deleted."
              },
              {
                "case": "Delete 'the timeline is rendered above the technical views' (asserts renderDetailViews( and id=\"tab-body\" in renderDetail)",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "",
                "expected": "Case removed rather than skipped.",
                "got": "Deleted."
              },
              {
                "case": "Amend 'the context panel has a container of its own, between the timeline and the technical views': rename to say the container sits below the timeline, drop the viewsIdx lookup and its assertion, keep timelineIdx < panelIdx",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the context panel has a container of its own, below the timeline",
                "expected": "Only the timelineIdx < panelIdx assertion remains, under the renamed title.",
                "got": "Amended as asked; still green (currently true, untouched by this increment's remaining work) — `node --test tools/argus-ui/test/page.test.mjs` shows it passing."
              },
              {
                "case": "Amend \"the scrub control's input reaches the scrub\": drop the third assertion ordering timeline-scrub before event-search",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the scrub control's input reaches the scrub",
                "expected": "The first two assertions (timeline-scrub present, scrubTo( called) stay; the ordering assertion against event-search is gone.",
                "got": "Amended as asked; still green."
              },
              {
                "case": "no file under public renders a tab nav",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "no file under public renders a tab nav",
                "expected": "walk(PUBLIC) over every public/ file finds none of data-tab, role=\"tablist\", tab-body, class=\"tabs\", aria-label=\"Technical views\"; problems collected as `${relative} names ${needle}` and asserted empty.",
                "got": "Red today: problems = ['app.js names data-tab', 'app.js names tab-body', 'app.js names class=\"tabs\"' (via renderDetailViews call site text), 'timeline.js names data-tab', 'timeline.js names role=\"tablist\"', 'timeline.js names class=\"tabs\"', 'timeline.js names aria-label=\"Technical views\"', and 'styles.css names class=\"tabs\"'-style survivors] — the tab nav is still rendered and styled."
              },
              {
                "case": "app.js declares none of the tab renderers",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "app.js declares none of the tab renderers",
                "expected": "For renderTabBody, renderOverviewTab, renderTodosTab, renderTracesTab, renderWaterfall, renderSpanInspector, renderEventsTab, renderMetricsTab, renderRawTab, loadTabData, kpi, formatValue, spanKind, spanNote, todoStatusChip, app.js matches neither `function <name>(` nor `async function <name>(`; app.js also does not match /\\bSPAN_KINDS\\b/. Matched on the declaration form, not the bare word.",
                "got": "Red today: problems lists every one of the fifteen names plus SPAN_KINDS as still declared in public/app.js."
              },
              {
                "case": "the page state carries no tab, trace, event or metric field",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the page state carries no tab, trace, event or metric field",
                "expected": "The state literal slice matches none of tab:, trace:, selectedTraceId:, selectedSpanId:, events:, eventFilters:, metrics:, facets:; the whole file matches none of state.tab, state.trace, state.selectedTraceId, state.selectedSpanId, state.events, state.eventFilters, state.metrics, state.facets; the literal still carries contextHidden: new Set() and contextFilterOpen: false.",
                "got": "Red today: 'the state literal must carry no field matching /\\btab:/ — the tabs took it with them' — app.js's state literal still declares tab: null and the other seven fields."
              },
              {
                "case": "the click handler acts on no tab, trace, span or event row",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the click handler acts on no tab, trace, span or event row",
                "expected": "detailListener(appJs, 'click') matches none of data-tab, data-trace, data-span, data-event-seq, data-detail-seq, and still matches data-lane, data-ctx-all and summary[data-ctx-filter].",
                "got": "Red today: 'the click handler must act on no /data-tab/ branch the removed tabs left behind' — the [data-tab] branch is still in wireEvents()'s click listener."
              },
              {
                "case": "the change handler is the filter's alone",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the change handler is the filter's alone",
                "expected": "detailListener(appJs, 'change') has no event-filter and no event-errors; still matches input[data-ctx-entry] and renderLanePanel(.",
                "got": "Red today: \"the removed events tab's event-name filter must go from the change handler\" — event-filter is still handled."
              },
              {
                "case": "the input handler is the scrub's alone",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the input handler is the scrub's alone",
                "expected": "detailListener(appJs, 'input') has no event-search and no searchTimer; still matches timeline-scrub and scrubTo(.",
                "got": "Red today: \"the removed events tab's search box must go from the input handler\" — event-search is still handled and searchTimer still exists."
              },
              {
                "case": "the slow interval repaints the session list and nothing else",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the slow interval repaints the session list and nothing else",
                "expected": "functionSource(appJs, 'boot') matches setInterval( and renderSessionList(); matches neither renderTabBody nor state.tab.",
                "got": "Red today: 'the slow interval must no longer repaint a tab body that no longer exists' — boot()'s setInterval still calls renderTabBody() when state.tab === 'overview'."
              },
              {
                "case": "renderDetail builds no counts object",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "renderDetail builds no counts object",
                "expected": "functionSource(appJs, 'renderDetail') does not match /\\bconst\\s+counts\\b/; still matches renderTimeline(, id=\"lane-panel\" and renderLanePanel(.",
                "got": "Red today: 'renderDetail must no longer build the counts object only the removed tab nav read' — renderDetail still builds `const counts = { traces: …, events: …, metrics: … }`."
              },
              {
                "case": "no refresh asks the collector for traces, facets or metrics",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "no refresh asks the collector for traces, facets or metrics",
                "expected": "app.js matches none of /\\/api\\/traces/, /\\/api\\/facets/, /\\/api\\/metrics/.",
                "got": "Red today: 'no refresh may ask the collector for traces any more' — loadTabData() still fetches /api/traces/<id>, /api/facets and /api/metrics."
              },
              {
                "case": "every name a module under public imports is used below the import",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "every name a module under public imports is used below the import",
                "expected": "For app.js, timeline.js and context.js, every imported name occurs at least twice (import plus one use). Empty today for timeline.js/context.js; goes red for app.js once renderDetailViews and fmtDur stop being used elsewhere but are left in the import.",
                "got": "Green today (not red): renderDetailViews and fmtDur are both still used at least once beyond their import (renderDetailViews in renderDetail, fmtDur across renderOverviewTab/renderWaterfall/renderSpanInspector), so no dangling import exists yet. This is a forward-looking regression guard rather than a currently-failing case — its purpose is to catch the implementer if the two imports are left behind once their only callers are deleted."
              },
              {
                "case": "every function app.js declares is called somewhere in it",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "every function app.js declares is called somewhere in it",
                "expected": "Every `(?:async )?function (\\w+)(` at the start of a line occurs at least twice in the file. boot qualifies through its boot(); call.",
                "got": "Green today (not red): every function app.js currently declares is still called at least once, since nothing has been deleted yet — this is a regression guard against a helper (e.g. kpi() or formatValue()) surviving its only caller's removal, not a case pinning missing behaviour today."
              },
              {
                "case": "the stylesheet has no rule for the removed views",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the stylesheet has no rule for the removed views",
                "expected": "styles.css matches none of the closed list of 31 removed-view selectors; still carries .panel {, .timeline, .lane, .ctx-filter, .ctx-block, .chip[data-tone=\"live\"] and .chip[data-tone=\"error\"].",
                "got": "Red today: problems lists most of the 31 patterns as still present (.tabs, .tab, .tab .count, kpi, .table-scroll, table {, th {, td {, tbody, .panel > h3, .trace-pill, .trace-picker, .waterfall, .axis-tick, .span-row, .span-bar, .span-label, .span-track, .span-duration, .span-inspector, .detail-note, .filters, .event-row, .event-time, .event-name, .event-summary, .attr-table, data-tone=\"ok\", data-tone=\"warn\", .mono, .bad, .good) — the removed views' rules are all still in styles.css."
              },
              {
                "case": "no class the stylesheet styles is one no source can emit",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "no class the stylesheet styles is one no source can emit",
                "expected": "Every class token styles.css defines is emittable by some file under public/, via class=\"…\" attributes or a bare quoted string literal.",
                "got": "Red today: problems = ['styles.css styles .mono, which no source under public/ can emit', 'styles.css styles .bad, which no source under public/ can emit', 'styles.css styles .good, which no source under public/ can emit'] — these three are already dead before this increment touches anything, exactly as the plan notes ('.mono already is today'). Implementing note: the two quote kinds must be scanned with separate matchAll passes rather than one alternation regex — a combined `/'([^']*)'|\"([^\"]*)\"/g` lets an outer double-quoted class attribute (`class=\"${cond ? 'a' : 'b'}\"`) swallow the single-quoted literals inside it before they can be matched, which produced a false positive on .session-id/.session-name until split into two passes."
              },
              {
                "case": "no page under tools/argus-ui promises a removed view",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "no page under tools/argus-ui promises a removed view",
                "expected": "No *.md file directly under tools/argus-ui matches the six removed-view bullets, /\\btabs?\\b/i, or /the views below it/.",
                "got": "Red today: README.md matches all six bullet patterns (- **Overview**, - **Tasks**, - **Traces**, - **Events**, - **Metrics**, - **Attributes**). Note: README.md's current sentence 'the\\n  views below it are reachable from there' is line-wrapped, so the literal /the views below it/ does not match it as written today — the bullets alone already turn this case red, so the case still proves the point, but this literal-phrase check will not catch a reworded multi-line variant of that sentence; flagged in openQuestions."
              },
              {
                "case": "a live refresh still repaints the timeline and the lane panel, dropdown left open",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "a live refresh still repaints the timeline and the lane panel, dropdown left open",
                "expected": "refresh matches renderDetail(); renderDetail matches renderTimeline( and renderLanePanel(; renderLanePanel matches filterOpen: state.contextFilterOpen.",
                "got": "Green today (not red): all three already hold in the current app.js, since criterion 8 asks only that this increment's deletion not take these listeners with it — the untouched code already satisfies it. It stands as the regression guard the plan asks for; the rest of criterion 8 is covered by the untouched pre-existing cases the plan names, which were left in place."
              },
              {
                "case": "the proxy knows no collector route by name",
                "file": "tools/argus-ui/test/server.test.mjs",
                "testName": "the proxy knows no collector route by name",
                "expected": "src/server.mjs matches /pathname\\.startsWith\\('\\/api\\/'\\)/ and matches no /\\/api\\/\\w/ — it may name the prefix and nothing under it.",
                "got": "Green today (not red): src/server.mjs already satisfies this — its PROXIED constant checks pathname.startsWith('/api/') and names no route under it — because this criterion asks that server.mjs stay unchanged by this increment, which it already is. This is the regression guard the plan describes ('it goes red the moment the deletion teaches the proxy a route'), added with the two fs/url imports server.test.mjs needed."
              }
            ],
            "openQuestions": [
              "Four of the seventeen new cases (case 11 'every name … used below the import', case 12 'every function app.js declares is called', case 16 the live-refresh/lane-panel survival case, and case 17 the server.mjs route-naming case) pass today rather than fail — each pins behaviour this increment does not touch or that already holds, matching the plan's own framing ('it goes red the moment …'), but this differs from the general instruction to confirm every new case fails today; flagging so the implementer round doesn't read a passing regression guard as a sign the test is wrong.",
              "Case 15's /the views below it/ literal does not match README.md's current wording because the phrase is line-wrapped across two lines in the source; the case still goes red today on the six bullet patterns alone, but if the implementer rewords the timeline description without deleting the exact bullets, this sub-check could pass even though the reworded sentence still promises 'the views below it' with different line-wrapping — worth a human glance at the rewritten README rather than trusting this one regex.",
              "Case 14 depends on a source-scanning heuristic (bare-token quoted-literal scan) that can only approximate what a template literal actually emits; it is exactly as the plan specifies, but a class name that happens to coincide with an unrelated bare string literal anywhere under public/ would be wrongly counted as emittable — an acceptable false-negative risk the plan already accepts by name."
            ],
            "questions": []
          }
        }
      ]
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-07T14:59:54.673Z"
      },
      {
        "label": "replan:filter-core",
        "at": "2026-08-07T15:40:15.809Z"
      },
      {
        "label": "replan:filter-honesty",
        "at": "2026-08-07T16:00:26.345Z"
      },
      {
        "label": "replan:tools-box",
        "at": "2026-08-07T16:27:59.866Z",
        "return": {
          "increments": [
            {
              "id": "filter-core",
              "title": "The context panel gets a working block filter",
              "goal": "Give the context panel head a dropdown that lists the block kinds and top-level fields the shown request actually contains, and let the reader hide and show them, against the record the panel already holds.",
              "criteria": [
                "The context panel head carries a dropdown built as a `<details>` popover, with no framework and no new dependency, listing every block kind and every top-level field the currently shown request actually contains, one checkbox each, with the number of blocks behind that entry (e.g. `tool_result 12`).",
                "The list is in two labelled groups: **Blocks** first, holding the block kinds present (`assistant`, `other`, `raw`, `system`, `thinking`, `tool_result`, `tool_use`, `user`) sorted alphabetically; then **Fields**, holding the body's remaining top-level keys (`max_tokens`, `metadata`, `model`, `tools`, …) sorted alphabetically. Entries are labelled by the kind name and the field key verbatim, so `system` and `tools` read as one list.",
                "The dropdown holds two buttons, **all on** and **all off**. *All on* makes every entry visible. *All off* hides every entry the currently shown request contains.",
                "Unchecking an entry removes its blocks from the list below; checking it puts them back. Neither triggers a fetch: the filter is applied to the record the panel already holds, and `/api/content/at` is not called again.",
                "The selection is one page-wide state, held as the set of *hidden* entries. It survives a lane change, a scrub, a live refresh and a session change; it is not persisted and a page reload starts with everything visible. An entry never hidden is visible, which is what makes an entry that appears for the first time — a `thinking` block in a later request, a field an earlier body did not carry — arrive visible, including after *all off* was pressed on a request that did not contain it.",
                "An entry the current request does not contain is not listed, and its hidden state is remembered: scrubbing to a request that carries it again shows it still unchecked and its blocks still hidden.",
                "The derivation and the markup are pure functions in `public/context.js` — no `document`, no `fetch`, no `location`, the contract `test/independence.test.mjs` guards — and only the click wiring lives in `app.js`.",
                "The filter's markup carries no `data-lane` attribute and its `<summary>` carries no `data-block`, so opening the dropdown neither toggles the lane selection nor a block's expansion.",
                "An open dropdown survives a repaint: a live refresh that re-renders `#lane-panel` does not snap it shut, the same way `state.expanded` keeps expanded blocks open today.",
                "`npm --prefix tools/argus-ui test` passes after this increment, and each criterion above has at least one test that goes red when its behaviour is broken or removed."
              ],
              "status": "done",
              "note": ""
            },
            {
              "id": "filter-honesty",
              "title": "The panel head names visible and total, and the empty case says so",
              "goal": "Make the filtered panel tell the truth about how much it is hiding: the head names visible-of-total blocks and characters, and a panel with everything hidden shows a placeholder instead of an empty list.",
              "criteria": [
                "With nothing hidden, the head line reads as it does today (`210.114 chars · 34 blocks`). With something hidden it names both numbers — visible of total — for blocks and for characters, so the filter can never make a context look smaller than it is. The totals stay readable off `data-chars` and `data-blocks`; the visible counts arrive in data attributes of their own.",
                "With every entry hidden the panel keeps its `data-state=\"ready\"` and shows a placeholder in place of the rows, saying every kind is hidden and where to turn them back on.",
                "The head's new numbers and the placeholder are derived and rendered by pure functions in `public/context.js`, under the contract `test/independence.test.mjs` guards; no new wiring is needed in `app.js` for either.",
                "The cases the previous increment wrote to pin today's behaviour — that a partly filtered panel leaves the head's totals untouched, and that an all-hidden panel renders an empty block list with no placeholder — are updated to the new behaviour in the same change, so no case is left red, skipped or asserting the superseded shape.",
                "`npm --prefix tools/argus-ui test` passes over the whole suite after this increment, and each criterion above has at least one test that goes red when its behaviour is broken or removed."
              ],
              "status": "done",
              "note": ""
            },
            {
              "id": "tools-box",
              "title": "The tools box under the timeline goes, its tick marks stay",
              "goal": "Delete the redundant tool list panel and the per-call parameter ballast it forced into page state, while leaving every other tool signal on the timeline exactly as it is.",
              "criteria": [
                "`public/tools.js` and `test/tools.test.mjs` are deleted, and `app.js` imports neither `laneToolInput` nor `renderToolPanel`.",
                "`renderLanePanel()` paints only the context panel into `#lane-panel`.",
                "Everything else about tools on the timeline is untouched: the `data-kind=\"tool\"` tick marks on the lane track, the `data-tools` count in each lane's meta, the `tool call` legend entry, and the incremental `/api/events?event=claude_code.tool_result` poll with its `sinceSeq` watermark.",
                "`toolCallOf()` keeps only what a mark needs — `seq`, `timeMs`, `spanId` — and no longer derives or holds `name`, `preview`, `text`, `chars` or `truncated`. `TOOL_PARAM_CHARS` and the `paramText()` helper go with it. `mergeToolMarks()` keeps its contract: duplicates dropped by `seq`, the watermark returned as the highest `seq` actually held, the input array untouched.",
                "The context panel comes through the deletion whole: it is still built from one input builder, the filter still renders, hides and reopens, the head still names visible-of-total blocks and characters when something is hidden, and an all-hidden record still shows its placeholder with `data-state=\"ready\"`.",
                "Every case outside the deleted file that asserts the old tool-mark shape or the tool panel's presence — whole-object assertions on `toolCallOf()`'s return among them — is updated or removed in the same change, so no case is left red, skipped or asserting the superseded shape.",
                "`npm --prefix tools/argus-ui test` passes over the whole suite after this increment, and each criterion above has at least one test that goes red when its behaviour is broken or removed."
              ],
              "status": "done",
              "note": "Closed done on this call. Accepted in review round 1: public/tools.js and test/tools.test.mjs are gone, renderLanePanel() assigns only the context panel, toolCallOf() returns just seq/timeMs/spanId with TOOL_PARAM_CHARS and paramText() removed, mergeToolMarks() keeps its contract, and the tick marks, data-tools count, legend and incremental tool poll are untouched; the whole argus-ui suite ran 223 cases at exit 0. Round 0 was rejected for one defect only: the README still promised the deleted tool list, which round 1 removed."
            },
            {
              "id": "tabs-out",
              "title": "The six technical tabs go out of the code",
              "goal": "Remove the pre-timeline tab interface — its views, its state, its wiring, its per-refresh fetches, and everything left over that only it could produce — and land the whole issue with both suites green.",
              "criteria": [
                "`DETAIL_VIEWS` and `renderDetailViews()` are gone from `public/timeline.js`, and no tab nav is rendered anywhere.",
                "Gone from `public/app.js`: `renderTabBody()`, `renderOverviewTab()`, `renderTodosTab()`, `renderTracesTab()`, `renderWaterfall()`, `renderSpanInspector()`, `renderEventsTab()`, `renderMetricsTab()`, `renderRawTab()`, `loadTabData()`, the helpers only they used (`kpi`, `formatValue`, `spanKind`, `spanNote`, `todoStatusChip`, `SPAN_KINDS`), the `#tab-body` container, and the state fields `tab`, `trace`, `selectedTraceId`, `selectedSpanId`, `events`, `eventFilters`, `metrics`, `facets`.",
                "Gone from the wiring in `wireEvents()`: the handlers for `[data-tab]`, `[data-trace]`, `[data-span]` and `[data-event-seq]`, the `change` handler for `event-filter` and `event-errors`, and the `event-search` branch of the `input` handler. The 15-second interval no longer repaints a tab body.",
                "`renderDetail()` no longer builds the `counts` object, and no refresh requests `/api/traces`, `/api/facets` or `/api/metrics`. The `/api/events` request that feeds the tool marks stays.",
                "No unused import or dead helper is left behind in `app.js` or `timeline.js`, and the tests that pinned the removed views are removed with them rather than left skipped.",
                "Nothing survives elsewhere in the interface that only a removed view could produce: the stylesheet keeps no rule for a tab nav, a tab body or any of the six views' markup, and no selector is left that no renderer can emit.",
                "The interface's own documentation stops promising the removed views: the README no longer describes Overview, Tasks, Traces, Events, Metrics or Attributes as things a reader can open, and no other page under `tools/argus-ui` still names a tab. The previous increment was rejected in review for exactly this kind of stale prose, so it is part of the deletion rather than a follow-up.",
                "The `change` and `input` listeners the context filter and the rest of the page still need survive the removal: the filter's checkboxes, the *all on* and *all off* buttons and the lane selection all still work, and the live refresh still repaints the timeline and the lane panel with an open dropdown left open.",
                "`src/server.mjs` is unchanged: it proxies `/api/*` blindly and knows no route by name.",
                "`npm --prefix tools/argus-ui test` is green over the whole suite, and each criterion above has at least one test that goes red when its behaviour is broken or removed.",
                "`./test.sh` is green."
              ],
              "status": "todo",
              "note": "Re-cut after tools-box: two criteria added, both learned from that increment rather than from the issue. The reviewer rejected its first round solely for a README sentence that still promised the deleted panel, so removing the six tabs' README bullets is now named as part of this increment; and the same deletion had to take the stylesheet rules only the deleted module could produce, so the dead-CSS rule is named here too. Criterion 8 also now says the live refresh must still repaint the timeline and lane panel with the dropdown left open, which is the regression the filter increments would be most likely to lose to this deletion."
            }
          ],
          "questions": [],
          "summary": "Closed tools-box as done — the review accepted it in correction round 1, with the whole argus-ui suite at 223 cases, exit 0. One increment stays open, tabs-out, and I left it as one increment rather than splitting it: removing the six tabs is a single deletion, and any half of it (views without wiring, wiring without state) leaves the page referring to things that are gone, which is the definition of an increment that does not stand on its own. Two criteria were added to it, both taught by the run rather than by the issue. First, tools-box was rejected on its first review round for exactly one defect — the README still promised the panel it had deleted — and its research step recorded that the README's Overview/Tasks/Traces/Events/Metrics/Attributes bullets belong to this increment; so keeping documentation honest is now a criterion here rather than a defect waiting to be found. Second, that same deletion had to take the stylesheet rules no renderer could produce any more, so the dead-CSS rule is stated up front. I also sharpened the existing criterion about surviving listeners to say the live refresh must still repaint the timeline and the lane panel with an open dropdown left open — the two filter increments built that behaviour, and a deletion that rips the interval and the change/input wiring apart is where it would most plausibly be lost. Nothing else changed: the criteria carried over from the issue keep their wording and their increment, and no criterion moved between increments."
        }
      }
    ]
  }
}
