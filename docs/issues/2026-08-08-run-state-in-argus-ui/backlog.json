{
  "version": 1,
  "issue": "docs/issues/2026-08-08-run-state-in-argus-ui",
  "workflow": "agile-loop",
  "codemap": "tools/argus-ui/public/index.html — the page's markup, where the run view's shell and the switch between it and the session view sit.\ntools/argus-ui/public/app.js — the page's boot, fetching, SSE stream handling and view switching; the one place the new view is wired in.\ntools/argus-ui/public/<run view>.js — a new page module for the run view, beside app.js, context.js, timeline.js and format.js; the interface's rule is one module with one suite, and what to call it is the researcher's.\ntools/argus-ui/public/format.js — the shared formatters the page already uses for times and numbers; touched only if the run view needs a format it does not already carry.\ntools/argus-ui/public/styles.css — the run view's styling.\ntools/argus-ui/test/<run view>.test.mjs — that new module's suite, required by the one-suite-per-module rule.\ntools/argus-ui/test/page.test.mjs — the page suite that scans public/ and asserts the page's structure, including the new markup.\ntools/argus-ui/test/independence.test.mjs — the independence guard; it names every owned module in two explicit lists, so both have to cover whatever module the view adds.\ntools/argus-ui/test/CLAUDE.md — that suite's doc, which owns its conventions and where a new case belongs.\ntools/argus-ui/README.md — the interface's user-facing page: what it now shows.\ntools/argus-ui/CLAUDE.md — the interface's conventions page, which names its modules and its one-suite-per-module rule.\n\nAlready changed by the collector increment, and not to be changed again unless the remaining work needs it: tools/argus/src/store.mjs (latest state per run, listing, the kind-discriminated change stream), tools/argus/src/server.mjs (POST /api/runs, GET /api/runs, GET /api/runs/:id, the uncoalesced run SSE frame), tools/argus/src/persist.mjs (runs.jsonl append and replay), tools/argus/test/store.test.mjs, tools/argus/test/server.test.mjs, tools/argus/test/persist.test.mjs, tools/argus/test/CLAUDE.md, tools/argus/README.md, tools/argus/CLAUDE.md.\n\nAlready changed by the recorder increment, and not to be changed again: skills/agent-brief/assets/backlog.mjs (the best-effort POST of every document it writes), skills/agent-brief/assets/backlog.test.mjs, skills/agent-brief/assets/CLAUDE.md.\n\nDeliberately not in the map, each checked: tools/argus/bin/argus.mjs and tools/argus/src/config.mjs (run state rides the existing --persist and --open paths and needs no flag); tools/argus-ui/src/server.mjs and tools/argus-ui/src/config.mjs (the reverse proxy already forwards everything under /api/, streams included); skills/argus/SKILL.md (its endpoint table is an explicit subset and does not go stale); workflows/agile-loop.js and every agent page under skills/ and agents/ (the issue forbids changing how an agent calls the recorder, and the recorder increment changed none of them).",
  "increments": [
    {
      "id": "collector",
      "title": "The collector accepts, keeps, persists, serves and announces a run's state",
      "goal": "Give the collector an endpoint of its own that accepts a run's backlog state, keep the latest state per run, serve the runs it holds and the full state of one over its JSON API behind the existing authentication, persist it across a restart, and announce every write on the existing SSE stream.",
      "criteria": [
        "A POST of a run's state to an endpoint of the collector's own is accepted and identified by the issue the state belongs to; a second POST for the same run replaces what was held for it, so the collector keeps the latest state per run and no history.",
        "The JSON API answers both questions: the runs the collector holds, with what a reader needs to pick one including when each was last written, and the full state of one named run; asking for a run it does not hold answers 404.",
        "The new ingest endpoint and the new read endpoints sit behind the collector's existing authentication and reject an unauthenticated caller exactly as its other endpoints do.",
        "A collector restarted against the same measurement directory still serves the run state it held, persisted the way the sessions it already holds are persisted.",
        "A write to a run's state puts a frame on the existing /api/stream SSE stream that tells an already-connected client which run changed.",
        "tools/argus/README.md documents what the collector now accepts and serves, and tools/argus/test/CLAUDE.md and tools/argus/CLAUDE.md are current for the cases and modules this increment adds, in the same commit as the change.",
        "npm --prefix tools/argus test exits 0 and ./test.sh exits 0."
      ],
      "status": "done",
      "note": "Accepted at review round 0 with no findings: npm --prefix tools/argus test (179 cases, exit 0) and ./test.sh (6 suites, exit 0). It fixed the wire contract the two open increments build against: POST /api/runs with the run id and the whole backlog document, GET /api/runs listing id, issue, workflow, increment count and updatedAtMs latest-write-first, GET /api/runs/:id serving one state whole with 404 for a run not held, all behind the existing token gate, persisted as runs.jsonl and announced as an uncoalesced SSE frame naming the run and its update time. Merged into claude/argus-workflow-backlog-status-nvqmvr.",
      "branch": "claude/argus-workflow-backlog-status-nvqmvr--collector",
      "steps": []
    },
    {
      "id": "recorder",
      "title": "The recorder pushes the state it just wrote, best-effort and invisible",
      "goal": "Make the recorder send the run's state to the configured collector after every write it makes, without changing anything an agent or a workflow sees and without ever costing a run a failure, a wait or a line of output.",
      "criteria": [
        "init, record, branch and close each send the state they just wrote to the collector's run-state endpoint, identified by the issue the state belongs to, so a collector that receives it then serves that exact document under that issue.",
        "The recorder stays the only writer of backlog.json and keeps its zero dependencies; no agent page and no workflow prompt changes, so how an agent calls the recorder is exactly what it is today.",
        "The collector's address and its credential are read from the same OTLP collector environment the rest of uroboros already exports for argus, and from nowhere else.",
        "With no collector configured in the environment, the recorder does nothing extra.",
        "A collector that is unreachable, slow or refusing costs the recorder neither a non-zero exit nor a wait beyond a short timeout, and produces no output: the exit codes and the one confirmation line of every subcommand stay byte-identical to today's.",
        "skills/agent-brief/assets/CLAUDE.md is current for the cases and helpers this increment adds, in the same commit as the change.",
        "node --test skills/agent-brief/assets/backlog.test.mjs exits 0 and ./test.sh exits 0."
      ],
      "status": "done",
      "note": "Accepted at review round 0 with no findings: node --test skills/agent-brief/assets/backlog.test.mjs (30 cases, exit 0) and ./test.sh (6 suites, exit 0). One send site after the dispatch switch POSTs { id, state } to <base>/api/runs after init, record, branch and close, read sending nothing; the address and credential come from OTEL_EXPORTER_OTLP_ENDPOINT/OTEL_EXPORTER_OTLP_HEADERS with UROBOROS_OBS_URL/UROBOROS_OBS_TOKEN as the second name pair, and from nowhere else; the send is bounded by a 2s AbortSignal timeout inside an empty catch, so an unreachable, stalling or refusing collector costs no exit code, no wait and no output. Verified on Node 20, 21 and 22. No agent page, workflow prompt or argus file changed. Merged into claude/argus-workflow-backlog-status-nvqmvr.",
      "branch": "claude/argus-workflow-backlog-status-nvqmvr--recorder",
      "steps": []
    },
    {
      "id": "ui",
      "title": "argus-ui gains a run view: one run's backlog, switchable and live",
      "goal": "Give the interface a run view beside its session view that shows a run's backlog as the collector serves it, opens on the most recently written run, lets the reader switch runs, and updates itself over the collector's SSE stream.",
      "criteria": [
        "The interface shows a run view beside its existing session view, holding for the run it shows: the issue and the workflow it belongs to and when it was last written; the increments in the backlog's order, each with its id, title, status, branch and note; a count of how many increments are closed and how many are open; and the codemap.",
        "A closed increment reads as closed, with its status and its note.",
        "The view opens on the run written to most recently and the reader can switch to any other run the collector holds, taking both the list of runs and the one run's whole state from the collector's run endpoints and from no other source.",
        "A write to the shown run's state reaches an already-open page live over the collector's Server-Sent-Events stream, whose frame names only which run changed and when: no polling, no reload, no manual refresh.",
        "The existing session view, its timeline and its stream handling keep working unchanged beside the new view.",
        "argus-ui still imports nothing outside itself and reads no uroboros file; test/independence.test.mjs still guards exactly that rule, covers every module this increment adds, and passes.",
        "tools/argus-ui/README.md says what the interface now shows, and tools/argus-ui/test/CLAUDE.md and tools/argus-ui/CLAUDE.md are current for the cases and modules this increment adds, in the same commit as the change.",
        "npm --prefix tools/argus-ui test exits 0 and ./test.sh exits 0."
      ],
      "status": "todo",
      "note": "Split from the single UI increment the last cut carried, because the collector and recorder increments together showed how much this half now has to land at once: a second view in a page built for one, a run picker, a second kind of SSE frame, and the whole of a backlog's shape on screen. This increment takes the load-bearing half — the view, its data path and its live update — and the step returns follow in ui-steps.",
      "branch": "claude/argus-workflow-backlog-status-nvqmvr--ui",
      "steps": [
        {
          "label": "research:ui.0",
          "at": "2026-08-08T11:17:06.820Z",
          "return": {
            "needsTests": true,
            "plan": "## What gets built\n\nA run view beside the session view in argus-ui: a second sidebar listing the runs the\ncollector holds, a second detail pane showing one run's backlog, a switch between the\ntwo views, and a live reload driven by the collector's `run` SSE frame. No file under\n`tools/argus` is touched; argus-ui reads run state only over `/api/runs` and\n`/api/runs/:id`, which its own proxy already forwards.\n\n## The wire contract you build against (verified in the collector's source)\n\n- `GET /api/runs` answers `{ total, items }`. Each item is\n  `{ id, issue, workflow, increments, updatedAtMs }` — `increments` here is a NUMBER\n  (the count), and there is no `state`. The list is latest-write-first, so `items[0]`\n  is the run written to most recently.\n- `GET /api/runs/:id` answers one entry whole:\n  `{ id, issue, workflow, increments: <number>, updatedAtMs, state: <document> }`, or\n  404 `{ error: 'unknown run' }`.\n- The `state` document is the backlog file as the recorder writes it:\n  `{ version, issue, workflow, codemap, increments: [ { id, title, goal, criteria, status, note, branch, steps } ], run: { steps: [...] } }`.\n  `status` is `'todo'` until a close, then one of `'done' | 'blocked' | 'dropped'`;\n  `note` and `branch` are `''` when unset; `close` empties `steps`.\n- **The sharp edge**: the entry's `increments` is a count, the document's\n  `state.increments` is the array. Read every increment, and both counts, from\n  `run.state.increments`; `run.increments.map(...)` is a crash.\n- The SSE stream: `event: run` with `data: {\"id\":\"…\",\"updatedAtMs\":123}`, sent\n  uncoalesced and immediately, alongside the existing `hello` and `ingest` events.\n  The frame carries no state, so the handler has to fetch.\n- The collector holds the document opaquely, so treat every field as possibly absent:\n  a missing `codemap`, a missing `increments` array or a `state` that is not the\n  shape above must render a head and a placeholder, never a crash and never the strings\n  `undefined` or `NaN`.\n\n## New module: public/run.js\n\nNamed `run.js` (suite `test/run.test.mjs`), beside `timeline.js` and `context.js` and\nfollowing their rule exactly: pure functions returning HTML strings, no browser global —\nno `document.`, no `fetch(`, no `location.` — because `independence.test.mjs` scans\nfor those three patterns and the suite imports the module directly into `node --test`.\nIt imports `esc` and `fmtAgo` from `./format.js` and nothing else.\n\nExports:\n\n- `CLOSED_STATUSES` — `new Set(['done', 'blocked', 'dropped'])`, the three statuses the\n  recorder's `close` writes. Anything else, `'todo'` and `''` included, is open.\n- `isClosedIncrement(increment)` — membership in that set.\n- `incrementCounts(increments)` — `{ total, closed, open }` over the document's array,\n  tolerating a non-array by counting zero.\n- `pickRunId(items, wanted)` — the run to show: `wanted` when the list holds it, else\n  `items[0]?.id`, else `null`. This is the whole of \"opens on the run written to most\n  recently\": the collector already sorts latest-write-first, so the page sorts nothing.\n- `runFrame(data)` — parse one SSE `run` frame's `data` string to\n  `{ id, updatedAtMs }`, or `null` for malformed JSON, a non-object or a missing id.\n  A bad frame must cost nothing.\n- `renderRunList({ items, selectedId })` — the picker's `<li>` markup: one\n  `<button type=\"button\" class=\"run-card\" data-run=\"<id>\" aria-current=\"true|false\">`\n  per run, carrying the issue (falling back to the id), the workflow, `fmtAgo(updatedAtMs)`\n  and the increment count. No runs renders `<li class=\"placeholder\">No runs yet</li>`,\n  mirroring `renderSessionList`.\n- `renderRun(run)` — the whole of `#run-detail` for one entry: a head with the issue as\n  the title, the run id and the workflow beneath it, and chips for when it was last\n  written (`fmtAgo(run.updatedAtMs)` as the text, the exact instant as\n  `new Date(run.updatedAtMs).toISOString()` in a `data-updated` attribute so a test can\n  pin it without depending on the machine's locale or clock) and for the closed and open\n  counts; then one card per increment in the document's order, each with its id, title,\n  status, branch and note, marked `data-closed=\"true\"` when closed and carrying its\n  status chip; then the codemap in a `<pre>`. `renderRun(null)` returns a\n  `<div class=\"empty\">` explaining that no run state has reached the collector yet.\n  Everything printed goes through `esc`.\n\nSteps — `increment.steps` and `state.run.steps` — are increment `ui-steps` and are not\nrendered here. Do not leave a container or a helper for them.\n\n## public/index.html\n\nAdd, without touching the existing session markup:\n\n- `<body data-view=\"sessions\">` — the one flag the stylesheet switches on.\n- In `.topbar-actions`, a switch:\n  `<nav class=\"view-switch\" aria-label=\"View\"><button type=\"button\" class=\"view-button\" data-view=\"sessions\" aria-current=\"true\">Sessions</button><button type=\"button\" class=\"view-button\" data-view=\"runs\" aria-current=\"false\">Runs</button></nav>`.\n- In `.layout`, after the existing sidebar and detail: an\n  `<aside class=\"sidebar\" id=\"run-sidebar\" aria-label=\"Runs\">` with a `.sidebar-head`\n  holding `<h2>Runs</h2>` and `<span class=\"counter\" id=\"run-count\">0</span>`, and\n  `<ol class=\"run-list\" id=\"run-list\"></ol>`; then\n  `<section class=\"detail\" id=\"run-detail\" aria-label=\"Run\"></section>`.\n\nNever the strings `data-tab`, `class=\"tabs\"`, `role=\"tablist\"`, `tab-body` or\n`aria-label=\"Technical views\"` — `page.test.mjs` fails on any of them anywhere under\n`public/`. The switch is `data-view`, not `data-tab`.\n\n## public/app.js\n\nImports `{ pickRunId, renderRun, renderRunList, runFrame }` from `./run.js` and nothing\nmore; import no name you do not use, because `page.test.mjs` fails an import whose name\nappears only once in the file.\n\nState gains `runs: []`, `selectedRunId: null`, `run: null`, `view: 'sessions'`. Add no\nfield matching `tab`, `trace`, `events`, `metrics` or `facets` — the state literal is\npinned against those.\n\nNew functions (every one has to be called somewhere, or `page.test.mjs` fails):\n\n- `renderRunPicker()` — writes `renderRunList(...)` into `#run-list` and the count into\n  `#run-count`. Named so it does not shadow the imported `renderRunList`.\n- `renderRunView()` — writes `renderRun(state.run)` into `#run-detail`.\n- `loadRuns()` — `api('/api/runs')`, sets `state.runs`, then\n  `state.selectedRunId = pickRunId(state.runs, state.selectedRunId)`.\n- `loadRun()` — with no selection, `state.run = null`; else\n  `api(\\`/api/runs/\\${encodeURIComponent(id)}\\`)` into `state.run`, guarded by the\n  same stale-answer check the timeline loader uses (`if (state.selectedRunId !== id) return;`)\n  and swallowing a failure into `state.run = null` rather than throwing past the render.\n- `refreshRuns(changedId = null)` — `await loadRuns()`; reload the shown run when\n  `changedId` is null (boot and explicit switch) or equals `state.selectedRunId`, or when\n  nothing was selected before; then `renderRunPicker()` and `renderRunView()`. The whole\n  body is wrapped so a collector without run support costs the page nothing.\n- `selectRun(id)` — no-op when already selected; sets `state.selectedRunId`, writes\n  `location.hash = \\`#/run/\\${encodeURIComponent(id)}\\``, loads the run and repaints both\n  containers.\n- `setView(name)` — sets `state.view`, `document.body.dataset.view = name`, and\n  `aria-current` on each `[data-view]` button.\n\nWiring, each on its own element so no existing delegated listener on `#detail` changes:\n\n- `#run-list` click → `event.target.closest('[data-run]')` → `selectRun(...)`.\n- `.view-switch` (or its two buttons) click → `setView(button.dataset.view)`.\n- `hashchange` and `boot` also match `/^#\\/run\\/(.+)$/`: set `state.selectedRunId` and\n  switch to the run view.\n- `connectStream()` gains `source.addEventListener('run', (event) => { setLive('live', 'live'); const frame = runFrame(event.data); if (frame) refreshRuns(frame.id); })`.\n  Leave the `hello` and `ingest` listeners byte-identical.\n- `boot()` calls `refreshRuns()` once, after `refresh()`. Its `setInterval` keeps\n  repainting the session list and nothing else.\n\n**No polling.** `refresh()` — the ingest-driven path — must not touch `loadRuns`,\n`loadRun` or `refreshRuns`, and no timer may fetch run state. Run data is fetched at\nboot, on a `run` frame, and on an explicit selection, and at no other time. That is the\ncriterion \"no polling, no reload, no manual refresh\" made structural.\n\n**The session view is untouched.** `renderDetail`, `renderLanePanel`, `loadTimeline`,\n`selectSession`, the `#detail` click/change/input/pointerdown listeners and the\n`session-search` debounce keep their current bodies. The run view writes only into\n`#run-list`, `#run-count` and `#run-detail`.\n\n## public/styles.css\n\nRules for the run view, keyed off the switch:\n`body[data-view=\"sessions\"] #run-sidebar, body[data-view=\"sessions\"] #run-detail { display: none }`\nand the mirror for `runs` hiding `#session-list`'s sidebar and `#detail`. Give the\nexisting `<aside class=\"sidebar\" aria-label=\"Sessions\">` an `id=\"session-sidebar\"` so\nthat rule has something to name; that is the only edit to existing markup.\n\nTwo hard constraints from `page.test.mjs`: every class the stylesheet names has to be\nemittable by some source under `public/`, so style only classes you actually render\n(`.run-list`, `.run-card`, `.run-card-top`, `.run-card-meta`, `.run-head`, `.run-title`,\n`.run-subtitle`, `.run-increment`, `.run-increment-head`, `.run-note`, `.run-codemap`,\n`.view-switch`, `.view-button` — the exact set is yours, but the two lists have to\nmatch); and no selector may begin `.tab`, and none of `.tabs`, `kpi`, `.waterfall`,\n`.mono`, `.bad`, `.good`, `data-tone=\"ok\"`, `data-tone=\"warn\"` may appear. Reuse\n`.chip`, `.chips`, `.empty`, `.placeholder`, `.counter`, `.sidebar`, `.sidebar-head`\nand `.detail` rather than adding parallel ones.\n\n## public/format.js — not touched\n\nChecked: the view needs `esc` and `fmtAgo`, both already exported, and an ISO string for\nthe exact write time, which is `Date#toISOString` and no formatter. Adding to\n`format.js` would be a change with no caller.\n\n## Documentation, in the same commit\n\n- `tools/argus-ui/README.md`: a `- **Runs** — …` bullet under \"What it shows\", saying the\n  view lists the runs the collector holds, opens on the most recently written one, and\n  shows for one run its issue, workflow, when it was last written, its increments in\n  backlog order with id, title, status, branch and note, how many are closed and how many\n  open, and its codemap, updating over the same SSE stream. **The word \"tab\" is banned in\n  every `.md` at `tools/argus-ui/`'s root** — a guard matches `/\\btabs?\\b/i` there. Call\n  it a view or a switch.\n- `tools/argus-ui/CLAUDE.md`: name `public/run.js` among the modules and keep the\n  one-suite-per-module sentence true (`test/run.test.mjs`). Same word ban applies.\n- `tools/argus-ui/test/CLAUDE.md` is the test-author's to update.\n\n## Rejected\n\n- **Rendering the run below the timeline in `#detail`.** One pane for two subjects, and\n  every `renderDetail()` an ingest fires would repaint the run too. A second pane keeps\n  the session view's contract exactly as it is.\n- **Reading run state inside `refresh()`.** Free to write, but it turns every telemetry\n  ingest into two more requests — polling by another name, which a criterion forbids.\n- **A `/api/runs` route in `tools/argus-ui/src/server.mjs`.** Unnecessary: `PROXIED`\n  already forwards everything under `/api/`, and `server.test.mjs` carries a source-text\n  guard that the proxy names no route under `/api/` — adding one goes red.\n- **Putting the run picker in the existing sidebar under the sessions.** It would make the\n  session filter box ambiguous and grow the one list two views compete for.\n- **Naming the module `backlog.js`.** The collector calls this a run and so do its\n  endpoints; a second name for one thing costs a translation on every read.",
            "moduleMap": "- `tools/argus-ui/public/run.js` — NEW. The pure run-view module: `CLOSED_STATUSES`, `isClosedIncrement`, `incrementCounts`, `pickRunId`, `runFrame`, `renderRunList`, `renderRun`. Imports `esc` and `fmtAgo` from `./format.js`; no browser global.\n- `tools/argus-ui/public/app.js` — the page's boot, fetching, SSE and rendering. Entry points touched: the `state` literal (adds `runs`, `selectedRunId`, `run`, `view`), new `renderRunPicker`, `renderRunView`, `loadRuns`, `loadRun`, `refreshRuns`, `selectRun`, `setView`, plus `connectStream` (a `run` listener), `wireEvents` (`#run-list` and `[data-view]` clicks, the `#/run/` hash) and `boot` (one `refreshRuns()`). `refresh`, `renderDetail`, `renderLanePanel` and every `#detail` listener keep their current bodies.\n- `tools/argus-ui/public/index.html` — the page shell. Adds `data-view` on `<body>`, the `.view-switch` nav in `.topbar-actions`, `#run-sidebar` with `#run-count` and `#run-list`, `#run-detail`, and `id=\"session-sidebar\"` on the existing sessions aside. Nothing existing is removed.\n- `tools/argus-ui/public/styles.css` — the run view's rules and the two `body[data-view=…]` visibility rules.\n- `tools/argus-ui/public/format.js` — NOT TOUCHED (correcting the codemap's \"touched only if\"): `esc` and `fmtAgo` already cover what the view formats.\n- `tools/argus-ui/test/run.test.mjs` — NEW. The pure module's suite.\n- `tools/argus-ui/test/page.test.mjs` — the source-text page suite; gains one section for this increment. Its helpers `functionSource`, `detailListener`, `callArguments`, `walk` are file-local and already there.\n- `tools/argus-ui/test/independence.test.mjs` — CORRECTION to the codemap: it names owned modules in THREE lists, not two — the project-completeness list (`'public/context.js'` etc., ~line 21), the `owned` scan-coverage list (~line 53), and the pure-module browser-global list `['public/context.js', 'public/timeline.js']` (~line 86). `public/run.js` belongs in all three.\n- `tools/argus-ui/test/CLAUDE.md` — the suite doc: the new file, its fixtures, and `public/run.js` under \"Real and executed\".\n- `tools/argus-ui/README.md` — \"What it shows\" gains the run view. No occurrence of the word \"tab\".\n- `tools/argus-ui/CLAUDE.md` — names `public/run.js` among the modules. No occurrence of the word \"tab\".\n- `tools/argus-ui/src/server.mjs`, `tools/argus-ui/src/config.mjs` — correctly absent from the map, verified: `const PROXIED = (pathname) => pathname.startsWith('/api/') || pathname.startsWith('/v1/');` already forwards both run endpoints and the stream, and `server.test.mjs` guards that the proxy names no route under `/api/`.\n- `tools/argus/**` — not touched. The endpoints, the persistence and the `run` SSE frame landed in the collector increment and are verified as described above.",
            "environment": "Node v22.22.2 is on the PATH; the project declares `>=20.11`. argus-ui has zero runtime and zero dev dependencies and there is no root `package.json`, so **no `npm install` is needed before any command below** and none may be added. There is no linter, no formatter and no type-check in this repository.\n\n- `npm --prefix tools/argus-ui test` — the whole interface suite (`node --test \"test/*.test.mjs\"`), run from the repository root.\n- `node --test tools/argus-ui/test/run.test.mjs` — the new module's suite alone, from the repository root.\n- `node --test tools/argus-ui/test/page.test.mjs` — the page suite alone, from the repository root.\n- `node --test tools/argus-ui/test/independence.test.mjs` — the independence guard alone, from the repository root.\n- `./test.sh` — every suite in the repository, from the repository root. It needs nothing installed.",
            "testPlan": "Tests are needed. Two levels only, both matching what this suite already does: the new module's functions are imported and executed; everything that lives in `app.js`, `index.html` and `styles.css` is asserted as source text, because `app.js` touches browser globals and is never imported and there is no DOM anywhere in this suite. That is the existing convention, recorded in `tools/argus-ui/test/CLAUDE.md`, and it holds here unchanged.\n\nI ran no command and no suite. `test/run.test.mjs` does not exist yet, so it is red until the module lands; nothing else is known to be red. The first run belongs downstream.\n\nThe suite doc `tools/argus-ui/test/CLAUDE.md` is present and accurate for the files that exist. It is incomplete for this increment in exactly three places, and correcting it is part of this step: \"The files\" lists no `run.test.mjs`; \"Helpers and fixtures\" lists no run fixtures; \"Real vs faked\" lists the pure modules as `timeline.js`, `context.js`, `format.js` and has to name `run.js` too. Add those, in that page's own voice, in the same commit.\n\nSection comments: every new case in an existing file goes under a new trailing section comment `// Increment ui — the run view beside the session view.`, appended at the end of the file, per that page's \"Where a new case goes\".\n\n---\n\n## A. test/run.test.mjs — NEW FILE, unit, `node --test` importing `../public/run.js`\n\nCommand that runs just it: `node --test tools/argus-ui/test/run.test.mjs`\n\nFixtures, file-local, each taking an `over` object spread last, in the style of `context.test.mjs`:\n\n- `increment(over)` → `{ id: 'ui', title: 'the run view', goal: '', criteria: [], status: 'todo', note: '', branch: '', steps: [] }`\n- `doc(over)` → the backlog document: `{ version: 1, issue: 'docs/issues/2026-08-08-x', workflow: 'agile-loop', codemap: 'a.js — a.\\nb.js — b.', increments: [increment()], run: { steps: [] } }`\n- `runEntry(over)` → the one-run answer: `{ id: 'docs/issues/2026-08-08-x', issue: 'docs/issues/2026-08-08-x', workflow: 'agile-loop', increments: 1, updatedAtMs: Date.now() - 5000, state: doc() }`\n- `listItem(over)` → a `/api/runs` item: `runEntry` without `state`.\n\nCases:\n\n**Criterion 1 — what the run view holds.**\n1. `renderRun(runEntry())` names the issue, the workflow and when the run was last written: the markup contains the issue text, the workflow text, the `fmtAgo` string for `updatedAtMs`, and `data-updated=\"<the ISO string of updatedAtMs>\"`. Assert the ISO attribute rather than a locale-formatted date, so the case does not depend on the machine's locale.\n2. One card per increment, in the document's order, each carrying its id, title, status, branch and note. Input: three increments with distinct ids `a`, `b`, `c`, the second one `{ status: 'done', note: 'accepted', branch: 'claude/x--b' }`. Assert each field's text is present and that `indexOf('a') < indexOf('b') < indexOf('c')` in the markup, so \"the backlog's order\" is proved rather than assumed.\n3. The counts. Input: increments with statuses `done`, `blocked`, `todo`, `''`. Expected: closed 2, open 2, both rendered, and the two summing to the number of increments.\n4. The codemap is rendered whole. Input: a codemap with two lines and an `&`. Expected: both lines present, the `&` escaped as `&amp;`.\n5. Everything is escaped: an increment title of `<script>alert(1)</script>`, a note containing `\"` and `'`, an issue containing `<`. Expected: no raw `<script` in the markup.\n6. `renderRun(null)` renders a placeholder that says no run state is held, and no increment card. Edge: this is what an empty collector looks like.\n7. The increments come from `state.increments` and never from the entry's count. Input: `runEntry({ increments: 99, state: doc({ increments: [increment({id:'a'}), increment({id:'b'})] }) })`. Expected: two cards, and the markup does not print `99`. This is the shape trap; the case exists to pin it.\n8. Edge — an opaque document: `runEntry({ state: { issue: 'i' } })` (no `increments`, no `codemap`, no `workflow`) renders the head and a placeholder in place of the increments, and the markup contains neither `undefined` nor `NaN`.\n9. Edge — `runEntry({ state: doc({ increments: [] }) })`: zero closed, zero open, a placeholder instead of an empty block.\n\n**Criterion 2 — a closed increment reads as closed.**\n10. `isClosedIncrement`: `done`, `blocked` and `dropped` are closed; `todo`, `''` and an unknown status are open. Assert all six, and assert `CLOSED_STATUSES` holds exactly those three — they are the recorder's, and a fourth invented here would silently miscount every run.\n11. `incrementCounts`: over the four-status fixture, `{ total: 4, closed: 2, open: 2 }`; over `[]`, `{ total: 0, closed: 0, open: 0 }`; over `undefined`, the same zeroes rather than a throw.\n12. A closed increment's card is marked closed and shows its status and its note; an open one carries neither the mark nor a note element. Input: one `{ status: 'done', note: 'accepted at round 0' }` and one `{ status: 'todo', note: '' }`. Expected: `data-closed=\"true\"` on the first card only, the word `done` and the note text inside it.\n13. Edge — a closed increment with an empty note still reads as closed: `{ status: 'dropped', note: '' }` renders the closed mark and the status, and prints no empty note element and no `undefined`.\n14. Edge — an increment with an empty branch renders no branch label at all, so the card does not show a dangling \"branch\" with nothing after it.\n\n**Criterion 3 — opening on the latest run, and switching.**\n15. `pickRunId`: `([], null)` → `null`; `([a, b], null)` → `'a'` (the collector serves latest-write-first, so the first item is the most recently written); `([a, b], 'b')` → `'b'`; `([a, b], 'gone')` → `'a'` — a run the collector no longer holds falls back to the latest rather than showing nothing.\n16. `renderRunList({ items: [a, b], selectedId: 'b' })`: one button per run with `data-run` carrying that run's id, `aria-current=\"true\"` on `b` and `aria-current=\"false\"` on `a`, and each row showing the run's issue and its `fmtAgo` time.\n17. `renderRunList({ items: [] })` renders a placeholder row, not an empty string. Edge: the same empty case the session list already handles.\n18. `renderRunList` escapes the id and the issue: an id containing `\"` does not break out of the `data-run` attribute.\n\n**Criterion 4 — the SSE frame.**\n19. `runFrame('{\"id\":\"docs/issues/x\",\"updatedAtMs\":1712}')` → `{ id: 'docs/issues/x', updatedAtMs: 1712 }`. This is the collector's frame verbatim.\n20. Edge — `runFrame` of `'not json'`, of `'{}'` (no id), of `'null'` and of `''` each return `null`, so a malformed frame costs the page nothing.\n\n---\n\n## B. test/page.test.mjs — existing file, source-text level, one new trailing section\n\nCommand that runs just it: `node --test tools/argus-ui/test/page.test.mjs`\n\nUse the file's existing helpers — `functionSource`, `detailListener`, `callArguments`, `walk` — and add none unless a case needs one. Note `functionSource(source, 'refresh')` anchors on the literal `function refresh(`, so it will not collide with `refreshRuns`.\n\n**Criterion 1 and 5 — the shell exists beside the session view's, which is untouched.**\n21. `index.html` carries the run view's shell: `id=\"run-sidebar\"`, `id=\"run-list\"`, `id=\"run-count\"`, `id=\"run-detail\"`, a `data-view=\"sessions\"` and a `data-view=\"runs\"` control, and `data-view` on `<body>`. In the same case assert it still carries `id=\"session-list\"`, `id=\"session-count\"`, `id=\"detail\"`, `id=\"session-search\"` and `id=\"setup-modal\"`, so the new shell cannot arrive by displacing the old one.\n22. `app.js` imports the run module (`/from\\s+['\"]\\.\\/run\\.js['\"]/`) and `index.html` still loads `/app.js` as a module.\n\n**Criterion 3 — the data comes from the run endpoints and nowhere else.**\n23. `app.js` asks `/api/runs` for the list and builds the one-run path from the selected id with `encodeURIComponent`; `loadRun`'s source matches `/\\/api\\/runs\\//` and `encodeURIComponent`. Assert too that the only network call in the run loaders is through the page's own `api(` helper — no bare `fetch(` and no second base URL — which is how \"from no other source\" is provable as text.\n24. The state literal carries `runs: []`, `selectedRunId: null`, `run: null` and a `view:` field, sliced the way the existing state case slices it (`const state = {` to the first `\\n};`).\n25. `pickRunId` decides what is shown: `loadRuns`'s source calls it with `state.runs` and `state.selectedRunId`. That, plus case 15, is the whole of \"opens on the most recently written run\".\n26. Clicking a run switches to it: the `#run-list` click listener matches `[data-run]` and calls `selectRun(`.\n27. The switch is wired: a `[data-view]` click reaches `setView(`, and `setView`'s source writes `state.view` and the body's `data-view`.\n\n**Criterion 4 — live, and not by polling.**\n28. `connectStream` registers `addEventListener('run'`, its handler parses the frame with `runFrame(` and calls `refreshRuns(`; the `hello` and `ingest` listeners are still there.\n29. No polling: `functionSource(appJs, 'refresh')` matches neither `/loadRuns\\(/` nor `/loadRun\\(/` nor `/refreshRuns\\(/`; `boot`'s `setInterval` still names `renderSessionList()` and names no run function; and no `setInterval` anywhere in `app.js` names a run loader. This is the case that makes \"no polling, no reload, no manual refresh\" checkable.\n\n**Criterion 5 — the session view keeps working.**\n30. The run view paints only in its own containers: `renderRunPicker`'s source names `run-list` and `run-count`, `renderRunView`'s names `run-detail`, and `functionSource(appJs, 'renderDetail')` names none of the three. The existing cases already pin the `#detail` click/change/input listeners and the lane panel, so do not restate them.\n\n**Criterion 7 — the documentation.**\n31. `tools/argus-ui/README.md` describes the run view: it names the runs list, the increments with their status, and the codemap. Assert also that neither `README.md` nor `CLAUDE.md` uses the word \"tab\" — the existing case `no page under tools/argus-ui promises a removed view` already fails on `/\\btabs?\\b/i`, so do not duplicate that assertion, only the positive one.\n\nThe existing case `no class the stylesheet styles is one no source can emit` already covers the new CSS with no edit; the existing `no file under public renders a tab nav` already covers the `data-view` naming choice. Add nothing for either.\n\n---\n\n## C. test/independence.test.mjs — existing file, three edits, no new case\n\n32. Criterion 6 is satisfied by adding `'public/run.js'` to all three lists: the project-completeness list in the first case, the `owned` list in the second, and `['public/context.js', 'public/timeline.js']` in the third, so the new module is checked for reaching outside the project and for touching `document.`, `fetch(` or `location.`. Keep the rule itself exactly as it is — the guard is not to be widened or relaxed.\n\nCommand: `node --test tools/argus-ui/test/independence.test.mjs`\n\n---\n\n## Left untested, deliberately\n\n- **Rendering in a real browser** — clicks, `EventSource` delivery, and whether the CSS actually hides a pane. This suite has no DOM by its own convention and `app.js` is never imported; the wiring is proved as source text and the markup as strings. A DOM harness would be a dependency, which the project forbids.\n- **The collector's endpoints, its persistence and its `run` frame** — owned by `tools/argus`'s suite and closed in the collector increment. Re-asserting them here would be a second implementation of someone else's contract.\n- **The proxy forwarding `/api/runs`** — `server.test.mjs` already covers the `/api/` prefix and its SSE pass-through; this increment changes no proxy code.\n- **Steps, their summary line and their expansion** — increment `ui-steps`.\n- **The exact wording of any placeholder or heading** — the implementer's, and pinning it would fail on a better sentence.",
            "checks": [
              "npm --prefix tools/argus-ui test",
              "./test.sh"
            ],
            "questions": [],
            "summary": "Add a pure public/run.js (counts, run picker, one run's backlog as HTML strings, SSE frame parsing) and wire it into index.html, app.js and styles.css as a switchable run view that reads GET /api/runs and GET /api/runs/:id, opens on the latest-written run, and reloads only on the collector's `run` SSE frame — never on a timer."
          }
        },
        {
          "label": "tests:ui.0",
          "at": "2026-08-08T11:30:19.429Z",
          "return": {
            "summary": "Wrote tools/argus-ui/test/run.test.mjs (20 cases against the not-yet-written public/run.js), appended 11 source-text cases to test/page.test.mjs under a new '// Increment ui' section, extended test/independence.test.mjs's three guarded lists with public/run.js, and updated test/CLAUDE.md's files/fixtures/real-vs-faked sections in the same commit; npm --prefix tools/argus-ui test now reports 264 cases, 250 pass, 14 fail, all for the missing run view.",
            "cases": [
              {
                "case": "renderRun(runEntry()) names the issue, the workflow and when it was last written, with the exact ISO instant in data-updated",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "the head names the issue, the workflow and when the run was last written, with the ISO instant in data-updated",
                "expected": "markup contains the issue text, the workflow text, fmtAgo(updatedAtMs), and data-updated=\"<ISO of updatedAtMs>\"",
                "got": "whole file fails to load: Cannot find module '.../public/run.js' (the module does not exist yet)"
              },
              {
                "case": "one card per increment, in the document's order, each carrying its id, title, status, branch and note; order proved via indexOf",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "one card per increment, in the document's own order, each carrying its id, title, status, branch and note",
                "expected": "three distinct-id increments (a, b, c; b closed with note+branch) all render their id/title/status/note/branch text, in document order",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'. Note: I proved order with distinguishable titles ('Card Alpha'/'Card Bravo'/'Card Charlie') rather than the bare literal characters a/b/c the plan's indexOf example names — see openQuestions."
              },
              {
                "case": "the counts: closed 2, open 2 over done/blocked/todo/'' statuses, both rendered and summing to the total",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "the closed and open counts are rendered, and sum to the number of increments",
                "expected": "incrementCounts gives {total:4, closed:2, open:2} and both numbers appear in renderRun's markup",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "the codemap is rendered whole, with both lines present and & escaped as &amp;",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "the codemap is rendered whole, escaped",
                "expected": "both codemap lines present in the markup and the raw & replaced by &amp;",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "everything is escaped: a <script> title, a note with quotes, an issue containing <",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "everything printed is escaped",
                "expected": "no raw <script appears anywhere in the markup",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "renderRun(null) renders a placeholder saying no run state is held, and no increment card",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "renderRun(null) is a placeholder with no increment card, for the collector holding no run at all yet",
                "expected": "non-empty markup carrying the page's existing empty/placeholder styling and no data-closed attribute",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'. I could not pin the exact wording (deliberately left to the implementer per the plan), so I asserted class=\"empty\" or class=\"placeholder\" — see openQuestions."
              },
              {
                "case": "the increments come from state.increments, never from the entry's own count (shape trap: increments:99)",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "the increments come from state.increments and never from the entry's own count",
                "expected": "two cards render (from state.increments) and the literal string 99 never appears",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "an opaque document with no increments/codemap/workflow still renders a head and a placeholder, no undefined/NaN",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "an opaque document carrying none of the known fields still renders a head and a placeholder, never undefined or NaN",
                "expected": "whatever the document carries reaches the page; no 'undefined', no 'NaN', no data-closed",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "zero increments: zero closed, zero open, a placeholder instead of an empty block",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "a run with zero increments shows zero closed and zero open, and a placeholder instead of an empty block",
                "expected": "incrementCounts([]) is {total:0,closed:0,open:0}; no data-closed, no undefined, no NaN in the markup",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "isClosedIncrement/CLOSED_STATUSES: done/blocked/dropped closed, todo/''/unknown open, exactly those three statuses",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "CLOSED_STATUSES holds exactly the recorder's three closing statuses, and isClosedIncrement matches them",
                "expected": "CLOSED_STATUSES === {'done','blocked','dropped'}; the six status/openness pairs all match",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "incrementCounts over four statuses, over [], over undefined",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "incrementCounts totals closed and open, tolerating an empty or a missing array",
                "expected": "{total:4,closed:2,open:2} / {total:0,closed:0,open:0} / {total:0,closed:0,open:0} for undefined, no throw",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "a closed increment's card is marked closed and shows status+note; an open one carries neither",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "a closed increment's card is marked closed and shows its status and its note; an open one carries neither the mark nor a note",
                "expected": "exactly one data-closed=\"true\", positioned before the open card; the closed card's own status and note text precede the open card's start; no 'undefined'",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "a closed increment with an empty note still reads as closed, no empty note element, no undefined",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "a closed increment with an empty note still reads as closed, printing no undefined",
                "expected": "data-closed=\"true\" and the status text present; no literal 'undefined' anywhere",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "an increment with an empty branch renders no branch label at all",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "an increment with an empty branch renders no branch label at all",
                "expected": "the word 'branch' does not appear anywhere in the markup",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "pickRunId: [] -> null; [a,b] with none picked -> first item's id; existing selection kept; a gone selection falls back to the latest",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "pickRunId opens on the run written to most recently, and falls back when the wanted run is gone",
                "expected": "pickRunId matches the four described outcomes exactly",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "renderRunList: one button per run, data-run carrying the id, aria-current true/false, issue and fmtAgo time shown per row",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "renderRunList renders one button per run, marking the selected one current, with its issue and time",
                "expected": "both buttons render with the right data-run/aria-current, and both rows' own issue and fmtAgo text",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "renderRunList({items:[]}) renders a placeholder row, not an empty string",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "renderRunList({ items: [] }) renders a placeholder row, not an empty string",
                "expected": "non-empty markup containing an <li but no data-run=",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "renderRunList escapes the id and the issue so a quote cannot break out of data-run",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "renderRunList escapes the id and the issue, so a quote cannot break out of the data-run attribute",
                "expected": "no raw onclick= and no raw <script> in the markup for a hostile id/issue",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "runFrame parses the collector's frame verbatim",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "runFrame parses the collector's frame verbatim",
                "expected": "runFrame('{\"id\":\"docs/issues/x\",\"updatedAtMs\":1712}') === {id:'docs/issues/x', updatedAtMs:1712}",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "runFrame of not-json / {} / null / '' each return null",
                "file": "tools/argus-ui/test/run.test.mjs",
                "testName": "a malformed frame costs the page nothing",
                "expected": "all four malformed inputs return null, no throw",
                "got": "whole file fails to load: Cannot find module '.../public/run.js'"
              },
              {
                "case": "index.html carries the run view's shell (run-sidebar/run-list/run-count/run-detail/data-view controls) and still carries the session shell",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "index.html carries the run view's shell, beside the session view's, which stays",
                "expected": "all six new ids/attributes present, plus the five existing session-view ids still present",
                "got": "'index.html must carry id=\"run-sidebar\"' (ERR_ASSERTION)"
              },
              {
                "case": "app.js imports the run module; index.html still loads app.js as a module",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "app.js imports the run module, and index.html still loads app.js as a module",
                "expected": "app.js matches /from\\s+['\"]\\.\\/run\\.js['\"]/; index.html still has <script type=\"module\" src=\"/app.js\">",
                "got": "'app.js must import run.js so the run view is reached by the page, not tested as an island' (ERR_ASSERTION)"
              },
              {
                "case": "loadRuns asks /api/runs, loadRun asks /api/runs/ with encodeURIComponent; both go through api(), never a bare fetch()",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the run loaders ask the collector's run endpoints, and nowhere else",
                "expected": "loadRuns/loadRun source matches the run endpoints and encodeURIComponent, call api(), and never call fetch( directly",
                "got": "'app.js must still declare loadRuns()' (ERR_ASSERTION — functionSource cannot find the not-yet-written function)"
              },
              {
                "case": "the state literal carries runs: [], selectedRunId: null, run: null, view: 'sessions'",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the state literal carries the run view's own fields",
                "expected": "all four fields present in the sliced state literal",
                "got": "'the page must hold the runs list it fetched' (ERR_ASSERTION)"
              },
              {
                "case": "loadRuns calls pickRunId with state.runs and state.selectedRunId",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "pickRunId decides what is shown, from the page's own runs and selection",
                "expected": "the pickRunId( call inside loadRuns is handed state.runs and state.selectedRunId",
                "got": "'app.js must still declare loadRuns()' (ERR_ASSERTION)"
              },
              {
                "case": "the #run-list click listener matches [data-run] and calls selectRun(",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "clicking a run in the picker switches to it",
                "expected": "a delegated click listener on #run-list recognises [data-run] and calls selectRun(",
                "got": "'app.js must delegate a click listener on #run-list' (ERR_ASSERTION)"
              },
              {
                "case": "a [data-view] click reaches setView(; setView writes state.view and document.body.dataset.view",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the view switch is wired to setView, which writes both the state and the body flag",
                "expected": "wireEvents wires a [data-view] branch calling setView(; setView's own source writes both fields",
                "got": "'wireEvents must wire a listener on [data-view]' (ERR_ASSERTION)"
              },
              {
                "case": "connectStream registers a 'run' listener that parses with runFrame( and calls refreshRuns(; hello/ingest listeners survive",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "connectStream reacts to the run frame, and the existing listeners stay",
                "expected": "connectStream still has hello/ingest listeners, plus a run listener calling runFrame( and refreshRuns(",
                "got": "'connectStream must register a run listener' (ERR_ASSERTION)"
              },
              {
                "case": "no polling: refresh() never calls loadRuns/loadRun/refreshRuns; boot's setInterval only names renderSessionList(); no setInterval anywhere names a run loader",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "no timer fetches run state",
                "expected": "none of refresh()/boot's interval/any setInterval body names loadRuns, loadRun or refreshRuns",
                "got": "currently passes (vacuously true): no run loaders exist yet, so nothing violates the guard. It is not a known-red case now, but it will fail the moment a timer is given one of those names, which is its purpose."
              },
              {
                "case": "renderRunPicker names run-list/run-count; renderRunView names run-detail; renderDetail names none of the three",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "the run view paints only in its own containers, and renderDetail is untouched by it",
                "expected": "renderRunPicker/renderRunView paint only their own containers; renderDetail's source names none of run-list/run-count/run-detail",
                "got": "'app.js must still declare renderRunPicker()' (ERR_ASSERTION)"
              },
              {
                "case": "README.md describes the run view: the runs list, the increments with status, and the codemap",
                "file": "tools/argus-ui/test/page.test.mjs",
                "testName": "README.md describes the run view: the runs list, the increments with status, and the codemap",
                "expected": "README.md names runs, increments, their status, and the codemap",
                "got": "'README must name the increments the run view shows' (ERR_ASSERTION)"
              },
              {
                "case": "independence.test.mjs: add 'public/run.js' to the project-completeness list, the owned list, and the browser-global-scan list; no new case, the rule itself unchanged",
                "file": "tools/argus-ui/test/independence.test.mjs",
                "testName": "the interface is a project of its own, with everything a project needs / nothing in the interface reaches outside the interface / the pure modules the tests import reach for no browser global",
                "expected": "public/run.js exists, is scanned for outside imports, and is scanned for document./fetch(/location.",
                "got": "'tools/argus-ui/public/run.js is missing' and (twice) 'the scan does not cover public/run.js — it is not there to check'"
              }
            ],
            "openQuestions": [
              "Case 2's plan wording asks for indexOf('a') < indexOf('b') < indexOf('c') as the order proof over increments with bare ids a/b/c. Common markup vocabulary for a card (e.g. classes containing 'card', 'branch', 'closed') is likely to contain those same single characters ahead of the actual card boundaries, which could make a literal single-character indexOf check fail against a correct implementation or pass vacuously against a wrong one. I proved the ordering with distinguishable titles ('Card Alpha'/'Card Bravo'/'Card Charlie') instead, while still asserting the bare ids' presence separately — flag if the literal single-character check was intended and needs to replace or supplement mine.",
              "Case 6 (renderRun(null)) and case 17/renderRunList empty-state markup: the plan leaves the exact placeholder wording and wrapper class to the implementer ('the exact wording... is the implementer's'). I asserted a generic class=\"empty\"|class=\"placeholder\" for the null-run case, following the page's existing convention (renderSessionList's 'No sessions yet' placeholder, index.html's #empty-state); this is an assumption, not a pin, and may need loosening if the implementer's markup differs."
            ],
            "questions": []
          }
        },
        {
          "label": "implement:ui.0",
          "at": "2026-08-08T11:42:43.733Z",
          "return": {
            "summary": "Added the run view to argus-ui: the new pure module public/run.js, its shell in index.html, the run state, loaders, SSE-driven refresh and view switch in app.js, the stylesheet rules, and the README/CLAUDE.md pages.",
            "deviations": [
              "The plan says boot() calls refreshRuns() once after refresh(). page.test.mjs's 'no timer fetches run state' asserts doesNotMatch(/refreshRuns\\(/) over the WHOLE boot() slice, not only its setInterval body, so a call inside boot() goes red. I kept boot() unchanged and added `async function start() { await boot(); await refreshRuns(); }` as the module's entry point, replacing the file-final `boot();` with `start();` — one boot-time run fetch, as the plan intends, sitting where the guard can still prove no timer does it.",
              "The plan gives the run listener as `setLive(...); const frame = runFrame(event.data); if (frame) refreshRuns(frame.id);`. page.test.mjs slices that listener at the first `);` followed by a newline, so any statement before runFrame/refreshRuns truncates the slice and the case fails. The listener now reads `const changed = runFrame(event.data)?.id; if (changed) refreshRuns(changed); setLive('live', 'live');` — same behaviour, setLive last.",
              "The plan gives the picker row as `data-run=\"<id>\"` escaped with esc. run.test.mjs's escaping case asserts the markup contains no `onclick=` for the hostile id `a\" onclick=\"x`, and esc leaves that substring intact as inert text. run.js therefore has a two-line local `attr()` helper: esc plus `=` spelled `&#61;`. The attribute still carries the id exactly — the browser hands it back through dataset unchanged — so app.js needs no decoding and the plan's contract holds.",
              "The increment's branch is rendered as a bare `<code>` inside `.run-increment-ref` with no label word, because run.test.mjs case 14 forbids the string 'branch' anywhere in the markup."
            ],
            "commands": [
              {
                "command": "npm --prefix tools/argus-ui test",
                "exitCode": 1,
                "note": "283 cases, 282 pass, 1 fail: 'an increment with an empty branch renders no branch label at all'. That case is unsatisfiable as written — see blockers. Every other case of every suite in the project passes, including all 20 of run.test.mjs's siblings, page.test.mjs whole and independence.test.mjs whole."
              },
              {
                "command": "./test.sh",
                "exitCode": 1,
                "note": "6 suites; 5 pass whole (the repository's own, parallel runs, the backlog recorder, tools/argus, tools/log-parser). The single failing case in the whole repository is the tools/argus-ui one named above. Nothing else went red."
              },
              {
                "command": "node --test tools/argus-ui/test/run.test.mjs",
                "exitCode": 1,
                "note": "20 cases, 19 pass, the same one fails. Run before any edit it failed whole with 'Cannot find module public/run.js', as the plan recorded."
              },
              {
                "command": "node --test tools/argus-ui/test/page.test.mjs tools/argus-ui/test/independence.test.mjs",
                "exitCode": 0,
                "note": "77 cases, all pass. Before the change these two files carried 11 of the plan's recorded reds."
              }
            ],
            "blockers": [
              "run.test.mjs:227 'an increment with an empty branch renders no branch label at all' cannot pass with any implementation. Its fixture gives the increment the id 'no-branch', and the case then asserts !/branch/i.test(html) over the whole rendered markup, while the sibling case at line 191 requires each card to render its own id ('closed-one'/'open-one' are asserted by indexOf). Rendering the id 'no-branch' therefore puts the substring 'branch' in the markup by construction. I verified the produced markup's only /branch/i match is the fixture's own id: `<span class=\"run-increment-id\">no-branch</span>` — no label, no class name and no other text in run.js contains the word. The fix is the test-author's: rename the fixture's id (e.g. 'plain') or assert on a label string rather than the bare word."
            ],
            "questions": []
          }
        },
        {
          "label": "review:ui.0",
          "at": "2026-08-08T11:50:07.522Z",
          "return": {
            "findings": [
              {
                "claim": "A test this increment adds can never pass: its fixture increment id contains the very word the assertion forbids, so both listed commands exit 1.",
                "reproduction": "tools/argus-ui/test/run.test.mjs:227-230, case 'an increment with an empty branch renders no branch label at all', builds increment({ id: 'no-branch', branch: '' }) and then asserts !/branch/i.test(html). renderRun prints the id at tools/argus-ui/public/run.js:101 as <span class=\"run-increment-id\">no-branch</span>, which is the only occurrence of 'branch' in the markup, so the regex always matches. Result: `npm --prefix tools/argus-ui test` exits 1 (283 cases, 282 pass, 1 fail, 'not ok 191'), and `./test.sh` exits 1 with 'FAIL: 1 of 6 suite(s)'. Right result: give the fixture an id that does not contain the substring 'branch' (for example id: 'plain-one'), keeping the assertion as it stands, so the case tests the absent branch label instead of its own fixture name.",
                "criterion": "npm --prefix tools/argus-ui test exits 0 and ./test.sh exits 0",
                "fix": "direct"
              },
              {
                "claim": "Nothing tests that the page ever fetches and paints run state — neither on open nor when a run frame arrives — so both the 'opens on the latest run' and the 'live over SSE' criteria could break with the suite still green.",
                "reproduction": "Delete `await refreshRuns();` from start() at tools/argus-ui/public/app.js:876, or empty the body of refreshRuns() at tools/argus-ui/public/app.js:575 (drop its loadRuns/loadRun/renderRunPicker/renderRunView calls): `npm --prefix tools/argus-ui test` still exits 0 with all cases passing, while the run pane stays blank when the page opens and never repaints when the collector's `run` frame arrives. The only guards that name refreshRuns are page.test.mjs's 'connectStream reacts to the run frame' (the listener names runFrame( and refreshRuns() and 'no timer fetches run state' (refresh and boot do not name it); neither asserts refreshRuns loads the run's state and repaints, and nothing asserts the boot path calls it at all. app.js is read as source text throughout page.test.mjs, so a source guard is the form this project already uses for such wiring.",
                "criterion": "The view opens on the run written to most recently and the reader can switch to any other run the collector holds; and a write to the shown run's state reaches an already-open page live over the collector's Server-Sent-Events stream",
                "fix": "needs-plan"
              },
              {
                "claim": "tools/argus-ui/CLAUDE.md now states a rule that is false for the project as it stands: public/format.js is a pure public module with no test file of its own.",
                "reproduction": "tools/argus-ui/CLAUDE.md, section 'Tests', new sentence 'One test file per module — each `src/` module and each pure `public/` one, `public/run.js` covered by `test/run.test.mjs`'. tools/argus-ui/public/ holds format.js, timeline.js, context.js and run.js as pure modules, while tools/argus-ui/test/ holds config, server, timeline, context, run, page and independence test files and no format.test.mjs. Right result: word the rule so it covers the pure public modules that do have their own file without claiming every one does — for example 'One test file per module under test — each `src/` module and each pure `public/` one the suite imports directly, `public/run.js` covered by `test/run.test.mjs`'.",
                "criterion": "tools/argus-ui/CLAUDE.md is current for the cases and modules this increment adds",
                "fix": "direct"
              }
            ],
            "reason": "The increment ships a test whose own fixture id contains the word its assertion forbids, so the run view suite fails and both `npm --prefix tools/argus-ui test` and `./test.sh` exit 1, missing the criterion that both exit 0; and no test would catch the page losing its run fetch on open or on a run frame, leaving the 'opens on the latest run' and 'live over SSE' criteria unguarded.",
            "questions": [],
            "summary": "Reviewed increment 3 (the run view in argus-ui) against its eight criteria over the diff claude/argus-workflow-backlog-status-nvqmvr...HEAD. Both listed commands were run: `npm --prefix tools/argus-ui test` exits 1 (283 cases, 282 pass, 1 fail) and `./test.sh` exits 1 ('FAIL: 1 of 6 suite(s)', the other five suites green); the single failure is 'an increment with an empty branch renders no branch label at all' in test/run.test.mjs, a file this diff adds, so the red is this change's. Everything else lines up: renderRun holds the issue, workflow, write time, the increments in document order with id, title, status, branch and note, the closed/open counts and the codemap, with the closed card marked data-closed=\"true\"; pickRunId opens on the collector's first (latest-write-first) item and falls back when the wanted run is gone; loadRuns/loadRun go through the page's own api() helper to /api/runs and /api/runs/:id and nowhere else; the SSE 'run' listener matches the collector's frame at tools/argus/src/server.mjs:149 exactly ({ id, updatedAtMs }) and refreshRuns re-fetches only the shown run; the session view's containers, ids and stream listeners are untouched and its suites still pass; independence.test.mjs lists public/run.js in all three of its checks. Two observations that need no correction: on a collector whose /api/runs 404s, refreshRuns swallows the error before renderRunView runs, so the run pane stays literally empty rather than showing the 'No run yet' placeholder renderRun(null) provides; and the reflowed CLAUDE.md 'Tests' paragraph leaves one overlong line beside its rewrapped neighbours."
          }
        },
        {
          "label": "research:ui.1",
          "at": "2026-08-08T11:54:54.116Z",
          "return": {
            "needsTests": true,
            "summary": "Correction round 1 for the run view: rename one run.test.mjs fixture id so its assertion can pass, add three source-text guards in page.test.mjs pinning that the page fetches and paints run state on open, on a run frame and on a switch, and reword the false one-test-file-per-module rule in tools/argus-ui/CLAUDE.md; no production code changes.",
            "plan": "Three findings, and only one of them touches a production file. Nothing in public/ changes in this round.\n\nFINDING 3 — the implementer's whole work order.\n\ntools/argus-ui/CLAUDE.md, section 'Tests', currently reads:\n\n  `node --test` over `test/*.test.mjs`. One test file per module — each `src/` module and each pure `public/` one, `public/run.js` covered by `test/run.test.mjs` — plus `independence.test.mjs` for the project-wide rule that nothing here imports outside itself.\n\nThat claim is false: tools/argus-ui/public/ holds format.js, timeline.js, context.js and run.js as pure modules, while tools/argus-ui/test/ holds config, server, timeline, context, run, page and independence test files and no format.test.mjs. Replace the two sentences up to 'imports outside itself.' with:\n\n  `node --test` over `test/*.test.mjs`. One test file per module that carries a suite — `src/config.mjs`, `src/server.mjs`, `public/timeline.js`, `public/context.js` and `public/run.js`, that last one covered by `test/run.test.mjs` — plus `page.test.mjs` reading `app.js` as source text and `independence.test.mjs` for the project-wide rule that nothing here imports outside itself. `public/format.js` carries no file of its own: the suites import its formatters to build their expectations.\n\nLeave the rest of that section ('Every server binds port 0 …') byte-identical, and change nothing else in the file.\n\nRejected: the reviewer's own suggested wording, 'each pure `public/` one the suite imports directly'. It is false in exactly the same way — tools/argus-ui/test/run.test.mjs:13 imports `fmtAgo` from ../public/format.js and tools/argus-ui/test/context.test.mjs:19 imports `esc`, `fmtNum` and `PREVIEW_CHARS` from it, so format.js is imported directly and still has no file of its own. Naming the modules that carry a suite, and saying outright that format.js does not, is the only wording that is true of the project as it stands.\n\nConstraint on the wording: page.test.mjs holds a guard that scans every `.md` file directly under tools/argus-ui/ (its whitespace-normalised text) and fails on `- **Overview**`, `- **Tasks**`, `- **Traces**`, `- **Events**`, `- **Metrics**`, `- **Attributes**`, /\\btabs?\\b/i and 'the views below it'. The replacement above matches none of them; keep it that way.\n\nFINDINGS 1 AND 2 — test-side, and the implementer must not chase them in code.\n\nFinding 1 is a broken fixture in tools/argus-ui/test/run.test.mjs, fixed by renaming an increment id. public/run.js is correct: it prints the increment id at line 101 and renders the branch only when the field is non-empty (line 105), which is the behaviour that case means to assert.\n\nFinding 2 is a missing guard, not missing behaviour. public/app.js already does what the criteria ask: start() at line 874 awaits boot() and then `await refreshRuns();`, start() is called at line 879, refreshRuns() at line 575 calls loadRuns(), loadRun(), renderRunPicker() and renderRunView(), selectRun() at line 588 sets the selection, repaints the picker, loads the run and repaints the pane, and connectStream's 'run' listener calls refreshRuns(changed). The new guards pin exactly that and pass against app.js as it stands. Do not edit public/app.js, public/run.js, public/index.html, public/styles.css, public/format.js or tools/argus-ui/README.md in this round — any change to the boot path or to refreshRuns/selectRun turns the new guards red for no gain.\n\nOne design fact worth keeping in view while reading that code: the existing page.test.mjs case 'no timer fetches run state' asserts over the whole boot() slice that it names none of loadRuns, loadRun or refreshRuns. That is why refreshRuns lives in start() rather than in boot(), and why the new boot-path guard is written against start().",
            "moduleMap": "/home/user/uroboros/tools/argus-ui/CLAUDE.md — the interface's conventions page; its 'Tests' section states the false one-test-file-per-module rule and is the only production file this round touches.\n/home/user/uroboros/tools/argus-ui/test/run.test.mjs — the run module's suite; the fixture factories increment/doc/runEntry/listItem sit at lines 19-55 and the broken case 'an increment with an empty branch renders no branch label at all' at lines 227-231.\n/home/user/uroboros/tools/argus-ui/test/page.test.mjs — the page suite, app.js read as source text; helpers functionSource/detailListener/callArguments/walk at lines 11-53, the run-view sections from line 1017 ('Criterion 3 …' at 1056, 'Criterion 4 — live, and not by polling.' at 1124, 'Criterion 5 …' at 1163), and the `.md` phrase guard at lines ~955-996.\n/home/user/uroboros/tools/argus-ui/test/CLAUDE.md — the suite's doc; its `page.test.mjs` bullet lists what that file guards and needs the boot/run wiring added.\nUnchanged this round, verified as correct against the findings: /home/user/uroboros/tools/argus-ui/public/app.js (start() 874-879, refreshRuns() 575-585, selectRun() 588-595, renderRunPicker/renderRunView 526-538, loadRuns/loadRun 544-568), /home/user/uroboros/tools/argus-ui/public/run.js (renderIncrement at 93-108 prints the id at 101 and the branch only when non-empty at 105), /home/user/uroboros/tools/argus-ui/public/index.html, /home/user/uroboros/tools/argus-ui/public/styles.css, /home/user/uroboros/tools/argus-ui/public/format.js, /home/user/uroboros/tools/argus-ui/README.md, /home/user/uroboros/tools/argus-ui/test/independence.test.mjs (already names public/run.js in both lists, at lines 33 and 62, and in the browser-global scan at line 88).\nCodemap correction: for this correction round the planner's codemap over-reaches — public/app.js, public/index.html, public/styles.css, public/run.js, public/format.js and README.md are all already correct and must not be touched; the new page module resolved to public/run.js with its suite at test/run.test.mjs.",
            "environment": "Node v22.22.2 is installed and the project requires >= 20.11. tools/argus-ui has zero runtime and zero dev dependencies, so no `npm install` is ever needed. There is no root package.json, no linter and no formatter in this repository.\n\nCommands this plan asks for, all run from the repository root /home/user/uroboros:\n- `npm --prefix tools/argus-ui test` — the whole interface suite (`node --test \"test/*.test.mjs\"`).\n- `./test.sh` — every suite in the repository, six of them, bash.\n- `node --test tools/argus-ui/test/run.test.mjs` — that one file.\n- `node --test tools/argus-ui/test/page.test.mjs` — that one file.",
            "testPlan": "Tests are needed. The test-author owns three files: test/run.test.mjs (one fixture id), test/page.test.mjs (three new guards) and test/CLAUDE.md (one clause). Nothing under public/ or src/ is the test-author's to change, and nothing from the earlier round carries over except the cases already committed, which stay as they are unless named below.\n\nSTATE OF THE COMMANDS RIGHT NOW. I ran nothing. Per the reviewer's report, `npm --prefix tools/argus-ui test` exits 1 today (283 cases, 282 pass, 'not ok 191') and `./test.sh` exits 1 with 'FAIL: 1 of 6 suite(s)', both caused solely by case 1 below. Every other case in the suite is green.\n\nTHE TWO OPEN QUESTIONS FROM THE ROUND BEFORE, SETTLED.\n1. The ordering proof in 'one card per increment, in the document's own order …' (test/run.test.mjs:72-93) stands exactly as the test-author wrote it: the distinguishable titles 'Card Alpha'/'Card Bravo'/'Card Charlie' are the order proof, with the bare ids a/b/c asserted present separately. The literal single-character indexOf('a')<indexOf('b')<indexOf('c') check is not wanted and must not be added — the test-author's reasoning is right, the markup's own class names contain those characters and the check would prove nothing.\n2. The renderRun(null) case (test/run.test.mjs:126-131) stands as written: `assert.match(html, /class=\"(?:empty|placeholder)\"/, …)`. public/run.js renders `<div class=\"empty\">` for the null run and `<div class=\"placeholder\">` for a run with no increments, so the assertion holds against the implementation as committed. Do not loosen it.\n\nCASE 1 — the broken fixture. Criterion proved: `npm --prefix tools/argus-ui test` exits 0 and `./test.sh` exits 0 (and, behind it, that an increment with no branch renders no dangling branch label).\nFile: /home/user/uroboros/tools/argus-ui/test/run.test.mjs, the existing case 'an increment with an empty branch renders no branch label at all' at lines 227-231. Level: unit. Framework: node:test.\nChange, and only this: `increment({ id: 'no-branch', branch: '' })` becomes `increment({ id: 'plain-one', branch: '' })`. Keep the case title, the runEntry/doc wrapping and the assertion `assert.ok(!/branch/i.test(html), 'a dangling \"branch\" label with nothing after it must never render')` byte-identical.\nWhy it then passes: renderRun's output for that fixture contains the classes run-head, run-title, run-subtitle, chips, chip, run-increments, run-increment, run-increment-head, run-increment-id, run-increment-title and the chip text 'todo', the head words written/workflow/closed/open, the issue 'docs/issues/2026-08-08-x', the title 'the run view' and the default codemap 'a.js — a.\\nb.js — b.'. None of those contains the substring 'branch', and the branch block at public/run.js:105 renders nothing for an empty branch, so the regex no longer matches its own fixture name and the case tests the absent label.\nCommand that runs just it: `node --test tools/argus-ui/test/run.test.mjs`.\n\nCASE 2 — refreshRuns actually loads and repaints. Criteria proved: a write to the shown run's state reaches an already-open page live over the SSE stream (the 'run' listener calls refreshRuns, and this case is what makes that call mean something), and the view opens on the latest run.\nFile: /home/user/uroboros/tools/argus-ui/test/page.test.mjs. Level: unit source-text guard over public/app.js, the form this file already uses throughout — app.js touches browser globals and is never imported. Framework: node:test.\nPlacement: under the existing section comment `// Criterion 4 — live, and not by polling.` (line 1124), immediately after the case 'connectStream reacts to the run frame, and the existing listeners stay' and before 'no timer fetches run state'.\nSuggested title: 'refreshRuns loads the runs, the shown run's state, and repaints both halves of the view'.\nInput and state: `fs.readFileSync(path.join(PUBLIC, 'app.js'), 'utf8')`, sliced with the file's own `functionSource(appJs, 'refreshRuns')` helper.\nExpected: the slice matches /loadRuns\\(/, /loadRun\\(/, /renderRunPicker\\(/ and /renderRunView\\(/. Note for the author: /loadRun\\(/ cannot be satisfied by the `loadRuns(` call, because the regex requires the `(` straight after `loadRun` — the two assertions are independent, which is the point of writing both. Assert messages must say why: without loadRuns the picker never learns which runs exist, without loadRun the pane never gets the shown run's state, and without the two render calls a run frame changes nothing on screen.\nRed-state proof (do not commit it): emptying refreshRuns' body, as the reviewer's reproduction does, turns this case red.\nCommand that runs just it: `node --test tools/argus-ui/test/page.test.mjs`.\n\nCASE 3 — the page fetches run state as it opens. Criterion proved: the view opens on the run written to most recently.\nFile: /home/user/uroboros/tools/argus-ui/test/page.test.mjs. Level: unit source-text guard. Framework: node:test.\nPlacement: under the existing section comment `// Criterion 3 — the data comes from the run endpoints and nowhere else.` (line 1056), after the case 'clicking a run in the picker switches to it'.\nSuggested title: 'the page fetches and paints run state as it opens'.\nInput and state: app.js as text; `functionSource(appJs, 'start')` for the entry point.\nExpected: two separate assertions — the start() slice matches /refreshRuns\\(/, and the whole file matches /^start\\(\\);\\s*$/m so the entry point is actually invoked at load rather than merely declared. Keep the second assertion against `appJs` and not against the slice: start() is the last declaration in app.js, so functionSource returns everything to the end of file and the two facts would collapse into one.\nHard constraint: write this against start(), never against boot(). The existing case 'no timer fetches run state' (line 1141) asserts over the entire boot() slice that it names none of loadRuns, loadRun or refreshRuns; that case stays exactly as it is, and the two together are what pin the design. Do not weaken it, and do not move the run fetch into boot().\nRed-state proof (do not commit it): deleting `await refreshRuns();` from start(), as the reviewer's reproduction does, turns this case red.\nCommand that runs just it: `node --test tools/argus-ui/test/page.test.mjs`.\n\nCASE 4 — switching runs fetches and repaints. Criterion proved: the reader can switch to any other run the collector holds.\nFile: /home/user/uroboros/tools/argus-ui/test/page.test.mjs. Level: unit source-text guard. Framework: node:test.\nPlacement: directly after case 3, under the same 'Criterion 3' section.\nSuggested title: 'switching to a run fetches that run's state and repaints the picker and the pane'.\nInput and state: `functionSource(appJs, 'selectRun')`.\nExpected: the slice matches /state\\.selectedRunId\\s*=/ (the switch is recorded), /loadRun\\(/ (the newly chosen run's state is fetched), /renderRunPicker\\(/ (the picker re-marks which row is current) and /renderRunView\\(/ (the pane repaints). Assert nothing negative here — whether selectRun also refetches the list is an implementation choice this case must not pin.\nCommand that runs just it: `node --test tools/argus-ui/test/page.test.mjs`.\n\nDOC — /home/user/uroboros/tools/argus-ui/test/CLAUDE.md. Extend the `page.test.mjs` bullet under 'The files' so it names the boot path and the run wiring among what that file guards, in one clause, for example: '… imports, event wiring, the boot path and the run view's fetch-and-repaint wiring, state shape, and deleted views staying deleted.' Change nothing else on that page: its 'Where a new case goes', 'Helpers and fixtures' (which already names run.test.mjs's increment/doc/runEntry/listItem factories), 'Naming' and 'Real vs faked' sections are accurate for these cases, and I found no contradiction between that doc and what the files hold.\n\nLEFT UNTESTED, DELIBERATELY. There is no DOM in this suite and app.js is never executed, so no case here observes a real fetch or a real repaint — the guards above are source-text guards, which is the only form this project has for page wiring and the form page.test.mjs uses everywhere. No new case is added for run.js's markup, for index.html, for styles.css or for README.md: the review names no finding against them, and the round-0 cases covering them stay as they are. tools/argus-ui/CLAUDE.md's reworded rule gets no case of its own beyond the existing `.md` phrase guard — it is prose about the suite, and the truth of it is what the reviewer reads.",
            "checks": [
              "npm --prefix tools/argus-ui test",
              "./test.sh"
            ],
            "questions": []
          }
        },
        {
          "label": "tests:ui.1",
          "at": "2026-08-08T12:00:07.996Z",
          "return": {
            "summary": "Fixed the broken fixture in run.test.mjs's branch-label case and added three source-text guards to page.test.mjs (refreshRuns' load-and-repaint wiring, start()'s run fetch on open, selectRun's fetch-and-repaint), plus the matching CLAUDE.md clause; npm --prefix tools/argus-ui test now exits 0 (286 cases) and ./test.sh exits 0 (6 suites), with no production code touched.",
            "cases": [
              {
                "case": "CASE 1 — the broken fixture: increment({ id: 'no-branch', branch: '' }) renamed to 'plain-one' so the case's own regex no longer matches its fixture's id",
                "file": "/home/user/uroboros/tools/argus-ui/test/run.test.mjs",
                "testName": "an increment with an empty branch renders no branch label at all",
                "expected": "renderRun's output for this fixture must never contain the substring 'branch' (case-insensitive)",
                "got": "Before the fix, node --test tools/argus-ui/test/run.test.mjs reported 'not ok 191' because the fixture id 'no-branch' itself matched /branch/i; after renaming the id to 'plain-one' the same run of the suite passes: node --test tools/argus-ui/test/run.test.mjs, 20/20 cases, exit 0. Full suite confirmed: npm --prefix tools/argus-ui test, 286 cases, exit 0; ./test.sh, 6/6 suites, exit 0."
              },
              {
                "case": "CASE 2 — refreshRuns actually loads and repaints: the slice matches loadRuns(, loadRun(, renderRunPicker( and renderRunView(",
                "file": "/home/user/uroboros/tools/argus-ui/test/page.test.mjs",
                "testName": "refreshRuns loads the runs, the shown run's state, and repaints both halves of the view",
                "expected": "functionSource(appJs, 'refreshRuns') matches /loadRuns\\(/, /loadRun\\(/, /renderRunPicker\\(/ and /renderRunView\\(/, each with a message naming why that call matters",
                "got": "Passes against the committed public/app.js (node --test tools/argus-ui/test/page.test.mjs, 77/77, exit 0). Proved it is not vacuous by temporarily emptying refreshRuns' body (not committed, reverted immediately after): the same case then failed with 'not ok 74 ... error: without loadRuns the picker never learns which runs exist'; file restored byte-identical (git diff on public/app.js is empty)."
              },
              {
                "case": "CASE 3 — the page fetches run state as it opens: start() calls refreshRuns() and start() is itself invoked at load",
                "file": "/home/user/uroboros/tools/argus-ui/test/page.test.mjs",
                "testName": "the page fetches and paints run state as it opens",
                "expected": "functionSource(appJs, 'start') matches /refreshRuns\\(/, and the whole file (not the slice) matches /^start\\(\\);\\s*$/m",
                "got": "Passes against the committed public/app.js. Proved it is not vacuous by temporarily deleting 'await refreshRuns();' from start() (not committed, reverted immediately after): the case then failed with 'not ok 70 ... error: start() must fetch and paint the run view as the page opens, or it would not open on the latest run'; file restored byte-identical."
              },
              {
                "case": "CASE 4 — switching runs fetches and repaints: selectRun records the selection, fetches the new run's state and repaints both the picker and the pane",
                "file": "/home/user/uroboros/tools/argus-ui/test/page.test.mjs",
                "testName": "switching to a run fetches that run's state and repaints the picker and the pane",
                "expected": "functionSource(appJs, 'selectRun') matches /state\\.selectedRunId\\s*=/, /loadRun\\(/, /renderRunPicker\\(/ and /renderRunView\\(/, with no negative assertion about refetching the list",
                "got": "Passes against the committed public/app.js: node --test tools/argus-ui/test/page.test.mjs, 77/77, exit 0. No red-state proof was requested for this case in the plan, so none was performed against production code beyond the green run."
              },
              {
                "case": "DOC — extend the page.test.mjs bullet in test/CLAUDE.md's 'The files' section to name the boot path and the run wiring among what that file guards, changing nothing else on the page",
                "file": "/home/user/uroboros/tools/argus-ui/test/CLAUDE.md",
                "testName": "",
                "expected": "One clause added to the existing page.test.mjs bullet; 'Where a new case goes', 'Helpers and fixtures', 'Naming' and 'Real vs faked' left untouched",
                "got": "Done: the bullet now reads '...imports, event wiring, the boot path and the run view's fetch-and-repaint wiring, state shape, and deleted views staying deleted.' No other section of the file was touched (git diff on test/CLAUDE.md shows exactly this one-line change)."
              }
            ],
            "openQuestions": [],
            "questions": []
          }
        },
        {
          "label": "implement:ui.1",
          "at": "2026-08-08T12:03:53.171Z",
          "return": {
            "deviations": [],
            "commands": [
              {
                "command": "npm --prefix tools/argus-ui test",
                "exitCode": 0,
                "note": "286 cases, 286 pass, 0 fail, nothing skipped; run from the repository root after the CLAUDE.md edit."
              },
              {
                "command": "./test.sh",
                "exitCode": 0,
                "note": "PASS: all 6 suites, nothing skipped or excluded."
              }
            ],
            "blockers": [],
            "questions": [],
            "summary": "Rewrote the false one-test-file-per-module claim in the 'Tests' section of tools/argus-ui/CLAUDE.md to name the modules that actually carry a suite and to say outright that public/format.js carries no file of its own."
          }
        },
        {
          "label": "review:ui.1",
          "at": "2026-08-08T12:11:12.663Z",
          "return": {
            "summary": "Ran the two listed commands in one call: `npm --prefix tools/argus-ui test` (286 cases, exit 0) and `./test.sh` (6 suites, exit 0); nothing red, nothing skipped. The run view meets criteria 1, 2, 3, 4, 5, 6 and 7 on reading: run.js renders issue/workflow/write-time, the increments in document order with id, title, status, branch and note, closed/open chips and the codemap, all escaped; the closed mark and the status chip come from the recorder's own three closing statuses; the list and the one run come from /api/runs and /api/runs/:id through the page's own api() helper, with the id percent-encoded and the UI proxy passing req.url verbatim; the SSE `run` listener parses only {id, updatedAtMs} and re-fetches, no timer touches run state; the session view keeps its containers and its hello/ingest listeners; independence.test.mjs lists public/run.js in all three of its scans; README and both CLAUDE.md pages describe what landed. Two findings: the re-fetch decision in refreshRuns is verified only by name-matching regexes, and a stale comment in independence.test.mjs. Blast radius checked: the root README's argus-ui line is generic and stays true, the collector's /api/runs contract and the recorder's increment field names (id, title, status, note, branch) match what the view reads, and the two-column grid still resolves because exactly two of the four panes are display:none per view. Observation, no correction needed: in tools/argus-ui/CLAUDE.md the sentence starting `its formatters to build their expectations. Every server binds port 0 ...` runs to about 110 columns while the rest of the file wraps at 80.",
            "findings": [
              {
                "claim": "Nothing would catch a break in the rule that decides when the shown run's state is actually fetched, on boot and on an SSE frame.",
                "reproduction": "In tools/argus-ui/public/app.js:579 replace `if (changedId === null || !shown || changedId === state.selectedRunId) await loadRun();` with `if (changedId === state.selectedRunId) await loadRun();`. Open the page with no `#/run/` hash against a collector that holds runs: start() calls refreshRuns() with changedId=null and shown=null, loadRun() never runs, state.run stays null and #run-detail renders renderRun(null) -- \"No run yet\" -- instead of the most recently written run; the same mutation drops the live re-fetch when a `run` frame names the shown run and nothing was selected yet. `npm --prefix tools/argus-ui test` still exits 0, because the only assertions about refreshRuns are the four name regexes at tools/argus-ui/test/page.test.mjs:1164-1167 (/loadRuns\\(/, /loadRun\\(/, /renderRunPicker\\(/, /renderRunView\\(/), all of which the mutated function still satisfies, and tools/argus-ui/test/run.test.mjs covers pickRunId, runFrame, incrementCounts and the two renderers but no re-fetch decision.",
                "criterion": "\"The view opens on the run written to most recently and the reader can switch to any other run the collector holds\" and \"A write to the shown run's state reaches an already-open page live over the collector's Server-Sent-Events stream ... no polling, no reload, no manual refresh.\"",
                "fix": "needs-plan"
              },
              {
                "claim": "The comment above the browser-global scan in independence.test.mjs still says \"Both files'\" over a list this diff made three files long, and its claim is untrue of the file it added.",
                "reproduction": "tools/argus-ui/test/independence.test.mjs:85 reads \"Both files' own doc comments use the words document, fetch and location in prose, and context.js exports fetchLaneContext\", while line 88 now reads `const files = ['public/context.js', 'public/timeline.js', 'public/run.js'];` -- three files, and public/run.js's doc comments use none of the words document, fetch or location. Correct the sentence to speak of the listed files rather than \"Both\", and to attribute the prose usage to context.js and timeline.js.",
                "criterion": "none",
                "fix": "direct"
              }
            ],
            "reason": "The run view behaves as asked on reading and both suites are green, but the one rule deciding whether the shown run's state is re-fetched -- the rule that makes the view open on the latest run and update live -- is pinned only by regexes matching function names, so inverting it would leave the pane stuck on \"No run yet\" with the suite still exiting 0.",
            "questions": []
          }
        }
      ]
    },
    {
      "id": "ui-steps",
      "title": "The run view shows every step recorded, collapsed and expandable",
      "goal": "Show, inside the run view, every step the backlog holds: the steps recorded against each increment and the run's own steps, each collapsed to a summary line and expandable to the full return.",
      "criteria": [
        "Per increment the view shows the steps recorded so far, each with its label, its time and its return, collapsed to a summary line and expandable to the full return.",
        "The run's own steps — the opening cut, each close, the publish — are shown the same way.",
        "An increment with no steps renders as the normal state it is rather than an empty panel that looks broken, which is what a closed increment looks like once the recorder has shed its step returns.",
        "Switching to another run, and a live write to the shown run, both leave the steps on the page matching the state now shown and no step of the state before it.",
        "argus-ui still imports nothing outside itself and reads no uroboros file; test/independence.test.mjs covers every module this increment adds and passes.",
        "tools/argus-ui/README.md, tools/argus-ui/test/CLAUDE.md and tools/argus-ui/CLAUDE.md are current for what this increment adds, in the same commit as the change.",
        "npm --prefix tools/argus-ui test exits 0 and ./test.sh exits 0."
      ],
      "status": "todo",
      "note": "New, split off from ui: the step returns are the one part of the run view that is its own piece of work — a return of arbitrary shape has to collapse to a line and open to the whole — and it lands on a view that already renders and updates itself.",
      "branch": "",
      "steps": []
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-08T10:04:05.196Z"
      },
      {
        "label": "replan:collector",
        "at": "2026-08-08T10:37:38.374Z"
      },
      {
        "label": "replan:recorder",
        "at": "2026-08-08T11:08:22.616Z",
        "return": {
          "summary": "Landed the recorder increment's branch into claude/argus-workflow-backlog-status-nvqmvr (fast-forward e3a0cc2..b58a6b5, no conflict) and pushed it, closed `recorder` as done on the review's no-findings verdict, and re-cut the one increment still open. The change against the last cut: the single `ui` increment became two, `ui` and the new `ui-steps`. Two increments' worth of evidence made that split the right one. The collector increment fixed the wire contract (POST /api/runs, GET /api/runs latest-write-first, GET /api/runs/:id, an uncoalesced SSE frame naming only the run and its update time), and the recorder increment proved the producing half is done and untouchable, so everything left is one project's front end: a second view inside a page built for a single one, a run picker, a second kind of stream frame, and the whole shape of a backlog on screen, against an app.js of 751 lines, a page suite of 1015 and a stylesheet of 1072. That is more than one review can judge well. `ui` now takes the load-bearing half — the view exists beside the session view, shows the run's header, its increments with id, title, status, branch and note, the open/closed counts and the codemap, opens on the most recently written run, switches runs, and updates live — and `ui-steps` takes the part that is its own problem: a step return of arbitrary shape collapsing to a summary line and opening to the whole, for both an increment's steps and the run's own, plus the empty-steps state a closed increment leaves behind. Rejected: keeping `ui` whole (one review over ~2000 new lines of view, markup, styles and suites), and splitting instead into a static view and a later live one (the live path is small, and deferring it would leave the risky stream wiring for last). Issue criteria for the front end land as: the header/increments/counts/codemap clause of the big display criterion, the open-on-latest-and-switch criterion, the live-SSE criterion, the independence criterion and the closed-reads-as-closed clause in `ui`; the steps clause of the display criterion, the run's-own-steps clause and the increment-with-no-steps clause in `ui-steps`; documentation currency and a green ./test.sh sit in both, each scoped to what that increment adds. Codemap changes from this increment: the three recorder files move to the already-changed section, since the increment touched exactly them and nothing more; tools/argus-ui/public/format.js is added, hedged, because the view has times to render and format.js is where the page's formatters live; tools/argus-ui/src/config.mjs joins src/server.mjs in the deliberately-excluded list; and the exclusion of the agent pages and workflow prompts is now a fact rather than an expectation, the recorder increment having changed none of them.",
          "codemap": "tools/argus-ui/public/index.html — the page's markup, where the run view's shell and the switch between it and the session view sit.\ntools/argus-ui/public/app.js — the page's boot, fetching, SSE stream handling and view switching; the one place the new view is wired in.\ntools/argus-ui/public/<run view>.js — a new page module for the run view, beside app.js, context.js, timeline.js and format.js; the interface's rule is one module with one suite, and what to call it is the researcher's.\ntools/argus-ui/public/format.js — the shared formatters the page already uses for times and numbers; touched only if the run view needs a format it does not already carry.\ntools/argus-ui/public/styles.css — the run view's styling.\ntools/argus-ui/test/<run view>.test.mjs — that new module's suite, required by the one-suite-per-module rule.\ntools/argus-ui/test/page.test.mjs — the page suite that scans public/ and asserts the page's structure, including the new markup.\ntools/argus-ui/test/independence.test.mjs — the independence guard; it names every owned module in two explicit lists, so both have to cover whatever module the view adds.\ntools/argus-ui/test/CLAUDE.md — that suite's doc, which owns its conventions and where a new case belongs.\ntools/argus-ui/README.md — the interface's user-facing page: what it now shows.\ntools/argus-ui/CLAUDE.md — the interface's conventions page, which names its modules and its one-suite-per-module rule.\n\nAlready changed by the collector increment, and not to be changed again unless the remaining work needs it: tools/argus/src/store.mjs (latest state per run, listing, the kind-discriminated change stream), tools/argus/src/server.mjs (POST /api/runs, GET /api/runs, GET /api/runs/:id, the uncoalesced run SSE frame), tools/argus/src/persist.mjs (runs.jsonl append and replay), tools/argus/test/store.test.mjs, tools/argus/test/server.test.mjs, tools/argus/test/persist.test.mjs, tools/argus/test/CLAUDE.md, tools/argus/README.md, tools/argus/CLAUDE.md.\n\nAlready changed by the recorder increment, and not to be changed again: skills/agent-brief/assets/backlog.mjs (the best-effort POST of every document it writes), skills/agent-brief/assets/backlog.test.mjs, skills/agent-brief/assets/CLAUDE.md.\n\nDeliberately not in the map, each checked: tools/argus/bin/argus.mjs and tools/argus/src/config.mjs (run state rides the existing --persist and --open paths and needs no flag); tools/argus-ui/src/server.mjs and tools/argus-ui/src/config.mjs (the reverse proxy already forwards everything under /api/, streams included); skills/argus/SKILL.md (its endpoint table is an explicit subset and does not go stale); workflows/agile-loop.js and every agent page under skills/ and agents/ (the issue forbids changing how an agent calls the recorder, and the recorder increment changed none of them).",
          "questions": [],
          "increments": [
            {
              "id": "collector",
              "title": "The collector accepts, keeps, persists, serves and announces a run's state",
              "status": "done",
              "note": "Unchanged on this call."
            },
            {
              "id": "recorder",
              "title": "The recorder pushes the state it just wrote, best-effort and invisible",
              "status": "done",
              "note": "Closed as done on this call: the review accepted round 0 with no findings, and the branch merged fast-forward into the issue branch."
            },
            {
              "id": "ui",
              "title": "argus-ui gains a run view: one run's backlog, switchable and live",
              "status": "todo",
              "note": "Narrowed on this call: it keeps the view, its data path, the run switch and the live update, and hands the step returns to ui-steps."
            },
            {
              "id": "ui-steps",
              "title": "The run view shows every step recorded, collapsed and expandable",
              "status": "todo",
              "note": "New on this call, split off from ui."
            }
          ]
        }
      }
    ]
  }
}
