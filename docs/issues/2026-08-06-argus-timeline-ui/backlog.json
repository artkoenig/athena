{
  "version": 1,
  "issue": "docs/issues/2026-08-06-argus-timeline-ui/issue.md",
  "workflow": "agile-loop",
  "increments": [
    {
      "id": "content-pipeline",
      "title": "Content-bearing telemetry is captured by default and served over the API",
      "goal": "Turn the content flags on by default in `argus env` and make the collector store and serve what the resulting events carry, so a caller can ask for the conversation content belonging to a point in time.",
      "criteria": [
        "`argus env` includes the content flags by default in both output formats: user prompts, tool details, tool content, and the flag or flags the CLI requires to emit `api_request_body`/`api_response_body` events.",
        "The exact flag names are verified against the CLI's monitoring documentation rather than assumed.",
        "If the current CLI turns out not to emit request/response body events at all, that is reported as an explicit finding, naming the best content the available flags do provide.",
        "What attribution actually arrives for subagent activity and for request bodies is established and reported, including any gap, so later increments know which records can be assigned to which agent.",
        "The collector stores the content those events carry and exposes those content-bearing records over the JSON API like the existing signals, such that the content belonging to a point in time can be asked for.",
        "Recordings made without the content flags are out of contract: no fallback rendering, no compatibility notice, and no test pins behavior for them.",
        "`./test.sh` is green."
      ],
      "status": "done",
      "note": "Review accepted with no findings: the four content flags (OTEL_LOG_USER_PROMPTS, OTEL_LOG_TOOL_DETAILS, OTEL_LOG_TOOL_CONTENT, OTEL_LOG_RAW_API_BODIES=1) are on by default in all four env formats, contentOf plus store.queryContent plus GET /api/content store and serve the content-bearing records with an inclusive 'at' bound, and the suite is green (156 argus cases exit 0; bash ./test.sh PASS all 6 suites). The attribution gap is established: body events carry query_source and span context but no agent.name or agent_id.",
      "steps": []
    },
    {
      "id": "timeline-landing",
      "title": "A session opens on the timeline, with a lane per agent",
      "goal": "Make opening a session in the argus UI land on a timeline view that draws one lane for the main session and one per subagent across each agent's lifetime, with the existing technical views reachable from it but subordinate.",
      "criteria": [
        "Opening a session in the argus UI lands on the timeline view.",
        "The previous technical views — event tail, traces, raw inspectors — remain reachable from the timeline view and are subordinate to it, not the other way round.",
        "The timeline draws one lane for the main session and one lane per subagent instance, so two concurrent subagents of the same type get two lanes; `query_source` names only the agent type, and per-instance identity has to come from the span attributes the previous increment reported (`agent_id`, `parent_agent_id`).",
        "Each agent lane spans that agent's lifetime, so any vertical slice answers which agents are running at that moment.",
        "Where the run places the record-to-agent join — collector side or UI side — that choice is stated, and activity that cannot be attributed to an agent instance is shown as unattributed rather than dropped or silently folded into the main session."
      ],
      "status": "done",
      "note": "Review accepted with no findings after two correction rounds, both of them test gaps rather than defects. What shipped: a collector-side lane join in store.getAgents behind GET /api/sessions/:id/agents, giving every span of a session a lane key of 'main', 'agent:<agent_id>' or 'unattributed' by resolving agent_id first and the parent-span chain second; a timeline module in argus-ui drawing one bar per lane over one shared window; and the six technical views (overview, todos, traces, events, metrics, raw) moved into a strip below the timeline, closed by default. npm --prefix tools/argus test 176 cases exit 0, npm --prefix tools/argus-ui test 39 cases exit 0. Both correction rounds were spent because the visible behaviour was computed by a tested pure helper but never pinned in the rendered markup or the booted page; the round-2 fake-DOM harness in tools/argus-ui/test/app.test.mjs now boots public/app.js in-process, so later UI increments have a way to pin what the human sees.",
      "steps": []
    },
    {
      "id": "lane-activity",
      "title": "Activity and context growth drawn on each lane",
      "goal": "Draw each lane's activity over time and, behind it, that agent's context size over time, so where token consumption grows is visible on the timeline itself.",
      "criteria": [
        "Each lane shows its activity over time - tool calls and API requests - placed at the times they occurred, on the lane of the agent instance the activity belongs to.",
        "Activity is assigned to lanes with the lane vocabulary the session's agents endpoint already serves ('main', 'agent:<agent_id>', 'unattributed'), not a second attribution scheme; the records drawn here carry no agent_id of their own, so where their lane comes from is stated.",
        "Activity that resolves to no agent instance stays visible on the unattributed lane rather than being dropped or folded into the main lane.",
        "Each lane carries, as a curve or area behind that activity, the context size over time for that agent.",
        "The curve is drawn from a size the telemetry actually records - the reported untruncated request-body length survives even when the body itself was truncated - and the view names which measure it is showing.",
        "Growth in context size is readable from the timeline alone, without opening any detail.",
        "What proves this increment is the drawn result in the page: a change that stops the activity marks or the curve reaching the rendered timeline is caught, not only a computation checked in isolation."
      ],
      "status": "todo",
      "note": "Re-cut after timeline-landing. Two criteria added and one sharpened. Timeline-landing established a lane vocabulary collector-side - every span of a session resolves to 'main', 'agent:<agent_id>' or 'unattributed', by agent_id first and the parent-span chain second, served by GET /api/sessions/:id/agents - so this increment inherits it instead of inventing per-lane attribution again, and the unattributed lane it already draws must keep receiving what cannot be attributed. Content-pipeline showed the body events carry no agent_id, so the same span-chain resolution is the only route from a record to a lane, and that route is the one open question its researcher settles first. The provability criterion is there because both correction rounds of timeline-landing were spent on exactly this: the geometry and the landing were computed correctly by tested pure helpers while nothing pinned that they reached the page.",
      "steps": []
    },
    {
      "id": "scrub-live",
      "title": "Scrubbing the timeline, and a live mode that follows the head",
      "goal": "Give the timeline a chosen time that can be moved to any point of the recorded session, plus a live mode that follows the newest data as it arrives.",
      "criteria": [
        "The timeline can be scrubbed backward and forward to any point of the recorded session, and the chosen time is visible on the timeline.",
        "A live mode follows the newest data as it arrives.",
        "Scrubbing away from the head leaves live mode.",
        "A control returns to live mode.",
        "The chosen time is one shared value the session view works from, so a later per-lane detail can be answered for exactly that moment.",
        "What proves this increment is the behaviour of the page: a change that stops scrubbing moving the chosen time, or stops the live control returning to the head, is caught, not only a helper that maps a position to a time."
      ],
      "status": "todo",
      "note": "Re-cut after timeline-landing. Two criteria added, none removed. The chosen time is named as one shared value because context-at-time, which follows, has to answer for exactly that moment, and timeline-landing showed the session view already keeps its timeline window in one place. The provability criterion carries forward the lesson that cost timeline-landing both correction rounds: a pinned pure helper is not a pinned behaviour.",
      "steps": []
    },
    {
      "id": "context-at-time",
      "title": "The selected agent's context and tool use as of the chosen time",
      "goal": "Show, for the lane selected at the chosen time, that agent's or the main session's context at that moment as a structured, expandable message list, together with the tools that agent has used up to that moment.",
      "criteria": [
        "Selecting a lane at the chosen time shows that agent's - or the main session's - context as of that moment.",
        "The lane selected is one of the lanes the timeline already draws, and the records shown are chosen by that lane and the chosen time using the lane vocabulary already in place, not a second attribution scheme.",
        "The context shown is the body of the nearest API request at or before the chosen time.",
        "It is rendered as a structured message list distinguishing system prompt, user, assistant, tool call and tool result blocks.",
        "Each block is collapsed to one line carrying its size.",
        "Each block expands to the exact full text the record carries, and a body the CLI truncated is marked as truncated instead of being presented as the whole context.",
        "The same selection also shows the tools that agent has used up to that moment, each entry carrying the tool name and that call's parameters.",
        "Both listings are per agent and per time, so 'which tools, and what for' is answerable for any agent at any point of the session.",
        "What proves this increment is the rendered panel: a change that stops a selection reaching the panel, or stops an expanded block showing the full text, is caught, not only a builder function checked in isolation."
      ],
      "status": "todo",
      "note": "Re-cut after timeline-landing. Kept whole - it still absorbs the dropped tools-at-time - with two criteria added: the selection reuses the lane vocabulary the collector serves ('main', 'agent:<agent_id>', 'unattributed') rather than a second join, and the panel is proved where it is seen. Both come from timeline-landing: it placed the join collector-side and it lost two correction rounds to behaviour that lived only in untested page wiring.",
      "steps": []
    },
    {
      "id": "tools-at-time",
      "title": "The selected agent's tool use up to the chosen time",
      "goal": "Show, for the lane selected at the chosen time, the tools that agent has used up to that moment together with the parameters of each call.",
      "criteria": [],
      "status": "dropped",
      "note": "Merged into context-at-time, which now carries all three of its criteria. content-pipeline showed tool parameters arrive as the same content records as request bodies, retrieved through the same query with the same time bound and the same per-agent join, so building them as a separate increment would repeat that increment's selection and attribution work for the sake of one extra list in the same panel.",
      "steps": []
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-07T09:12:30.164Z"
      },
      {
        "label": "replan:content-pipeline",
        "at": "2026-08-07T09:55:42.995Z"
      },
      {
        "label": "replan:timeline-landing",
        "at": "2026-08-07T11:06:23.979Z",
        "return": {
          "summary": "Closed timeline-landing as done (review accepted, no findings, after two correction rounds that were both test gaps rather than defects) and re-cut the three open increments against what it showed. Nothing was split, merged, dropped or reordered: the run confirmed the cut rather than contradicting it. Two facts changed the criteria. First, timeline-landing placed the record-to-agent join collector-side and fixed a lane vocabulary - every span of a session resolves to 'main', 'agent:<agent_id>' or 'unattributed', by agent_id first and the parent-span chain second, served by GET /api/sessions/:id/agents - so lane-activity and context-at-time now carry a criterion each that they select records by that same vocabulary instead of building a second attribution scheme, and lane-activity carries the unattributed-stays-visible criterion explicitly. Content-pipeline had already shown that body events carry no agent_id, which makes the span chain the only route from a content record to a lane; lane-activity's researcher settles that route first, since it is the last load-bearing unknown of the issue. Second, both of timeline-landing's correction rounds went on behaviour that was computed by a well-tested pure helper and never pinned in the rendered page - first the lane geometry, then the landing wiring itself - so each of the three open increments now carries one criterion demanding that what the human sees is what proves it done. The fake-DOM harness that round 2 built (tools/argus-ui/test/app.test.mjs boots public/app.js in-process) makes that cheap now, which is why the criterion is affordable. Order unchanged and deliberate: lane-activity first because per-lane attribution of activity and size records is the riskiest remaining piece, scrub-live second because it establishes the one chosen time, context-at-time last because it answers for exactly that time on exactly those lanes.",
          "increments": [
            {
              "id": "content-pipeline",
              "status": "done"
            },
            {
              "id": "timeline-landing",
              "status": "done"
            },
            {
              "id": "lane-activity",
              "status": "todo"
            },
            {
              "id": "scrub-live",
              "status": "todo"
            },
            {
              "id": "context-at-time",
              "status": "todo"
            },
            {
              "id": "tools-at-time",
              "status": "dropped"
            }
          ],
          "questions": []
        }
      }
    ]
  }
}
