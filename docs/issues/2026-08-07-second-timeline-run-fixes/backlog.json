{
  "version": 1,
  "issue": "docs/issues/2026-08-07-second-timeline-run-fixes",
  "workflow": "agile-loop",
  "increments": [
    {
      "id": "coverage-only-round",
      "title": "A correction round of pure coverage gaps runs test-author and reviewer only",
      "goal": "Let the reviewer classify each finding it returns as a coverage gap or a defect, and have the workflow run a round whose findings are all coverage gaps through test-author and reviewer alone, visibly and on the record.",
      "criteria": [
        "The reviewer's structured return carries a per-finding classification field that says whether the finding is a coverage gap or a defect; it is a value the reviewer sets, not a phrase the workflow greps out of prose.",
        "When every finding of a round is classified a coverage gap, that correction round runs the test-author and the reviewer only, and does not run the researcher or the implementer.",
        "A round with at least one finding classified a defect runs the full four-agent chain exactly as it does today.",
        "The workflow log()s that it is correcting coverage only when it takes the shortened path.",
        "The reviewer's classification of each finding is recorded in backlog.json alongside the round it belongs to.",
        "The reviewer's own brief states what makes a finding a coverage gap rather than a defect, so the classification is decided the same way twice.",
        "The test suite passes."
      ],
      "status": "done",
      "note": "Accepted at review round 2 with no findings: the per-finding kind enum, the coverage-only routing in both workflows, the log line, the recorded classification and the reviewer's stated rule are all pinned by test-repo.sh drivers w17/w18/w19 and a backlog recorder case; bash test-repo.sh 71 cases exit 0 and ./test.sh all 6 suites exit 0.",
      "steps": []
    },
    {
      "id": "ui-harness-null",
      "title": "The UI test harness returns null for an id the rendered markup does not contain",
      "goal": "Stop the argus-ui test harness conjuring an element for every id, so an assertion that reads a container the page never rendered fails the way a browser would, and prove the harness with a case in the suite.",
      "criteria": [
        "installFakes in tools/argus-ui/test/app.test.mjs resolves an id the rendered markup does not contain to null rather than to a created-and-cached fake element.",
        "Every existing case in the suite still passes, or is corrected to assert what the page actually renders; the report says which cases were corrected and why each was passing against markup the page never wrote.",
        "A case in the suite proves the harness itself: it removes a container from the rendered markup and shows at least one assertion going red as a result, so the guard is part of the run rather than a manual mutation check.",
        "npm --prefix tools/argus-ui test passes, with the case count reported."
      ],
      "status": "done",
      "note": "Accepted at review round 0 with no findings: installFakes now models the document as one mutable markup string seeded from public/index.html and resolves an id only while that markup carries it, three cases pin the null contract from different directions, and a dropIds guard case makes the panel assertion go red inside the run; npm --prefix tools/argus-ui test 240 cases exit 0 against 234 at the merge base, so the six new cases are the whole delta. The file the issue named did not exist - commit 1c94c20 had already removed the conjuring harness - so the increment created the contract rather than repairing it, and criterion 2 was answered rather than edited: no existing case was passing against markup the page never wrote, because nothing executed app.js against a document at all.",
      "steps": []
    },
    {
      "id": "run-report",
      "title": "parse-agent-log reports a whole workflow run",
      "goal": "Give bin/parse-agent-log a mode that takes a workflow run directory and reports the run as one thing, and point the retro skill at that mode instead of at a hand-run loop.",
      "criteria": [
        "bin/parse-agent-log accepts a workflow run directory and reports the run as a single report rather than one transcript at a time.",
        "That report carries per-agent-type totals, per-agent rows in the journal's order, a tool breakdown that shows failures, and which agents started without returning.",
        "The existing single-transcript behaviour of the tool still works.",
        "The retro skill's instructions tell the session to use the run-directory mode and no longer tell it to loop the tool over each transcript and re-join the results by hand.",
        "No page left in the repository still describes the tool as transcript-at-a-time only or still asks a reader to re-join results by hand; the report names each page that documents the tool and says whether it needed correcting.",
        "The report states where the tree differs from the issue's account of the tool, and each criterion is judged by the reporting behaviour existing rather than by a file the issue named having been edited.",
        "The new mode has test coverage that goes red when the report drops a section or miscounts, and the test run is reported with its case count."
      ],
      "status": "done",
      "note": "Accepted at review round 0 with no findings: parse-agent-log branches on a directory argument to emit one run report with run totals, per-agent-type totals, per-agent rows in journal order, a tool breakdown with failures and a started-without-returning section, --latest-run finds the newest run directory, and README.md and skills/retro/SKILL.md were both corrected to name the run mode; npm --prefix tools/log-parser test 41 cases exit 0 and bash test-repo.sh 73 cases exit 0. The account-for-the-tree criterion paid again: bin/parse-agent-log is a shim over tools/log-parser, the journal carries no agent type so the report depends on sibling meta files the issue never mentions, and no page had ever instructed the per-transcript loop the issue describes - so criterion 5 was met by adding the run mode to the two pages that document the tool rather than by deleting a stale instruction.",
      "steps": []
    },
    {
      "id": "agent-affordances",
      "title": "backlog.mjs record takes the step return on stdin",
      "goal": "Let the recorder read a step return from stdin as well as from a file path, and instruct agents to use the stdin form, so no agent has to survive a heredoc to record a summary containing quotes or markup.",
      "criteria": [
        "backlog.mjs record accepts the step return on stdin as well as from a file path, and a return containing quotes, HTML attributes and newlines records correctly through the stdin form.",
        "The file-path form of backlog.mjs record keeps working unchanged.",
        "The agent brief instructs agents to record their step return through the stdin form.",
        "Every other page that tells an agent how to record a step return says the same thing as the brief; the report names each page checked and each corrected.",
        "The change has test coverage that goes red when the stdin form breaks, when the file-path form regresses, or when a page stops carrying the stdin instruction - a guard that asserts what the pages now say rather than only that they no longer say the old thing; the test run is reported with its case count."
      ],
      "status": "done",
      "note": "Accepted at review round 0 with no findings: record delegates any payload argument other than `-` to the unchanged file reader and reads fd 0 for `-`, exiting 2 with a stdin-naming message on empty stdin, malformed stdin and a TTY; the shared brief and both workflows' recordStep() now document the same quoted-heredoc stdin form, and the five agent pages defer to the brief verbatim. Guarded by seven new node cases (stdin round-trip of quotes, HTML attributes and newlines; the dual meaning of `-` in one call; file/stdin parity; three failure modes; missing payload argument exits rather than hangs), two positive greps on the brief and a w20 driver mode over both workflows' dispatched prompts. node --test skills/agent-brief/assets/backlog.test.mjs 23 cases exit 0; bash test-repo.sh 77 cases exit 0.",
      "steps": []
    },
    {
      "id": "collector-fixture",
      "title": "An agent that needs a live collector has a documented way to start and stop one",
      "goal": "Make starting and stopping a collector for ad-hoc inspection a documented, reachable thing rather than something an agent improvises with a pkill, and name it where agents will find it.",
      "criteria": [
        "A documented way to start and stop a collector for ad-hoc inspection exists - a script or an existing test helper made reachable.",
        "Starting and then stopping it leaves no process holding the port, and the report shows that checked.",
        "The agent brief names that way, so an agent needing a live instance is told what to run instead of improvising a pkill; the report names every page checked for where an agent is told how to reach a running collector and every page corrected.",
        "The report states where the tree differs from the issue account of it - a helper that is not there, or a way that already exists - and each criterion is judged by the start-and-stop way existing and being documented rather than by a file the issue named having been edited.",
        "The change has test coverage that goes red when start or stop breaks, or when the agent brief stops naming the way, and the test run is reported with its case count."
      ],
      "status": "todo",
      "note": "Extended criterion 3 to a page sweep: agent-affordances found the record instruction living in four places at once (the brief, both workflow scripts, five agent pages that defer to it) and the reviewer had to enumerate them, so the collector instruction is to be checked the same way rather than assumed to live only in the brief. The positive-assertion doc guard and the account-for-the-tree criterion stay; both have now paid twice.",
      "steps": [
        {
          "label": "research:collector-fixture.0",
          "at": "2026-08-07T22:16:21.273Z",
          "return": {
            "needsTests": true,
            "summary": "Add tools/argus/scripts/with-collector.mjs, a fixture that starts a collector on a free port, runs one command against it and stops it whatever that command does, and name it in the shared agent brief and tools/argus/CLAUDE.md, guarded by a new node:test suite and five doc cases in test-repo.sh.",
            "plan": "## What the tree actually holds, against the issue's account\n\nReport these facts in the implement step's report; criterion 4 asks for them.\n\n- The issue says no fixture starts and stops the collector for an agent. True: `tools/argus/scripts/` holds only `demo-emit.mjs`, and nothing anywhere starts a collector for ad-hoc use.\n- The issue says `tools/argus/test/background.test.mjs` and `persist.test.mjs` already know how to start and stop one, unreachably. True and precise: `persist.test.mjs` line 111 has a private `startCollector({ cwd, args })` returning `{ base, port, proc, stop }`; `background.test.mjs` has `freePort()`, `runBackground()`, `collectorAnswers()` and `waitForSilence()`. Both are module-local in test files; neither is importable or runnable by an agent.\n- What the issue does not say, and the change turns on: the collector already has the whole stop mechanism. `bin/argus.mjs start --exit-with <pid>` installs `exitWhenGone` (`src/background.mjs`), so a collector dies by itself when the process it was started from dies, and `--ready-fd 3` makes it announce `{ready, pid, port, endpoint, token, persist}` on fd 3. So the fixture is a thin wrapper over shipped mechanism, not a new lifecycle.\n- `skills/argus/SKILL.md` line 29 and `tools/argus/README.md` line 45 both say \"there is no stop command\" — a true statement about the CLI, and the reason an agent that wanted a throwaway instance improvised a `pkill`.\n\n## What gets built\n\n### 1. `tools/argus/scripts/with-collector.mjs` (new, executable-by-node, ESM, zero dependencies)\n\nUsage: `node tools/argus/scripts/with-collector.mjs [--port <n>] -- <command> [args...]`.\n\nBehaviour, in order:\n\n1. Parse arguments by hand: `--port <n>` (also `--port=<n>`), `--help`, and everything after the first bare `--` is the command. Do not reuse `parseArgs` from `src/config.mjs` — it has no notion of a `--` terminator and would swallow the command's own flags.\n2. With no `--`, or with nothing after it, print the usage text on stderr and exit 2 without starting anything. `--help` prints the same usage on stdout and exits 0.\n3. Choose the port: the given `--port`, or one the OS hands out by binding a `net` server to port 0 on 127.0.0.1 and closing it — the same trick the suites use. Never a hard-coded default.\n4. Open a log file for the collector's own output at `path.join(os.tmpdir(), `argus-with-collector-${process.pid}.log`)` and pass its fd as the child's stdout and stderr, so a collector that fails to start leaves something to read.\n5. Start the collector with `spawnBackground` imported from `../src/background.mjs`, argv `[<abs path to ../bin/argus.mjs>, 'start', '--port', String(port), '--no-persist', '--ready-fd', '3', '--exit-with', String(process.pid)]`. `--no-persist` is deliberate: ad-hoc inspection must leave no `.uroboros-telemetry` directory in whatever project the agent is standing in. `--exit-with <own pid>` is the backstop that makes the port free even when this wrapper is killed outright.\n6. On rejection from `spawnBackground`, print one line on stderr naming the port, the log file and the error message, and exit 1 without running the command.\n7. Print a banner on stderr — never stdout, so the command's stdout stays clean for an agent to parse — naming the endpoint, the pid as `pid <n>`, and that the collector stops when the command exits.\n8. Run the command with `spawn(command, args, { stdio: 'inherit', env: { ...process.env, ARGUS_URL: `http://127.0.0.1:${port}`, ARGUS_PORT: String(port) } })`, no shell.\n9. Stop the collector in a `finally`, and from a `SIGINT`/`SIGTERM` handler: `process.kill(pid, 'SIGTERM')`, poll `isRunning(pid)` (exported by `src/background.mjs`) every 50 ms for up to 5 s, then `SIGKILL` and poll for another 2 s, then confirm nothing accepts a connection on the port for up to 5 s.\n10. Exit with the command's own exit code, or `128 + signal` when a signal killed it. If step 9 could not free the port, print a line naming the port and the pid and exit 70 instead, whatever the command returned — a leaked port is a failure of the fixture's one promise and must not be reported as success.\n\nNo `--token`, no `--persist`, no state file, no `stop` subcommand: the collector's design (`src/background.mjs`, and `background.test.mjs`'s \"no pidfile, no stop command, no registry\") is that a collector dies with the process it was started from, and a pidfile in a fixture would be a second, contradictory lifecycle. Rejected for that reason: a `start`/`stop` pair backed by a pidfile in the temp directory. Rejected as too little: exporting the test helper from a shared module, which leaves an agent writing Node to use it. Rejected as scope: an `argus stop` command in the CLI, which changes what the shipped tool promises and would need `skills/argus/SKILL.md`, `tools/argus/README.md` and `HELP` rewritten.\n\nThe child command gets `ARGUS_URL` and `ARGUS_PORT` and no other new variable. `UROBOROS_OBS_*` are the collector's configuration inputs; handing one back as an output would make the same name mean two things.\n\n### 2. `skills/agent-brief/SKILL.md`\n\nAdd one short section after \"Reporting a command run\" and before \"Your step return\", titled `## A live collector`. It must contain, in imperative sentences and once each:\n\n- the literal command `node tools/argus/scripts/with-collector.mjs [--port <n>] -- <command>`;\n- that it starts a collector on a free port, hands the command `ARGUS_URL` and `ARGUS_PORT`, and stops that collector when the command exits, whatever the command's exit code;\n- that an agent must not start a collector by hand and must not kill one with `pkill`;\n- that the script is there in an uroboros checkout — the plugin ships `skills/`, `workflows/` and `agents/` only (`.claude-plugin/plugin.json`), so an agent working in another project has no `tools/argus/`.\n\nKeep it to four sentences or fewer. Every agent pays for this page on every dispatch.\n\n### 3. `tools/argus/CLAUDE.md`\n\nUnder \"## Running it\", add one line to the existing bash block:\n\n```\nnode scripts/with-collector.mjs -- <cmd>   # a collector for one command, stopped when it exits\n```\n\n### 4. `test-repo.sh`\n\nAppend a new section immediately before the final `echo` and the `if [ \"$failed\" -eq 0 ]` summary block, with the five doc/asset cases the test plan specifies.\n\n## Pages checked, and what was done with each\n\nCriterion 3 asks for the sweep, so the implement report must carry this list. Checked for telling an agent how to reach a running collector: `skills/agent-brief/SKILL.md` (said nothing — corrected), `tools/argus/CLAUDE.md` (\"Running it\" showed only the blocking `node bin/argus.mjs` — corrected), `README.md`, `tools/argus/README.md`, `skills/argus/SKILL.md`, `tools/argus-ui/README.md`, `tools/argus-ui/CLAUDE.md`, `rulebook.md`, `.claude/rules/agents.md`, all five `agents/*.md`, `skills/CLAUDE.md`, `skills/grill/SKILL.md`, `skills/retro/SKILL.md`, `workflows/loop.js`, `workflows/agile-loop.js`, `hooks/session-start.sh`, `bin/argus`.\n\nOf those, `rulebook.md`, `.claude/rules/agents.md`, the five agent pages, `skills/grill/SKILL.md`, `skills/retro/SKILL.md`, `skills/CLAUDE.md`, both workflow scripts and `hooks/session-start.sh` never mention a collector at all — nothing to correct. `README.md`, `tools/argus/README.md`, `skills/argus/SKILL.md` and the two argus-ui pages document `argus start --background` for a session that is measuring itself; they are user-facing and ship through the plugin, so they must not gain a `tools/argus/scripts/…` path that does not exist outside a checkout, and their \"there is no stop command\" sentences stay true of the CLI. Leave all five untouched, and say so in the report rather than letting the omission read as an oversight. Do not repeat the instruction into the agent pages or the workflow prompts: the shared brief's own opening rule is that it carries what holds for every agent so a role page need not repeat it, and a second wording of one rule is what drifts.\n\n## What the implement report has to state\n\n- The tree-versus-issue account above.\n- The page sweep above: every page checked, every page corrected, and why the five documented pages were left.\n- That starting and stopping leaves no process holding the port, shown by a run you actually did. Beyond the checks, run this pair from the repository root and quote both exit codes:\n  `node tools/argus/scripts/with-collector.mjs --port 4399 -- node -e \"fetch(process.env.ARGUS_URL + '/api/health').then((r) => r.json()).then((b) => console.log(JSON.stringify(b)))\"`\n  then\n  `node -e \"const s = require('net').connect({ host: '127.0.0.1', port: 4399 }); s.on('connect', () => { console.log('HELD'); s.destroy(); process.exit(1); }); s.on('error', () => { console.log('free'); process.exit(0); });\"`\n  The second has to print `free` and exit 0.\n- Each check command with its case count and exit code.\n- Judge each criterion by whether the start-and-stop way exists and is documented, not by which file was edited.",
            "moduleMap": "tools/argus/scripts/with-collector.mjs — NEW. The fixture. Entry point: top-level `main(process.argv.slice(2))` with a `.catch` that prints `with-collector: <message>` and exits 1, mirroring bin/argus.mjs's tail. Imports `spawnBackground` and `isRunning` from ../src/background.mjs.\ntools/argus/src/background.mjs — UNCHANGED, read only. Exports `spawnBackground({ argv, readyTimeoutMs, cwd, env, stdio })` (spawns process.execPath detached with a ready pipe on fd 3, resolves with the child's announced JSON), `isRunning(pid)`, `exitWhenGone(pid, onGone, { intervalMs })`, `withoutFlags`.\ntools/argus/bin/argus.mjs — UNCHANGED, read only. The collector the fixture spawns; `start --ready-fd 3` announces `{ ready, pid, port, endpoint, token, persist }`, `--exit-with <pid>` makes it die with that process, `--no-persist` keeps nothing on disk.\ntools/argus/test/with-collector.test.mjs — NEW. The fixture's suite, picked up by the package's `test/*.test.mjs` glob.\ntools/argus/test/background.test.mjs — UNCHANGED, read only. The conventions to copy: `freePort()`, `projectDir()`, `collectorAnswers(port)`, `waitForSilence(port, ms)`, `alive(pid)`, the `/\\bpid\\b[^0-9]{0,12}(\\d+)/i` banner match.\ntools/argus/test/persist.test.mjs — UNCHANGED, read only. Its module-local `startCollector({ cwd, args })` at line 111 is the unreachable knowledge this increment replaces.\nskills/agent-brief/SKILL.md — the shared brief every agent preloads; gains a `## A live collector` section between \"Reporting a command run\" and \"Your step return\".\ntools/argus/CLAUDE.md — the page an agent changing argus reads; its \"## Running it\" bash block gains the fixture line.\ntest-repo.sh — the repository's own facts suite; `ok`/`no` helpers, `passed`/`failed` counters, a summary at the end. Gains a section immediately before that summary.\ntest.sh — UNCHANGED. It already runs `test-repo.sh` and `npm --prefix tools/argus test --silent`, so both new suites are in the command that proves the repository green without editing it.",
            "environment": "Node >= 20.11 and bash are on the PATH; check with `node --version`. No install step: tools/argus has zero runtime and zero dev dependencies, `npm --prefix tools/argus test --silent` just runs `node --test \"test/*.test.mjs\"` and works with no node_modules present. There is no linter and no formatter in this repository. The tests bind loopback ports only and need no network. `node --test` runs the suite's files concurrently, so every case must take its port from the OS rather than hard-coding one. The argus suite is slow by construction — background.test.mjs sleeps for seconds at a time — so allow several minutes for it.\n\nCommands used anywhere in this plan:\n- `npm --prefix tools/argus test --silent` — the whole argus suite, from the repository root.\n- `node --test tools/argus/test/with-collector.test.mjs` — only the new suite, from the repository root, for iterating.\n- `bash test-repo.sh` — the repository facts suite, from the repository root; prints `PASS: <n> cases` or `FAIL: <n> of <m> cases`.",
            "testPlan": "Tests are needed. Two suites: a behavioural one for the fixture, and doc/asset cases in the repository suite so the brief cannot quietly stop naming it.\n\n## A. tools/argus/test/with-collector.test.mjs (NEW, node:test, unit-level-integration)\n\nFramework: `node:test` with `node:assert/strict`, ESM `.mjs`, no dependencies. Run just this file with `node --test tools/argus/test/with-collector.test.mjs` from the repository root.\n\nConventions to follow, taken from tools/argus/test/background.test.mjs — copy them, do not invent:\n- Header imports: `import test from 'node:test'; import assert from 'node:assert/strict';` then `node:fs`, `node:http`, `node:net`, `node:os`, `node:path`, `node:child_process`, `node:events`, `node:timers/promises` as `delay`, `node:util`'s `promisify`, `node:url`'s `fileURLToPath`.\n- `const FIXTURE = fileURLToPath(new URL('../scripts/with-collector.mjs', import.meta.url));`\n- Copy these helpers verbatim in shape: `projectDir()` (`fs.realpathSync(fs.mkdtempSync(path.join(os.tmpdir(), 'argus-project-')))`), `freePort()` (bind a `net` server to 0 on 127.0.0.1, read `address().port`, close), `collectorAnswers(port)` (fetch `/api/health`, `.ok`, false on throw), `waitForSilence(port, timeoutMs)` (poll `collectorAnswers` until false), `alive(pid)` (`process.kill(pid, 0)` in try/catch).\n- Add one helper of your own, `nothingAccepts(port, timeoutMs)`: poll a raw `net.connect` on 127.0.0.1 until the connection is refused, returning true, or the deadline passes, returning false. A silent `/api/health` is weaker evidence than a refused connection, and the criterion is about the port being held.\n- Run the fixture with `promisify(execFile)(process.execPath, [FIXTURE, ...args], { cwd, timeout: 60_000, encoding: 'utf8' })` and `.then(ok => ok, failure => failure)` where a non-zero exit is expected, exactly as `runBackground` and `secondStart` do there; read `result.code` for the exit code and `${result.stdout ?? ''}${result.stderr ?? ''}` for the output.\n- Child commands are `.mjs` files written into the case's temp directory with `fs.writeFileSync` and run as `process.execPath <script>`, the way the `spawnBackground` cases write `child.mjs`.\n- Test names are lowercase sentences describing the behaviour, not the function. Every `assert` carries a message saying why it matters.\n- Every case cleans up in `finally`: `fs.rmSync(dir, { recursive: true, force: true })`, close any server, kill any process it spawned, guarding `process.kill` with try/catch since a process that has already gone is not a failure.\n- No case asserts a wall-clock duration; the per-command `timeout` is the only clock (tools/argus/CLAUDE.md).\n\nThe cases:\n\n1. `a command run through the fixture reaches a live collector and leaves no measurement behind` — criterion 1. State: fresh `projectDir()` as cwd, no `--port`. Input: a child script that fetches `${process.env.ARGUS_URL}/api/health`, writes the parsed body to `out.json` in its cwd, and prints `process.env.ARGUS_PORT` on stdout. Expected: fixture exits 0; `out.json` parses and its `ok` is `true`; `result.stdout.trim()` equals that port and nothing else, proving the banner went to stderr; `.uroboros-telemetry` does not exist in the temp directory.\n\n2. `the port is free and the collector gone once the fixture has returned` — criterion 2. State: `const port = await freePort()`, fresh cwd. Input: `--port <port> -- node <script that fetches /api/health once>`. Expected: exit 0; the pid parsed out of stderr with `/\\bpid\\b[^0-9]{0,12}(\\d+)/i` is present, and `alive(pid)` is false (poll up to 5 s); `nothingAccepts(port, 10_000)` is true.\n\n3. `a command that fails still takes the collector down, and its exit code comes back` — criterion 2, the exact failure the issue records. Input: a child script that fetches `/api/health` and then calls `process.exit(3)`. Expected: fixture exit code is 3; `nothingAccepts(port, 10_000)` is true; the banner's pid is not alive.\n\n4. `two runs on the same port both get a collector` — the repeat edge. State: one `freePort()` used for both. Input: the same `--port <port> -- node <script writing a marker with the health body>` run twice in sequence, in two fresh cwds. Expected: both exit 0, both markers report `ok: true`, and `nothingAccepts(port, 10_000)` after the second. This is what goes red if the stop only silences the collector without releasing the socket.\n\n5. `a fixture killed outright leaves no collector on the port` — the backstop, and the ugly path that produced the `pkill`. State: `freePort()`. Input: `spawn` (not `execFile`) the fixture with `--port <port> -- node -e \"setTimeout(() => {}, 120000)\"`, capturing stderr; wait until `collectorAnswers(port)` is true (poll to a 25 s deadline) and parse the pid from the stderr seen so far; then `wrapper.kill('SIGKILL')` and `await once(wrapper, 'exit')`. Expected: `nothingAccepts(port, 30_000)` is true — the 5 s `exitWhenGone` poll needs room — and the collector pid is not alive. Finally: kill the wrapper, the collector pid and any surviving child, each in try/catch.\n\n6. `a port already held is reported, and the command never runs` — criterion 1's failure edge. State: an `http.createServer` squatter listening on `freePort()`; a fresh cwd. Input: `--port <port> -- node <script that writes marker.txt>`. Expected: exit code is non-zero; the combined output contains `String(port)`; `marker.txt` does not exist; the squatter is still listening afterwards (`collectorAnswers` is irrelevant here — assert the server object has not been closed by fetching it, or simply that `nothingAccepts` is false). Finally: close the squatter.\n\n7. `with no command it says how it is used` — the empty edge. Input: the fixture with no arguments at all, and separately with `--port <freePort>` and no `--`. Expected: both exit with code 2 and print usage on stderr naming `--`; assert the output mentions `with-collector`. Nothing needs cleaning up.\n\nLeft untested, deliberately: the `--help` text and its wording; the exit-70 path for a teardown that could not free the port, which no case can provoke without breaking the collector on purpose; the location and contents of the collector's temp log file; whether the documentation prose reads well, which is the reviewer's to judge; two fixtures running at once, which the OS-assigned default port makes structural.\n\n## B. test-repo.sh (EXISTING, bash)\n\nFramework: plain bash with the file's own `ok \"<why it passed>\"` and `no \"<why it failed>\"` helpers and the `passed`/`failed` counters. Append one section immediately before the closing `echo` and the `if [ \"$failed\" -eq 0 ]` summary, opened exactly like its neighbours:\n\n```\necho\necho \"=== an agent has a documented way to start and stop a collector\"\n```\n\nPut a comment above each case naming what it guards, the way the sections at lines 1269-1315 do, and print the offending lines with `| sed 's/^/       /'` in the `no` branch of a grep case. Run the whole file with `bash test-repo.sh` from the repository root.\n\nThe cases:\n\n1. The shared brief names the fixture: `grep -qF 'tools/argus/scripts/with-collector.mjs' \"$root/skills/agent-brief/SKILL.md\"`. `-F` because the string carries slashes and dots. This is the case that goes red when the agent brief stops naming the way — the criterion says so in as many words.\n2. The shared brief names the variable the fixture hands the command: `grep -qF 'ARGUS_URL' \"$root/skills/agent-brief/SKILL.md\"`, so an agent is told how to reach the collector and not only how to start it.\n3. `tools/argus/CLAUDE.md` names it: `grep -qF 'scripts/with-collector.mjs' \"$root/tools/argus/CLAUDE.md\"`.\n4. The script exists: `[ -f \"$root/tools/argus/scripts/with-collector.mjs\" ]`.\n5. The script parses: `node --check \"$root/tools/argus/scripts/with-collector.mjs\" >/dev/null 2>&1` — the same pair the recorder gets at lines 321-330.\n\nAll five are positive assertions about what the pages say now, not the absence of an old phrase. No absence guard over `pkill`: the brief's own sentence tells agents not to use one, so a guard forbidding the word across the same file set would fail on the fix.\n\n## What is already red\n\nNothing was run for this plan; no command was executed to produce it. Before the implementation lands, the new suite does not exist and the five bash cases are absent, so `npm --prefix tools/argus test --silent` and `bash test-repo.sh` are expected green on the current tree and go red only in the test-author's step, which is where the first run belongs.\n\n## What counts as done\n\nThe two commands in `checks`, from the repository root, both exit 0. `npm --prefix tools/argus test --silent` covers the new suite together with the rest of the argus package, which also proves the new file coexists with the others under `node --test`'s concurrent file execution; running the file alone as well would buy nothing the package run does not. `./test.sh` as a whole is not in the list: the other suites are untouched by this increment, and the issue's \"`./test.sh` is green\" criterion is not one of mine.",
            "checks": [
              "npm --prefix tools/argus test --silent",
              "bash test-repo.sh"
            ],
            "questions": []
          }
        },
        {
          "label": "implement:collector-fixture.0",
          "at": "2026-08-07T22:34:42.174Z",
          "return": {
            "summary": "Added tools/argus/scripts/with-collector.mjs, a zero-dependency wrapper that starts a collector on a free port with --no-persist --ready-fd 3 --exit-with <own pid>, runs one command with ARGUS_URL and ARGUS_PORT, and stops that collector on the way out, and named it in skills/agent-brief/SKILL.md and tools/argus/CLAUDE.md. TREE VERSUS ISSUE, as found: tools/argus/scripts/ held only demo-emit.mjs and nothing started a collector for ad-hoc use; persist.test.mjs line 111 has a module-local startCollector({cwd,args}) and background.test.mjs has freePort(), runBackground(), collectorAnswers() and waitForSilence(), all unreachable from outside their test files; the collector already ships the whole stop mechanism, so the fixture is a thin wrapper over start --ready-fd 3, --exit-with <pid> and --no-persist rather than a new lifecycle; skills/argus/SKILL.md line 29 and tools/argus/README.md line 45 still say there is no stop command, which stays true of the CLI. PAGE SWEEP: checked skills/agent-brief/SKILL.md (said nothing about reaching a collector - corrected with a three-sentence '## A live collector' section between 'Reporting a command run' and 'Your step return'), tools/argus/CLAUDE.md ('## Running it' showed only the blocking node bin/argus.mjs - corrected with one line in the bash block), README.md, tools/argus/README.md, skills/argus/SKILL.md, tools/argus-ui/README.md, tools/argus-ui/CLAUDE.md, rulebook.md, .claude/rules/agents.md, all five agents/*.md, skills/CLAUDE.md, skills/grill/SKILL.md, skills/retro/SKILL.md, workflows/loop.js, workflows/agile-loop.js, hooks/session-start.sh and bin/argus. The five user-facing pages (README.md, tools/argus/README.md, skills/argus/SKILL.md and the two argus-ui pages) were left untouched on purpose: they ship through the plugin, which delivers skills/, workflows/ and agents/ only, so a tools/argus/scripts/... path would not exist for their readers. The remaining pages never mention a collector, and the rule is stated once in the shared brief rather than repeated into agent pages or workflow prompts. PORT EVIDENCE, run from the repository root: 'node tools/argus/scripts/with-collector.mjs --port 4399 -- node -e \"fetch(process.env.ARGUS_URL + '/api/health')...\"' printed the banner on stderr, {\"ok\":true,...} on stdout and exited 0; the follow-up raw connect to 127.0.0.1:4399 printed 'free' and exited 0. CRITERIA: the start-and-stop way exists (the fixture), is reachable by any agent (named in the shared brief with ARGUS_URL) and is named for argus maintainers (tools/argus/CLAUDE.md); the failing-command path returns the command's own exit code and still frees the port (case 3 green).",
            "deviations": [
              "Plan item 4 asked me to append five doc/asset cases to test-repo.sh; the test-author had already left exactly that section in the working tree, so I made no edit there - all five cases pass unchanged.",
              "The plan does not say what happens when the command itself cannot be spawned, so I added a child 'error' handler: the collector is stopped first and the wrapper then exits 127 with the spawn error on stderr, instead of crashing out and leaving the collector up.",
              "The plan names only 'no --', 'nothing after --' and '--help' as argument outcomes; I made an unknown option and a non-numeric or out-of-range --port take the same usage path (message on stderr, exit 2) rather than starting a collector on a nonsense port."
            ],
            "commands": [
              {
                "command": "npm --prefix tools/argus test --silent",
                "exitCode": 1,
                "note": "166 cases, 165 pass, 1 fail. The only red is with-collector.test.mjs case 5's last line, 'the collector process must not survive its wrapper being killed'. It is an artifact of this sandbox's init, not of the fixture: after the wrapper is SIGKILLed the collector's exitWhenGone poll shuts it down, the port is refused about 4.8 s later (the assertion before it, nothingAccepts(port, 30_000), passes), and at that instant the collector is 'Zs [node] <defunct>' with PPID 1, which kill(pid, 0) still reports as alive. pid 1 here is '/process_api --firecracker-init', and I measured its orphan reap latency three times at 1317, 1358 and 1341 ms, while the test checks alive() immediately after the first refused connect. On an init that reaps on SIGCHLD the same code passes. No implementation choice removes the window: the listening socket closes exactly when the process exits, so the zombie always exists at the moment the port frees."
              },
              {
                "command": "bash test-repo.sh",
                "exitCode": 0,
                "note": "PASS: 82 cases, including all five new ones - the shared brief names tools/argus/scripts/with-collector.mjs and ARGUS_URL, tools/argus/CLAUDE.md names scripts/with-collector.mjs, and the script exists and parses."
              },
              {
                "command": "node tools/argus/scripts/with-collector.mjs --port 4399 -- node -e \"fetch(process.env.ARGUS_URL + '/api/health').then((r) => r.json()).then((b) => console.log(JSON.stringify(b)))\"",
                "exitCode": 0,
                "note": "The plan's own start-and-stop check. stdout carried only {\"ok\":true,\"uptimeMs\":134,\"instance\":\"ecdbb0558767\",\"seq\":0}; the banner ('argus on http://127.0.0.1:4399, pid 9422' and 'it stops when the command exits.') went to stderr."
              },
              {
                "command": "node -e \"const s = require('net').connect({ host: '127.0.0.1', port: 4399 }); s.on('connect', () => { console.log('HELD'); s.destroy(); process.exit(1); }); s.on('error', () => { console.log('free'); process.exit(0); });\"",
                "exitCode": 0,
                "note": "Printed 'free': nothing held port 4399 after the fixture returned."
              }
            ],
            "blockers": [
              "with-collector.test.mjs case 5's final assert.equal(alive(collectorPid), false) cannot pass in this sandbox: pid 1 (/process_api --firecracker-init) reaps orphans on a ~1.3 s poll, so the collector is a reaped-a-moment-later zombie at the instant the port frees; the case's port assertion, which is what the criterion is about, passes."
            ],
            "questions": []
          }
        },
        {
          "label": "review:collector-fixture.0",
          "at": "2026-08-07T22:41:15.379Z",
          "return": {
            "findings": [
              {
                "claim": "The increment's own suite is red: `npm --prefix tools/argus test` exits 1, with the new case 'a fixture killed outright leaves no collector on the port' failing although the port was freed as promised.",
                "reproduction": "Run `npm --prefix tools/argus test --silent` on the branch head: 166 cases, 165 pass, `not ok 164 - a fixture killed outright leaves no collector on the port`, exit 1. The assertion that fails is tools/argus/test/with-collector.test.mjs:253, `alive(collectorPid) === false`, reported as `true !== false`. The preceding `nothingAccepts(port, 30_000)` at :249 returns the moment a connect to the port is refused, but the collector's shutdown path in tools/argus/bin/argus.mjs:565-573 closes the listening socket first and only calls `process.exit(0)` from the `server.close` callback, with a 2 s unref'd fallback timer, so the collector process legitimately outlives its freed port by up to two seconds. The case therefore asserts liveness with zero grace right at that window, unlike the sibling case at with-collector.test.mjs:147-153, which polls for the pid to disappear with a 5 s deadline; tools/argus/CLAUDE.md's own convention is that no case on a probe path may hang on wall-clock timing.",
                "criterion": "The change has test coverage that goes red when start or stop breaks, or when the agent brief stops naming the way, and the test run is reported with its case count.",
                "fix": "needs-plan"
              }
            ],
            "reason": "The collector fixture's own suite is red: the case that kills the wrapper outright demands the collector process be gone the instant the port stops accepting, while a shutting-down collector releases its socket first and exits up to two seconds later, so the suite goes red with nothing about starting or stopping actually broken.",
            "questions": [],
            "summary": "Reviewed the whole diff against main for increment 5 (the collector fixture) and ran the two listed commands in one call: `npm --prefix tools/argus test --silent` — 166 cases, 165 pass, 1 fail, exit 1, the failing case being the increment's own 'a fixture killed outright leaves no collector on the port' in tools/argus/test/with-collector.test.mjs, which this change introduced and which is my one finding; and `bash test-repo.sh` — 82 cases, 0 failures, exit 0, including the five new doc guards. The way itself is there and reachable: tools/argus/scripts/with-collector.mjs starts a collector on a free port (or --port), hands the command ARGUS_URL/ARGUS_PORT, runs it with stdout untouched, and stops the collector on any exit with SIGTERM, then SIGKILL, then a port poll, with `--exit-with` as the backstop for an outright kill; skills/agent-brief/SKILL.md's new 'A live collector' section names it and forbids the pkill, and tools/argus/CLAUDE.md names it under 'Running it'. Six of the seven new cases cover start, stop, port release, a failing command, reuse of the same port, a squatted port and usage, and test-repo.sh goes red if the brief stops naming the script or ARGUS_URL, so the criteria on existence, documentation and go-red coverage are met apart from the red case above. Observations, none of them a correction: the brief's line 'the plugin ships skills/, workflows/ and agents/, so a session in another project has no tools/argus/ to run it from' is right about the manifest's declared components but not about the delivered tree — the plugin cache at /root/.claude/plugins/cache/uroboros/uroboros/<hash>/ carries tools/ as well, though the relative path the brief gives still only works in a checkout, so the instruction stands; the brief's absolute 'Never start a collector by hand' sits next to skills/argus/SKILL.md, which tells a session to run `argus start --background` for a measurement, and a reader may take the two as disagreeing; the fixture couples start and stop into one command, so an agent that wants a collector alive across several turns has to wrap its inspection in a single command; and the new suite adds roughly 40 s to the argus run. Blast radius beyond the criteria: the script only reads src/background.mjs and adds no caller to anything, test.sh already runs the package suite that picks up the new file by glob, and no other page in the tree tells an agent to start or kill a collector by hand — the only `pkill` mentions left are in the issue records themselves. The criteria that bind the implementer's report (the port check shown, the pages checked and corrected, where the tree differs from the issue account) live in backlog.json, which I do not read, so I judged them by the tree only."
          }
        },
        {
          "label": "research:collector-fixture.1",
          "at": "2026-08-07T22:46:06.071Z",
          "return": {
            "needsTests": true,
            "summary": "Correct the one red case in tools/argus/test/with-collector.test.mjs by polling for the collector pid to disappear instead of asserting liveness with zero grace, tighten case 1's stdout check against the banner's port, and clean up the SIGKILL case's grandchild; no production code changes.",
            "plan": "## What is wrong\n\nThe increment shipped `tools/argus/scripts/with-collector.mjs`, its documentation in `skills/agent-brief/SKILL.md` and `tools/argus/CLAUDE.md`, five shell guards in `test-repo.sh`, and seven cases in `tools/argus/test/with-collector.test.mjs`. One of those seven is wrong and makes `npm --prefix tools/argus test` exit 1: `a fixture killed outright leaves no collector on the port` asserts `alive(collectorPid) === false` immediately after `nothingAccepts(port, 30_000)` returned.\n\nThe assertion races the collector's own shutdown. `tools/argus/bin/argus.mjs:565-573` defines `shutdown()` as `server.close(() => { persistence?.close(); process.exit(0); })` plus a 2 s `unref`'d fallback `setTimeout(() => process.exit(0), 2000)`. Closing the listening socket happens first, so the port is refused while the process is still alive — legitimately, for up to two seconds. `nothingAccepts` returns the instant a connect is refused, and the next line then demands the process already be gone. That is the whole defect: the fixture behaves correctly, the test does not allow for a window the collector is entitled to.\n\n## The correction\n\nAll of it is in `tools/argus/test/with-collector.test.mjs`. No file under `tools/argus/src`, `tools/argus/bin` or `tools/argus/scripts` changes, and no documentation changes.\n\n1. Add a `waitForExit(pid, timeoutMs)` helper next to `alive`: poll `alive(pid)` every 100 ms, return `true` as soon as the pid is gone, `false` when the deadline passes. Its doc comment says why the wait exists — the collector frees the port from `server.close` and only then exits — so the next reader does not shorten it back. This mirrors the polling the sibling case at lines 147-153 already does inline, and it is a bound rather than a measured duration, which is what `tools/argus/CLAUDE.md` requires of a probe path.\n2. Use `waitForExit` at the three places that assert a collector process is gone: the inline 5 s loop in `the port is free and the collector gone once the fixture has returned` (replaced by the helper), the bare `alive(...)` at line 181 in `a command that fails still takes the collector down`, and the failing line 253. A 10 s deadline everywhere: the collector's own fallback timer guarantees exit about 2 s after shutdown starts, so 10 s is slack, not a measurement, and the helper returns the moment the pid is gone so a healthy run pays nothing for it.\n3. Settle the test-author's open question about the SIGKILL backstop's grandchild. The command the wrapper runs (`node -e \"setTimeout(...)\"`) is spawned with `stdio: 'inherit'`, so it writes to the same stderr pipe the test already reads; `spawnBackground` marks only the collector `detached`, so the command is an ordinary child of the wrapper and SIGKILLing the wrapper does not cascade to it. Make the command announce itself — `process.stderr.write(\\`COMMAND ${process.pid}\\n\\`)` before its timer — and have the case learn that pid from the same stderr it already parses, so the `finally` can kill all three pids. The marker word must not be `pid`: the banner regex `/\\bpid\\b[^0-9]{0,12}(\\d+)/i` would match `child-pid` too. Shorten the command's timer from 120 s to 30 s as a second line of defence, so a grandchild that escapes the `finally` still dies on its own.\n4. Settle the test-author's open question about case 1's stdout. There is a second, independent source for the port: the wrapper prints its own banner on stderr (`with-collector: argus on http://127.0.0.1:<port>, pid <pid>`), which comes from the wrapper's variable, while stdout carries what the child read out of `ARGUS_PORT`. Extract the port from stderr with a loose `/127\\.0\\.0\\.1:(\\d+)/` and assert `stdout.trim()` equals it. That is strictly stronger than `/^\\d+$/`: it still fails if anything but the port lands on stdout, and it additionally fails if the child is handed a port the wrapper is not using. Matching only the address-and-port for discovery follows the precedent of the existing banner match; it does not pin the sentence, which `tools/argus/CLAUDE.md` forbids.\n5. Delete the unused `waitForSilence` helper at lines 35-42 — dead code copied in from `background.test.mjs`, never called in this file.\n\n## Alternatives rejected\n\n- **Make `shutdown()` in `bin/argus.mjs` exit as soon as the socket is closed.** It would silence the race, but it drops `persistence?.close()` and cuts in-flight responses, and that shutdown path is shared by every collector in the tool and pinned by `test/background.test.mjs` and `test/persist.test.mjs`. Changing production behaviour to suit one assertion, in an increment whose production side the reviewer found correct, is the wrong trade.\n- **Delete the pid assertion and let the freed port carry the case.** The port assertion alone stays green against a collector that closes its listener and then lives forever; the pid assertion is the only thing that goes red for that regression. Keep it, polled.\n- **Give the assertion a fixed `await delay(3000)` instead of a poll.** It asserts wall-clock behaviour on a probe path, which is exactly the convention `tools/argus/CLAUDE.md` records after two false reds on a loaded box.\n\n## What the implementer does\n\nNothing in production. The corrected cases are the whole change; confirm the two commands under `checks` pass and report each with its case count and exit code, `npm --prefix tools/argus test --silent` being the one the review named at 166 cases.\n\n## Where the tree differs from the issue's account, for the report\n\nThe issue says no fixture starts and stops the collector. Accurate as far as an agent's reach goes, but the mechanism was already shipped: `argus start` already had `--ready-fd`, `--exit-with` and `--no-persist`, and `tools/argus/src/background.mjs` already exported `spawnBackground`, `isRunning` and `exitWhenGone`. What was missing was a runnable entry point and a page naming it, not the capability; `tools/argus/scripts/with-collector.mjs` is a thin wrapper over what existed, which is why the increment's production side needed no correction this round. The pages checked for where an agent is told how to reach a running collector, and corrected, are `skills/agent-brief/SKILL.md` (new section `A live collector`) and `tools/argus/CLAUDE.md` (the `Running it` block); `tools/argus/README.md` and `skills/argus/SKILL.md` are user-facing and were left alone.",
            "moduleMap": "/home/user/uroboros/tools/argus/test/with-collector.test.mjs — the only file this round changes; 7 `node:test` cases over the fixture, helpers `freePort`, `collectorAnswers`, `waitForSilence` (dead, to be deleted), `alive`, `nothingAccepts`, `runFixture` at the top.\n/home/user/uroboros/tools/argus/scripts/with-collector.mjs — the fixture under test, unchanged; entry `main(process.argv.slice(2))`, `stop(pid, port)` is the SIGTERM-then-SIGKILL-then-port-poll teardown, exit code 70 means the port stayed held.\n/home/user/uroboros/tools/argus/bin/argus.mjs — unchanged; `shutdown()` at lines 565-573 closes the server first and exits from the close callback with a 2 s unref'd fallback, which is the window the failing assertion sat in.\n/home/user/uroboros/tools/argus/src/background.mjs — unchanged; `spawnBackground` spawns the collector `detached` with fd 3 as the ready channel, `exitWhenGone` is the poll that makes `--exit-with` work, `isRunning` is the liveness check.\n/home/user/uroboros/test-repo.sh — unchanged; the five `ok`/`no` guards under `=== an agent has a documented way to start and stop a collector` are the coverage that goes red if a page stops naming the fixture.\n/home/user/uroboros/skills/agent-brief/SKILL.md — unchanged; the `A live collector` section is what those guards read.\n/home/user/uroboros/tools/argus/CLAUDE.md — unchanged; the `Running it` block names `scripts/with-collector.mjs`, and the `Tests` section carries the conventions this suite is held to.",
            "environment": "Node ≥ 20.11, already installed. `tools/argus` has zero runtime dependencies and its suite is `node --test` from the platform, so no `npm install` is needed before any command below. Bash is present. There is no linter and no formatter in this repository — nothing to run and nothing to satisfy beyond the two commands under `checks`, both run from the repository root /home/user/uroboros.\n\n- `npm --prefix tools/argus test --silent` — the whole argus suite, `node --test \"test/*.test.mjs\"`, 166 cases at the branch head.\n- `bash test-repo.sh` — the repository's own shell suite, prints `PASS: <n> cases` or `FAIL`.\n- `node --test tools/argus/test/with-collector.test.mjs` — the one file this round touches, on its own, while iterating.",
            "testPlan": "Tests are needed: the correction lives entirely in the test file, and the test-author is the one who writes it. Everything below is the whole work order; the file already exists at /home/user/uroboros/tools/argus/test/with-collector.test.mjs with all seven cases, so this is an edit, and every case named here has to be in the file when you are done, whether or not it changes.\n\n## Framework and conventions of the file\n\n`node:test` with `node:assert/strict`, ESM `.mjs`, no dependencies and no test framework beyond what Node ships. A case is a top-level `test('<a sentence stating the behaviour, lower case, no prefix>', async () => { ... })`. Every assertion carries a message that says what has to hold and interpolates the captured output (`${result.out}`, `${stderr}`) so a red case is readable without a re-run. A case that creates a temp project directory (`projectDir()`) or a listening squatter tears it down in a `finally`. Ports come from `freePort()`, which binds port 0 and asks the OS — never a literal port. Nothing is mocked: the fixture, the collector and the HTTP calls are all real processes and real sockets. `tools/argus/CLAUDE.md` binds this file to two rules: no case asserts a wall-clock duration on a probe path (deadlines are upper bounds that return early, never measurements), and a message is asserted as an absence or matched loosely for discovery, never pinned as a wording.\n\n## Helper changes, before the cases\n\n- Delete `waitForSilence` (lines 35-42). It is never called in this file.\n- Add, next to `alive`:\n\n```js\n/**\n * The collector gives the port back before it exits: argus.mjs closes the\n * listening socket first and only exits from the close callback, with a 2 s\n * unref'd fallback. A pid asserted the moment a connect is refused is\n * therefore legitimately still alive. Polls until it is gone (true) or the\n * deadline passes (false) — it returns as soon as the pid goes, so this is a\n * bound and never a measured duration.\n */\nasync function waitForExit(pid, timeoutMs) {\n  const deadline = Date.now() + timeoutMs;\n  for (;;) {\n    if (!alive(pid)) return true;\n    if (Date.now() >= deadline) return false;\n    await delay(100);\n  }\n}\n```\n\n## The cases\n\n**Case 1 — `a command run through the fixture reaches a live collector and leaves no measurement behind`** (proves: starting works and the command reaches it; stdout stays the command's own). Unit-level, real processes. Keep as it stands except for the stdout assertion. Input: `runFixture(cwd, ['--', process.execPath, script])`, no `--port`, so the OS picks. State: `cwd` is a fresh `projectDir()`; `script` fetches `${ARGUS_URL}/api/health`, writes the body to `out.json` and writes `process.env.ARGUS_PORT` to stdout. Expected: exit 0; `out.json` parses with `ok === true`; no `.uroboros-telemetry` directory in `cwd`. Replace the `/^\\d+$/` match with a comparison against the second source the test-author asked for — the wrapper's banner, which the wrapper prints from its own port variable on stderr while stdout carries what the child read out of the environment:\n\n```js\nconst banner = (result.raw.stderr ?? '').match(/127\\.0\\.0\\.1:(\\d+)/);\nassert.ok(banner, `the banner has to name the collector's address: ${result.raw.stderr}`);\nassert.equal(\n  (result.raw.stdout ?? '').trim(),\n  banner[1],\n  'the command\\'s stdout has to be exactly the port it was handed, with the fixture\\'s banner kept on stderr',\n);\n```\n\nThat goes red both if anything but the port lands on stdout and if the command is handed a port the wrapper is not on. Do not assert the banner's wording beyond the address.\n\n**Case 2 — `the port is free and the collector gone once the fixture has returned`** (proves: stopping frees the port and ends the process). Input: `runFixture(cwd, ['--port', String(port), '--', process.execPath, script])` with `port` from `freePort()` and a script that fetches `/api/health`. Expected: exit 0; the banner names a pid; that pid is gone; nothing accepts on the port. Change only the inline 5 s poll at lines 147-153 to `assert.ok(await waitForExit(pid, 10_000), 'the collector process the banner named has to be gone once the fixture returns')`. Keep the `nothingAccepts(port, 10_000)` assertion after it, unchanged.\n\n**Case 3 — `a command that fails still takes the collector down, and its exit code comes back`** (proves: the teardown does not depend on the command succeeding, and the exit code is passed through). Input: a script that fetches `/api/health` then `process.exit(3)`, run with `--port`. Expected: exit 3; `nothingAccepts(port, 10_000)`; the banner's pid gone. Change the bare `assert.equal(alive(Number(printed[1])), false, ...)` at line 181 to `assert.ok(await waitForExit(Number(printed[1]), 10_000), 'the collector process must not survive a failing command')`. Everything else unchanged.\n\n**Case 4 — `two runs on the same port both get a collector`** (proves: the stop is a released socket, not a silenced collector — the repeat edge). Unchanged, verbatim.\n\n**Case 5 — `a fixture killed outright leaves no collector on the port`** (proves: the `--exit-with` backstop, i.e. that a wrapper killed with no chance to clean up still leaves the port free and the collector dead). This is the red case; rewrite it as follows.\n\n- Spawn the wrapper with `spawn(process.execPath, [FIXTURE, '--port', String(port), '--', process.execPath, '-e', \"process.stderr.write(`COMMAND ${process.pid}\\\\n`); setTimeout(() => {}, 30000);\"], { stdio: ['ignore', 'ignore', 'pipe'] })` and keep accumulating its stderr as now. The command inherits the wrapper's stderr, so its marker arrives on the same pipe. The marker word must not contain `pid`: the banner regex `/\\\\bpid\\\\b[^0-9]{0,12}(\\\\d+)/i` matches `child-pid` as well.\n- Poll, with the existing 25 s deadline and 200 ms sleep, until both `await collectorAnswers(port)` is true and `stderr` matches `/COMMAND (\\\\d+)/`. Assert both with messages interpolating `stderr`: the collector has to be up before the wrapper can be killed out from under it, and the command has to have named itself so the case can clean it up.\n- Capture `collectorPid` from the banner match as now, and `commandPid` from the `COMMAND` match, both into `let` bindings declared before the `try` so the `finally` sees them.\n- `wrapper.kill('SIGKILL')`, `await once(wrapper, 'exit')`.\n- Assert `await nothingAccepts(port, 30_000)` with the existing message — unchanged, it is the criterion's own assertion and its deadline has to stay wide enough for the 5 s `exitWhenGone` poll.\n- Then assert `await waitForExit(collectorPid, 10_000)` with the message that the collector process must not survive its wrapper being killed. This is the line that was red; the poll is the fix.\n- `finally`: SIGKILL the wrapper, then `commandPid`, then `collectorPid`, each guarded so an already-dead pid is not an error — the wrapper's SIGKILL does not cascade to the command, which is an ordinary child, nor to the collector, which `spawnBackground` starts `detached`.\n\n**Case 6 — `a port already held is reported, and the command never runs`** (proves: the limit edge, a port the fixture cannot take fails loudly and touches nothing). Unchanged, verbatim, including the closing check that the squatter is still listening.\n\n**Case 7 — `with no command it says how it is used`** (proves: the empty edge, no arguments and `--port` with no command are both usage errors at exit 2). Unchanged, verbatim.\n\n## The shell guards, unchanged\n\nKeep the five cases in /home/user/uroboros/test-repo.sh under `=== an agent has a documented way to start and stop a collector` exactly as they are: the shared brief names `tools/argus/scripts/with-collector.mjs`, the shared brief names `ARGUS_URL`, `tools/argus/CLAUDE.md` names `scripts/with-collector.mjs`, the script exists, the script parses under `node --check`. They are this increment's go-red coverage for the agent brief ceasing to name the way. Conventions there: `ok`/`no` counters, `$root`-prefixed paths, `grep -qF` for strings carrying slashes and dots, and a failing branch that prints the near-miss lines.\n\n## Left untested, and why\n\n- The collector's shutdown ordering in `bin/argus.mjs` — untouched production code, already covered by `test/background.test.mjs`.\n- The exact wording of the fixture's banner and usage text, beyond the address and the `--`/`with-collector` tokens the cases already match: `tools/argus/CLAUDE.md` forbids pinning a message the implementer owns.\n- Concurrent runs on two different OS-assigned ports: case 4 covers the reuse of one port, which is the edge that broke in the field.\n- The exit code 70 (`PORT_STILL_HELD`) path: it can only be produced by a collector that refuses SIGKILL, which no test can stage without faking the process table.\n\n## What is already red\n\nI ran nothing. From the review's own reproduction, `npm --prefix tools/argus test --silent` is at 166 cases, 165 passing, with `not ok 164 - a fixture killed outright leaves no collector on the port` the only failure, and the case count stays 166 after this correction because no case is added or removed. `bash test-repo.sh` was green when the increment was reviewed and nothing this round touches it. The first run of the list below is the implementer's.",
            "checks": [
              "npm --prefix tools/argus test --silent",
              "bash test-repo.sh"
            ],
            "questions": []
          }
        },
        {
          "label": "tests:collector-fixture.1",
          "at": "2026-08-07T22:57:09.057Z",
          "return": {
            "summary": "Corrected the seven cases in tools/argus/test/with-collector.test.mjs per the reviewer's reproduction: deleted the unused waitForSilence, added a polling waitForExit, tightened case 1's stdout assertion to the wrapper's own banner, swapped the racy immediate alive() checks in cases 2 and 3 for waitForExit polls, and rewrote case 5 to name the spawned command with a COMMAND marker and poll the collector pid to exit instead of asserting it dead the instant SIGKILL lands; reproduced the original case 5 failure verbatim before applying the fix, then confirmed all 166 cases pass afterward with no production file touched.",
            "cases": [
              {
                "case": "a command run through the fixture reaches a live collector and leaves no measurement behind",
                "file": "tools/argus/test/with-collector.test.mjs",
                "testName": "a command run through the fixture reaches a live collector and leaves no measurement behind",
                "expected": "exit 0; out.json parses with ok === true; no .uroboros-telemetry directory in cwd; stdout is exactly the port the wrapper's own stderr banner named (127.0.0.1:PORT), with the banner itself kept off stdout.",
                "got": "Kept as the plan specified except the stdout assertion, which now compares stdout to the port captured from the banner regex on stderr instead of a bare /^\\d+$/ match. Not the round's red case: ran green both before and after the edit (node --test test/with-collector.test.mjs -> ok 1, 644ms)."
              },
              {
                "case": "the port is free and the collector gone once the fixture has returned",
                "file": "tools/argus/test/with-collector.test.mjs",
                "testName": "the port is free and the collector gone once the fixture has returned",
                "expected": "exit 0; the banner names a pid that goes away (polled, not asserted the instant the process returns); nothing accepts on the port afterward.",
                "got": "Replaced the inline 5s poll at lines 147-153 with assert.ok(await waitForExit(pid, 10_000), ...); the nothingAccepts(port, 10_000) assertion kept unchanged. Not the round's red case: ran green (ok 2, 423ms)."
              },
              {
                "case": "a command that fails still takes the collector down, and its exit code comes back",
                "file": "tools/argus/test/with-collector.test.mjs",
                "testName": "a command that fails still takes the collector down, and its exit code comes back",
                "expected": "exit 3; nothingAccepts(port, 10_000); the banner's pid gone (polled).",
                "got": "Replaced the bare assert.equal(alive(...), false, ...) at line 181 with assert.ok(await waitForExit(Number(printed[1]), 10_000), ...). Not the round's red case: ran green (ok 3, 467ms)."
              },
              {
                "case": "two runs on the same port both get a collector",
                "file": "tools/argus/test/with-collector.test.mjs",
                "testName": "two runs on the same port both get a collector",
                "expected": "each of two sequential runs on the same freePort() reaches a live collector; the port is released (nothingAccepts), not merely silenced, after the second run returns.",
                "got": "Left verbatim as the plan required. Not the round's red case: ran green (ok 4, 815ms)."
              },
              {
                "case": "a fixture killed outright leaves no collector on the port",
                "file": "tools/argus/test/with-collector.test.mjs",
                "testName": "a fixture killed outright leaves no collector on the port",
                "expected": "spawning the wrapper with a command that marks itself with a COMMAND <pid> line on the inherited stderr; once the collector answers and the command has named itself, SIGKILL the wrapper; the port must refuse connections (nothingAccepts, 30s) and the collector pid named by the banner must be gone (waitForExit, 10s) rather than asserted dead the instant the wrapper's exit event fires.",
                "got": "This was the round's red case. Reproduced the original (pre-fix) assertion's failure by running the file as it stood at HEAD in isolation: `node --test --test-name-pattern=\"a fixture killed outright leaves no collector on the port\" test/__original_check.test.mjs` (a scratch copy of the pre-edit file, removed afterward) produced `not ok 1 - a fixture killed outright leaves no collector on the port ... the collector process must not survive its wrapper being killed / true !== false`. After rewriting the case per the plan (COMMAND marker, waitForExit(collectorPid, 10_000) replacing the bare alive() check, and a finally that SIGKILLs the wrapper, then commandPid, then collectorPid, each guarded), the same case passed cleanly across repeated runs (ok 5, 7.2s and 6.6s in two separate suite runs)."
              },
              {
                "case": "a port already held is reported, and the command never runs",
                "file": "tools/argus/test/with-collector.test.mjs",
                "testName": "a port already held is reported, and the command never runs",
                "expected": "a squatter already listening on the port makes the fixture fail loudly (non-zero exit, the port number in its output), never runs the command, and leaves the squatter's own listener untouched.",
                "got": "Left verbatim as the plan required. Not the round's red case: ran green (ok 6, 850ms)."
              },
              {
                "case": "with no command it says how it is used",
                "file": "tools/argus/test/with-collector.test.mjs",
                "testName": "with no command it says how it is used",
                "expected": "no arguments at all, and --port with no command, are both usage errors at exit 2 that name the -- separator and the fixture.",
                "got": "Left verbatim as the plan required. Not the round's red case: ran green (ok 7, 145ms)."
              },
              {
                "case": "the shared brief names tools/argus/scripts/with-collector.mjs (test-repo.sh guard)",
                "file": "test-repo.sh",
                "testName": "",
                "expected": "grep -qF 'tools/argus/scripts/with-collector.mjs' on skills/agent-brief/SKILL.md succeeds, so the guard goes red if the shared brief stops naming the fixture.",
                "got": "Not written or edited by me — the plan says keep it exactly as it stands, unchanged. Confirmed by hand it currently holds: skills/agent-brief/SKILL.md line 76 reads 'Run `node tools/argus/scripts/with-collector.mjs [--port <n>] -- <command>` where'. This increment does not touch test-repo.sh or the pages it checks."
              },
              {
                "case": "the shared brief names ARGUS_URL (test-repo.sh guard)",
                "file": "test-repo.sh",
                "testName": "",
                "expected": "grep -qF 'ARGUS_URL' on skills/agent-brief/SKILL.md succeeds.",
                "got": "Not written or edited by me — unchanged per the plan. Confirmed by hand: skills/agent-brief/SKILL.md line 78 reads '...ARGUS_URL and ARGUS_PORT, and stops it when the command exits...'."
              },
              {
                "case": "tools/argus/CLAUDE.md names scripts/with-collector.mjs (test-repo.sh guard)",
                "file": "test-repo.sh",
                "testName": "",
                "expected": "grep -qF 'scripts/with-collector.mjs' on tools/argus/CLAUDE.md succeeds.",
                "got": "Not written or edited by me — unchanged per the plan. Confirmed by hand: tools/argus/CLAUDE.md's Running-it section reads 'node scripts/with-collector.mjs -- <cmd>   # a collector for one command, stopped when it exits'."
              },
              {
                "case": "the script exists (test-repo.sh guard)",
                "file": "test-repo.sh",
                "testName": "",
                "expected": "tools/argus/scripts/with-collector.mjs is present on disk.",
                "got": "Not written or edited by me — unchanged per the plan. Confirmed by hand: the file exists (7722 bytes)."
              },
              {
                "case": "the script parses under node --check (test-repo.sh guard)",
                "file": "test-repo.sh",
                "testName": "",
                "expected": "`node --check tools/argus/scripts/with-collector.mjs` exits 0.",
                "got": "Not written or edited by me — unchanged per the plan. Confirmed by hand: `node --check tools/argus/scripts/with-collector.mjs` printed no error."
              }
            ],
            "openQuestions": [],
            "questions": []
          }
        }
      ]
    },
    {
      "id": "signing-check",
      "title": "test.sh catches a commit-signing misconfiguration before an agent commits",
      "goal": "Make ./test.sh verify that commit signing actually works in the environment it runs in, so a signing failure surfaces before an agent's first commit rather than after its work is done.",
      "criteria": [
        "./test.sh verifies that commit signing works in the environment it runs in - a signed commit in a throwaway repository, or the equivalent check the setup allows.",
        "The check fails loudly and says what is misconfigured when signing does not work, and leaves nothing behind in the repository under test.",
        "The check distinguishes an environment where signing is legitimately not configured for the run from one where signing is configured but fails, and does not make ./test.sh fail in the first; the report states which behaviour was chosen for each case and why.",
        "./test.sh is green, and the report names the command, what it covered and its exit code."
      ],
      "status": "todo",
      "note": "Sharpened the third criterion to name the two cases apart: the failure the issue evidences is a key configured and then ignored, which a naive is-signing-configured skip would pass over, and the criterion as written left that ambiguous.",
      "steps": []
    },
    {
      "id": "liveness-docs",
      "title": "The loop skill pages tell the session how to tell a dead run from a live one",
      "goal": "Correct the loop skill pages so the invoking session establishes liveness from the task's own status and knows that a user turn kills a running workflow.",
      "criteria": [
        "The loop skill pages tell the invoking session to establish whether the run is alive from the task's own status, and say plainly that transcript file timestamps do not establish it.",
        "The pages say that an ordinary user turn kills a running workflow, and that the killed agent restarts from zero rather than resuming mid-flight, so an interruption costs a whole agent.",
        "The pages tell a session that takes a message mid-run to check liveness and resume rather than to report progress from stale files.",
        "The pages do not describe the periodic self check-in or the resumeFromRunId re-entry, which another issue owns.",
        "No loop skill page is left saying something the coverage-only correction round made false, such as an enumeration of the agents a correction round runs; the report names each page checked.",
        "What the pages now say is provable: a check in the repository goes red when a page stops carrying the liveness rule or the user-turn fact, rather than the criteria resting on a reader's reading; the check is named with its case count."
      ],
      "status": "todo",
      "note": "Added the provability criterion, for the reason run-report's review surfaced: doc criteria guarded only by the absence of an old phrase survive a regression to that phrase's meaning. This is the run's last pass over the loop pages, so it is also where a page left stale by the coverage-only change gets caught.",
      "steps": []
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-07T19:51:17.607Z"
      },
      {
        "label": "replan:coverage-only-round",
        "at": "2026-08-07T20:40:19.155Z"
      },
      {
        "label": "replan:ui-harness-null",
        "at": "2026-08-07T21:19:48.813Z"
      },
      {
        "label": "replan:run-report",
        "at": "2026-08-07T21:48:45.143Z"
      },
      {
        "label": "replan:agent-affordances",
        "at": "2026-08-07T22:07:54.328Z",
        "return": {
          "increments": [
            {
              "id": "coverage-only-round",
              "status": "done",
              "note": ""
            },
            {
              "id": "ui-harness-null",
              "status": "done",
              "note": ""
            },
            {
              "id": "run-report",
              "status": "done",
              "note": ""
            },
            {
              "id": "agent-affordances",
              "status": "done",
              "note": "Closed done on this call: the review accepted it with no findings."
            },
            {
              "id": "collector-fixture",
              "status": "todo",
              "note": "Criterion 3 extended to a page sweep, taught by agent-affordances."
            },
            {
              "id": "signing-check",
              "status": "todo",
              "note": ""
            },
            {
              "id": "liveness-docs",
              "status": "todo",
              "note": ""
            }
          ],
          "questions": [],
          "summary": "Closed agent-affordances as done: the review accepted it at round 0 with no findings, record now reads fd 0 for a payload argument of `-` while the file-path form delegates to the unchanged reader, and the brief plus both workflows' recordStep() document the stdin form, pinned by seven node cases, two positive greps and a w20 driver mode (node --test backlog.test.mjs 23 cases exit 0; bash test-repo.sh 77 cases exit 0). Three increments stay open and four of at most eight are spent, so collector-fixture, signing-check and liveness-docs each still get a chain and the order stands: the two that touch the environment first, the pure-documentation one last. One change to the cut, and it is the only thing this increment taught that bears on the rest: agent-affordances found the record instruction living in four places at once - the shared brief, both workflow scripts, and five agent pages that defer to the brief verbatim - and its reviewer had to enumerate them to prove nothing stale was left. collector-fixture's third criterion said only that the agent brief names the way to start and stop a collector; it now also requires the report to name every page checked for where an agent is told how to reach a running collector, and every page corrected. liveness-docs already carries that sweep in its fifth criterion, so it is unchanged. Two things I deliberately did not turn into work. The reviewer noted the brief now documents a quoted heredoc while its rationale says no agent has to survive a heredoc, and that the heredoc truncates if a return ever contains a line that is exactly RETURN; the criterion the issue set - a summary with quotes or markup records correctly - is met, and both observations are outside the issue's acceptance criteria, so inventing an increment for them would be scope this run was not given. I also left the order alone: risk-first would put signing-check ahead of collector-fixture, but both depend on the environment in the same way and nothing this increment showed separates them, so the reorder would be churn a reader pays for."
        }
      }
    ]
  }
}
