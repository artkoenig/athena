{
  "version": 1,
  "issue": "docs/issues/2026-08-07-second-timeline-run-fixes",
  "workflow": "agile-loop",
  "increments": [
    {
      "id": "coverage-only-round",
      "title": "A correction round of pure coverage gaps runs test-author and reviewer only",
      "goal": "Let the reviewer classify each finding it returns as a coverage gap or a defect, and have the workflow run a round whose findings are all coverage gaps through test-author and reviewer alone, visibly and on the record.",
      "criteria": [
        "The reviewer's structured return carries a per-finding classification field that says whether the finding is a coverage gap or a defect; it is a value the reviewer sets, not a phrase the workflow greps out of prose.",
        "When every finding of a round is classified a coverage gap, that correction round runs the test-author and the reviewer only, and does not run the researcher or the implementer.",
        "A round with at least one finding classified a defect runs the full four-agent chain exactly as it does today.",
        "The workflow log()s that it is correcting coverage only when it takes the shortened path.",
        "The reviewer's classification of each finding is recorded in backlog.json alongside the round it belongs to.",
        "The reviewer's own brief states what makes a finding a coverage gap rather than a defect, so the classification is decided the same way twice.",
        "The test suite passes."
      ],
      "status": "done",
      "note": "Accepted at review round 2 with no findings: the per-finding kind enum, the coverage-only routing in both workflows, the log line, the recorded classification and the reviewer's stated rule are all pinned by test-repo.sh drivers w17/w18/w19 and a backlog recorder case; bash test-repo.sh 71 cases exit 0 and ./test.sh all 6 suites exit 0.",
      "steps": []
    },
    {
      "id": "ui-harness-null",
      "title": "The UI test harness returns null for an id the rendered markup does not contain",
      "goal": "Stop the argus-ui test harness conjuring an element for every id, so an assertion that reads a container the page never rendered fails the way a browser would, and prove the harness with a case in the suite.",
      "criteria": [
        "installFakes in tools/argus-ui/test/app.test.mjs resolves an id the rendered markup does not contain to null rather than to a created-and-cached fake element.",
        "Every existing case in the suite still passes, or is corrected to assert what the page actually renders; the report says which cases were corrected and why each was passing against markup the page never wrote.",
        "A case in the suite proves the harness itself: it removes a container from the rendered markup and shows at least one assertion going red as a result, so the guard is part of the run rather than a manual mutation check.",
        "npm --prefix tools/argus-ui test passes, with the case count reported."
      ],
      "status": "done",
      "note": "Accepted at review round 0 with no findings: installFakes now models the document as one mutable markup string seeded from public/index.html and resolves an id only while that markup carries it, three cases pin the null contract from different directions, and a dropIds guard case makes the panel assertion go red inside the run; npm --prefix tools/argus-ui test 240 cases exit 0 against 234 at the merge base, so the six new cases are the whole delta. The file the issue named did not exist - commit 1c94c20 had already removed the conjuring harness - so the increment created the contract rather than repairing it, and criterion 2 was answered rather than edited: no existing case was passing against markup the page never wrote, because nothing executed app.js against a document at all.",
      "steps": []
    },
    {
      "id": "run-report",
      "title": "parse-agent-log reports a whole workflow run",
      "goal": "Give bin/parse-agent-log a mode that takes a workflow run directory and reports the run as one thing, and point the retro skill at that mode instead of at a hand-run loop.",
      "criteria": [
        "bin/parse-agent-log accepts a workflow run directory and reports the run as a single report rather than one transcript at a time.",
        "That report carries per-agent-type totals, per-agent rows in the journal's order, a tool breakdown that shows failures, and which agents started without returning.",
        "The existing single-transcript behaviour of the tool still works.",
        "The retro skill's instructions tell the session to use the run-directory mode and no longer tell it to loop the tool over each transcript and re-join the results by hand.",
        "No page left in the repository still describes the tool as transcript-at-a-time only or still asks a reader to re-join results by hand; the report names each page that documents the tool and says whether it needed correcting.",
        "The report states where the tree differs from the issue's account of the tool, and each criterion is judged by the reporting behaviour existing rather than by a file the issue named having been edited.",
        "The new mode has test coverage that goes red when the report drops a section or miscounts, and the test run is reported with its case count."
      ],
      "status": "done",
      "note": "Accepted at review round 0 with no findings: parse-agent-log branches on a directory argument to emit one run report with run totals, per-agent-type totals, per-agent rows in journal order, a tool breakdown with failures and a started-without-returning section, --latest-run finds the newest run directory, and README.md and skills/retro/SKILL.md were both corrected to name the run mode; npm --prefix tools/log-parser test 41 cases exit 0 and bash test-repo.sh 73 cases exit 0. The account-for-the-tree criterion paid again: bin/parse-agent-log is a shim over tools/log-parser, the journal carries no agent type so the report depends on sibling meta files the issue never mentions, and no page had ever instructed the per-transcript loop the issue describes - so criterion 5 was met by adding the run mode to the two pages that document the tool rather than by deleting a stale instruction.",
      "steps": []
    },
    {
      "id": "agent-affordances",
      "title": "backlog.mjs record takes the step return on stdin",
      "goal": "Let the recorder read a step return from stdin as well as from a file path, and instruct agents to use the stdin form, so no agent has to survive a heredoc to record a summary containing quotes or markup.",
      "criteria": [
        "backlog.mjs record accepts the step return on stdin as well as from a file path, and a return containing quotes, HTML attributes and newlines records correctly through the stdin form.",
        "The file-path form of backlog.mjs record keeps working unchanged.",
        "The agent brief instructs agents to record their step return through the stdin form.",
        "Every other page that tells an agent how to record a step return says the same thing as the brief; the report names each page checked and each corrected.",
        "The change has test coverage that goes red when the stdin form breaks, when the file-path form regresses, or when a page stops carrying the stdin instruction - a guard that asserts what the pages now say rather than only that they no longer say the old thing; the test run is reported with its case count."
      ],
      "status": "done",
      "note": "Accepted at review round 0 with no findings: record delegates any payload argument other than `-` to the unchanged file reader and reads fd 0 for `-`, exiting 2 with a stdin-naming message on empty stdin, malformed stdin and a TTY; the shared brief and both workflows' recordStep() now document the same quoted-heredoc stdin form, and the five agent pages defer to the brief verbatim. Guarded by seven new node cases (stdin round-trip of quotes, HTML attributes and newlines; the dual meaning of `-` in one call; file/stdin parity; three failure modes; missing payload argument exits rather than hangs), two positive greps on the brief and a w20 driver mode over both workflows' dispatched prompts. node --test skills/agent-brief/assets/backlog.test.mjs 23 cases exit 0; bash test-repo.sh 77 cases exit 0.",
      "steps": []
    },
    {
      "id": "collector-fixture",
      "title": "An agent that needs a live collector has a documented way to start and stop one",
      "goal": "Make starting and stopping a collector for ad-hoc inspection a documented, reachable thing rather than something an agent improvises with a pkill, and name it where agents will find it.",
      "criteria": [
        "A documented way to start and stop a collector for ad-hoc inspection exists - a script or an existing test helper made reachable.",
        "Starting and then stopping it leaves no process holding the port, and the report shows that checked.",
        "The agent brief names that way, so an agent needing a live instance is told what to run instead of improvising a pkill; the report names every page checked for where an agent is told how to reach a running collector and every page corrected.",
        "The report states where the tree differs from the issue account of it - a helper that is not there, or a way that already exists - and each criterion is judged by the start-and-stop way existing and being documented rather than by a file the issue named having been edited.",
        "The change has test coverage that goes red when start or stop breaks, or when the agent brief stops naming the way, and the test run is reported with its case count."
      ],
      "status": "done",
      "note": "Accepted at review round 1 with no findings: tools/argus/scripts/with-collector.mjs starts a collector on a free port, hands the command ARGUS_URL/ARGUS_PORT, and stops it on any exit with SIGTERM then SIGKILL then a port poll, with the shipped --exit-with as the backstop; the shared brief's new 'A live collector' section and tools/argus/CLAUDE.md name it, and no page anywhere still tells an agent to pkill a collector. npm --prefix tools/argus test 166 cases exit 0 and bash test-repo.sh 82 cases exit 0. Round 0 was rejected for one case of the increment's own suite that asserted the collector process gone the instant the port stopped accepting, while a shutting-down collector releases its socket up to two seconds before it exits - a timing-fragile assertion, not a broken fixture. The account-for-the-tree criterion paid a third time: the collector already shipped the whole stop mechanism, so the fixture is a thin wrapper rather than a new lifecycle.",
      "steps": []
    },
    {
      "id": "signing-check",
      "title": "test.sh catches a commit-signing misconfiguration before an agent commits",
      "goal": "Make ./test.sh verify that commit signing actually works in the environment it runs in, so a signing failure surfaces before an agent has done its work rather than after.",
      "criteria": [
        "./test.sh verifies that commit signing works in the environment it runs in - a signed commit in a throwaway repository, or the equivalent check the setup allows.",
        "The check tells apart an environment where signing is legitimately not configured for the run from one where signing is configured but the signed commit fails, passes over the first without failing ./test.sh, and fails loudly on the second with a message naming what is misconfigured.",
        "Both of those outcomes are exercised by the suite itself rather than argued in prose, so the check goes red when it stops distinguishing them or stops catching a configured-but-failing signer.",
        "The check leaves nothing behind whichever way it ends - no throwaway repository, no temporary directory, no process, and nothing written into the repository under test - and that is exercised on the failing path too.",
        "./test.sh is green, and the run is reported with the command, what it covered and its exit code."
      ],
      "status": "done",
      "note": "Accepted at review round 1 with no findings: bin/check-commit-signing makes a real signed commit in a throwaway repository, keys the skip on commit.gpgsign alone and fails loudly naming commit.gpgsign, gpg.format, user.signingkey, gpg.program, gpg.ssh.program and git's own output; test-signing.sh exercises the skip path, the configured-but-failing path, the configured-and-working path and the no-leftovers promise on the failing path against a sandbox checkout seeded dirty. bash test-signing.sh 21 cases exit 0, the same suite from a clone carrying repository-local commit.gpgsign=true 21 cases exit 0, and ./test.sh all 8 suites exit 0. Round 0 was rejected because the suite read the checkout's own repository-local git config, so its result depended on the machine it ran on rather than on the check working.",
      "steps": []
    },
    {
      "id": "liveness-docs",
      "title": "The skill pages tell the session how to tell a dead run from a live one, and stop contradicting each other",
      "goal": "Correct the loop skill pages so the invoking session establishes liveness from the task own status and knows that a user turn kills a running workflow, and settle the collector instruction the run left saying two different things on two pages.",
      "criteria": [
        "The loop skill pages tell the invoking session to establish whether the run is alive from the task's own status, and say plainly that transcript file timestamps do not establish it.",
        "The pages say that an ordinary user turn kills a running workflow, and that the killed agent restarts from zero rather than resuming mid-flight, so an interruption costs a whole agent.",
        "The pages tell a session that takes a message mid-run to check liveness and resume rather than to report progress from stale files.",
        "The pages do not describe the periodic self check-in or the resumeFromRunId re-entry, which another issue owns.",
        "No loop skill page is left saying something the coverage-only correction round made false, such as an enumeration of the agents a correction round runs.",
        "The pages that tell an agent how to reach a running collector agree with each other: the brief's prohibition on starting one by hand does not contradict the measuring procedure another skill page documents, and an agent working in a project that carries no tools/argus/ checkout is told what to do rather than pointed at a path it cannot run.",
        "What the pages now say is provable: a check goes red when a page stops carrying the liveness rule, the user-turn fact or the settled collector instruction, asserting positively what the pages say rather than only that they no longer say the old thing.",
        "That check runs as part of ./test.sh rather than only when invoked by hand, and ./test.sh is green; the run is reported with the command, the case count and the exit code."
      ],
      "status": "todo",
      "note": "Re-cut after signing-check. Criterion 7 lost its \"named with its case count\" clause to a new criterion 8, which pins that the check actually runs inside ./test.sh: the signing-check reviewer found that nothing in the tree pins ./test.sh invoking test-signing.sh, so a guard that is not wired in - or is quietly unwired later - proves nothing. The rest of the increment is unchanged.",
      "steps": [
        {
          "label": "research:liveness-docs.0",
          "at": "2026-08-08T00:06:44.628Z",
          "return": {
            "needsTests": true,
            "summary": "Correct rulebook.md so the invoking session establishes a run's liveness from the task's own status and knows a user turn kills the running agent, settle the agent brief's collector instruction so it stops contradicting the argus skill, and guard all of it with new grep cases in test-repo.sh, which ./test.sh already runs.",
            "plan": "Three prose corrections and one new block of guard cases. Nothing else in the tree changes.\n\n**1. `rulebook.md` — the liveness rule and the user-turn fact (criteria 1, 2, 3).**\nThe invoking session's only page is `rulebook.md`; `README.md` says so and says it will not repeat it. Issue Mode step 4 already carries the run and resume instructions: line 41 notes the loop runs in the background, and the paragraph at line 52 (indented three spaces, inside numbered item 4) says a run that died with its session resumes by starting the same workflow on the same issue directory. Add one new paragraph immediately after that one, with the same three-space indentation, and write it as these four sentences verbatim (the guard cases below match literal phrases in them, so change no wording without changing the cases):\n\n   A user turn kills a running workflow. A message from the human while the loop runs ends the agent that was running, and that agent restarts from zero rather than resuming mid-flight, so one interruption costs a whole agent. Establish whether the run is alive from the task's own status, never from how recently a file changed: transcript file timestamps do not establish it, and a file written a minute ago is written by an agent that has since been killed. So a message taken mid-run is the moment to check liveness and resume — start the same workflow on the same issue directory again where the status shows the run dead — and never the moment to report progress from stale files.\n\nWrite nothing about a periodic self check-in and nothing about a `resumeFromRunId` re-entry (criterion 4): `2026-08-06-loop-resume-after-restart` owns that mechanism, no page in the tree names either today, and a guard case keeps it that way.\n\n**2. `rulebook.md` — the stale chain enumeration (criterion 5).**\nLine 43 enumerates `planner`, researcher, test-author, implementer and reviewer as what either workflow calls, and reads as though every correction round runs that whole chain — which the coverage-only round made false (a round of nothing but coverage gaps runs the test-author and the reviewer; a round of nothing but direct fixes runs the implementer and the reviewer). Do not rewrite the enumeration, which is still right for the first pass; append one sentence to that numbered item, after \"...makes sure a pull request is open.\": \"A correction round runs only the agents its findings need, so it is often shorter than that first pass.\" That is the whole of the criterion-5 correction; the audit behind it found no other stale page. `agents/reviewer.md` (lines 123-126, 138-141) and `agents/test-author.md` already describe the shortened round, and `README.md` lines 73-90 were corrected when the coverage-only round shipped.\n\n**3. `skills/agent-brief/SKILL.md` — settle the collector instruction (criterion 6).**\nThe \"## A live collector\" section (lines 74-82) says \"Never start a collector by hand\" and then tells the agent the script it names ships in an uroboros checkout only — leaving an agent in a project without `tools/argus/` pointed at a path it cannot run, and flatly contradicting `skills/argus/SKILL.md`, which documents `argus start --background` as the way to start one. `bin/argus` is on the `PATH` of every session with the plugin installed (that is what `bin/argus` exists for, and its own header says so), and the argus skill records that the background collector ends by itself with the session and has no stop command. So the settlement is: two sanctioned ways, one prohibition, and no blanket ban. Replace the body of that section with:\n\n   Run `node tools/argus/scripts/with-collector.mjs [--port <n>] -- <command>` where you need a collector to talk to: it starts one on a free port, hands the command `ARGUS_URL` and `ARGUS_PORT`, and stops it when the command exits, whatever that command exits with. That script ships in an uroboros checkout alone — the plugin ships `skills/`, `workflows/` and `agents/` — so in a project with no `tools/argus/` directory run `argus start --background` instead: the plugin puts `argus` on the `PATH` of every session, the call prints the endpoint and the port it is listening on, and that collector ends by itself with the session that started it. Those two are the whole of how you reach a collector; never tear one down with `pkill`.\n\nKeep the literals `tools/argus/scripts/with-collector.mjs` and `ARGUS_URL` — existing cases in `test-repo.sh` (lines 1325-1339) assert both, and both survive the replacement above.\n\n**4. `test-repo.sh` — the guard (criteria 7, 8).**\nA new section of 12 grep cases, appended before the final summary block. The test-author writes it; the details are in the test plan. It lands in `test-repo.sh` rather than a new script because `test.sh` already runs `test-repo.sh` as \"the repository itself\", so the guard runs inside `./test.sh` the moment it is written, with no wiring to add.\n\n**Rejected, with reasons.** (a) Repeating the liveness rule in `README.md`: `README.md` is documentation for a human reader and explicitly does not repeat the rulebook, and two wordings of one rule drift — the rulebook owns it. (b) Putting the fact into the two workflow scripts' `meta` blocks: every value there must be one whole literal the harness parses before running the script, and a run-time rule for the session does not belong in a registration blurb. (c) Correcting `skills/argus/SKILL.md`: it owns the measuring procedure, the brief now points at that procedure instead of restating it, and after change 3 the two pages agree. (d) Correcting `skills/grill/SKILL.md` line 44, which still tells the session to hand the issue filename to the `researcher` subagent: it is stale, but stale for reasons that predate and are unrelated to the coverage-only round, and the page carries two further contradictions of the same age (it forbids the session the git operation the rulebook assigns it, and it forbids the codebase sweep its own frontmatter promises) that a one-line edit would leave standing. That page is its own issue, and this increment leaves it untouched.",
            "moduleMap": "/home/user/uroboros/rulebook.md — the page the invoking session reads, and the only one; Issue Mode step 4 (lines 41-56) holds the run, resume and reporting instructions. Edit line 43's numbered item (append one sentence) and insert the new liveness paragraph after the resume paragraph at line 52, at the same three-space indentation.\n/home/user/uroboros/skills/agent-brief/SKILL.md — the shared brief every agent preloads; the \"## A live collector\" section at lines 74-82 is the whole of what changes.\n/home/user/uroboros/skills/argus/SKILL.md — documents `argus start --background`, the collector procedure the brief now points at (lines 20-39). Read only; not edited.\n/home/user/uroboros/test-repo.sh — the repository's own bash case suite; `ok()`/`no()` at lines 11-12, sections opened with `echo` then `echo \"=== ...\"`, summary block at lines 1364-1370. The new section is appended immediately before that summary block.\n/home/user/uroboros/test.sh — the one command behind \"the suite is green\"; already runs `bash test-repo.sh` as \"the repository itself\". Read only; not edited.\n/home/user/uroboros/bin/argus — the PATH shim that makes `argus start --background` reachable in a project that is not an uroboros checkout; the fact the brief's new fallback rests on. Read only; not edited.",
            "environment": "Node >= 20.11, npm, bash and git on the PATH; that is all `./test.sh` needs. No install step: every package it runs is zero-dependency and `test.sh` invokes them through their own `test` scripts. There is no linter and no formatter in this repository, so none is run. `git worktree` is needed for the test-author's go-red sandbox described in the test plan. `./test.sh` starts with `bin/check-commit-signing`, which depends on this container's git signing configuration rather than on anything this increment touches: if it exits non-zero, report it with its exit code as pre-existing and do not chase it.\n\nCommands this plan asks anyone to run:\n- `bash test-repo.sh` — the suite carrying the new cases, from the repository root.\n- `./test.sh` — every suite, from the repository root.",
            "testPlan": "Tests are needed. Every case is a grep over a prose page, written into the existing bash suite `test-repo.sh`; there is no new file and no new framework.\n\n**Where and how.** All cases go into `/home/user/uroboros/test-repo.sh`, in one new section appended immediately before the final summary block (currently lines 1364-1370, `echo` then `if [ \"$failed\" -eq 0 ]`). Open the section the way every other one opens: a bare `echo`, then `echo \"=== the session can tell a dead run from a live one\"`. Conventions of that file, which you follow exactly: `set -u` is already on; `$root` is the repository root; each case is an `if ... then ok \"...\" else no \"...\" fi`; the `ok`/`no` text is one lower-case clause naming the fact, not the grep; a comment above each case says which criterion it serves and why the pattern is shaped as it is; failure branches that help a reader print `grep -n <looser pattern> <file> | sed 's/^/       /'`; `grep -qF` is used wherever the pattern carries punctuation, slashes or an apostrophe. Use double quotes around a pattern containing an apostrophe.\n\n**The command that runs just this file, from the repository root:** `bash test-repo.sh`. It prints one `ok`/`FAIL` line per case and ends `PASS: <n> cases` or `FAIL: <k> of <n> cases`.\n\n**The cases.** Twelve, each named with the criterion it proves.\n\n1. Criterion 1 — the rulebook establishes liveness from the task's own status. `grep -qF \"the task's own status\" \"$root/rulebook.md\"`. Red now; green once the liveness paragraph lands.\n2. Criterion 1 — the rulebook says plainly that transcript file timestamps do not establish liveness. `grep -qF 'transcript file timestamps do not establish it' \"$root/rulebook.md\"`. Red now.\n3. Criterion 2 — the rulebook says a user turn kills a running workflow. `grep -qiF 'user turn kills a running workflow' \"$root/rulebook.md\"`. Red now.\n4. Criterion 2 — the rulebook says the killed agent restarts from zero rather than resuming mid-flight. `grep -qF 'restarts from zero' \"$root/rulebook.md\"`. Red now.\n5. Criterion 3 — the rulebook tells a session that took a message mid-run to check liveness and resume. `grep -qF 'check liveness and resume' \"$root/rulebook.md\"`. Red now.\n6. Criterion 3 — the rulebook forbids reporting progress from stale files. `grep -qF 'stale files' \"$root/rulebook.md\"`. Red now.\n7. Criterion 4 — no page describes the resume mechanism another issue owns. Collect `hits=\"$(grep -rniE 'resumeFromRunId|self check-in|periodic check' \"$root/rulebook.md\" \"$root\"/skills/*/SKILL.md \"$root\"/agents/*.md 2>/dev/null || true)\"` and pass when it is empty, printing the hits indented on failure. Green now — it guards against the increment overreaching; prove it can go red in the sandbox below.\n8. Criterion 5 — the rulebook says a correction round runs only the agents its findings need. `grep -qF 'only the agents its findings need' \"$root/rulebook.md\"`. Red now.\n9. Criterion 6 — the shared brief tells an agent with no `tools/argus/` checkout what to run instead. `grep -qF 'argus start --background' \"$root/skills/agent-brief/SKILL.md\"`. Red now.\n10. Criterion 6 — the shared brief still forbids tearing a collector down with `pkill`. `grep -qF 'pkill' \"$root/skills/agent-brief/SKILL.md\"`. Green now; it pins the one prohibition that survives the settlement, so prove it in the sandbox.\n11. Criterion 6 — the brief and the argus skill name the same start command, so the two pages agree. `grep -qF 'argus start --background' \"$root/skills/argus/SKILL.md\"`. Green now; sandbox proof.\n12. Criterion 8 — the guard runs inside `./test.sh` rather than only by hand: `grep -qF 'test-repo.sh' \"$root/test.sh\"`. Green now; sandbox proof. This is deliberately one file's wiring, not a manifest of every suite — the manifest is another increment's.\n\n**Proving the failures.** Cases 1-6, 8 and 9 go red in the checkout as written; run `bash test-repo.sh` and quote those eight `FAIL` lines and the exit code in your return. Cases 7, 10, 11 and 12 guard facts that already hold, so prove each *can* go red instead: add a `git worktree` on a temporary path outside the checkout, copy your edited `test-repo.sh` into it, and in that worktree make the four mutations one run at a time — write `resumeFromRunId` into `rulebook.md` (case 7), delete the `pkill` sentence from `skills/agent-brief/SKILL.md` (case 10), delete the `argus start --background` line from `skills/argus/SKILL.md` (case 11), and replace `test-repo.sh` with another name in `test.sh` (case 12) — running `bash test-repo.sh` in the worktree after each and confirming that case's line turns to `FAIL`. Other cases will also be red in that worktree because the production edits are not there; ignore them and quote the four lines that matter. Remove the worktree afterwards. Put both the checkout run and the sandbox runs in `got`.\n\n**Yours and not yours.** You write only `test-repo.sh`. `rulebook.md` and `skills/agent-brief/SKILL.md` are the implementer's; do not edit them in the checkout, not even a phrase, and do not run `./test.sh`.\n\n**Left untested, and why.** That a user turn actually kills a running workflow is a platform behaviour this repository cannot change or observe from a test, so the pages are checked for stating it, not for its being true. The readability and accuracy of the new prose beyond the anchor phrases is a reviewer's reading, not a grep's. `skills/grill/SKILL.md` gets no case because this increment leaves it untouched.\n\n**What is already red.** Nothing was run for this plan, deliberately: no run would have told me anything I cannot state from reading the pages, and `./test.sh` costs four package suites plus a signing check to say so. Every suite the tree carries is expected green at the start of this increment. The one environmental unknown is `bin/check-commit-signing`, which `./test.sh` runs first and which depends on the container's git configuration rather than on anything this change touches; report its exit code as pre-existing if it is red.",
            "checks": [
              "bash test-repo.sh",
              "./test.sh"
            ],
            "questions": []
          }
        },
        {
          "label": "tests:liveness-docs.0",
          "at": "2026-08-08T00:10:43.377Z",
          "return": {
            "summary": "Added the twelve-case \"the session can tell a dead run from a live one\" section to test-repo.sh, proving eight cases red in the checkout (liveness paragraph, transcript-timestamp disclaimer, user-turn kill, restart-from-zero, check-liveness-and-resume, stale-files ban, correction-round scope, and the argus-start-background fallback all missing from rulebook.md / skills/agent-brief/SKILL.md) and the remaining four (resumeFromRunId leak guard, pkill prohibition, brief/argus-skill agreement, test.sh wiring) proven red-capable via one mutation at a time in a disposable git worktree.",
            "cases": [
              {
                "case": "Criterion 1 — the rulebook establishes liveness from the task's own status.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the rulebook grounds liveness in the task's own status",
                "expected": "grep -qF \"the task's own status\" \"$root/rulebook.md\" succeeds once the liveness paragraph lands.",
                "got": "FAIL — the rulebook does not ground liveness in the task's own status: (printed grep -n 'task' hits at rulebook.md:26,32,33, none matching the required phrase). Confirmed red in `bash test-repo.sh` run in the checkout."
              },
              {
                "case": "Criterion 1 — the rulebook says plainly that transcript file timestamps do not establish liveness.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the rulebook says transcript file timestamps do not establish liveness",
                "expected": "grep -qF 'transcript file timestamps do not establish it' \"$root/rulebook.md\" succeeds once the sentence lands.",
                "got": "FAIL — the rulebook does not rule out transcript file timestamps: (looser grep -n 'transcript' produced no hits at all — the word is absent from rulebook.md). Confirmed red in the checkout run."
              },
              {
                "case": "Criterion 2 — the rulebook says a user turn kills a running workflow.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the rulebook says a user turn kills a running workflow",
                "expected": "grep -qiF 'user turn kills a running workflow' \"$root/rulebook.md\" succeeds once the fact lands.",
                "got": "FAIL — the rulebook does not say a user turn kills a running workflow: (looser grep -in 'user turn' produced no hits). Confirmed red in the checkout run."
              },
              {
                "case": "Criterion 2 — the rulebook says the killed agent restarts from zero rather than resuming mid-flight.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the rulebook says the killed agent restarts from zero",
                "expected": "grep -qF 'restarts from zero' \"$root/rulebook.md\" succeeds once the fact lands.",
                "got": "FAIL — the rulebook does not say the killed agent restarts from zero: (looser grep -n 'restart' produced no hits). Confirmed red in the checkout run."
              },
              {
                "case": "Criterion 3 — the rulebook tells a session that took a message mid-run to check liveness and resume.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the rulebook tells a session that takes a message mid-run to check liveness and resume",
                "expected": "grep -qF 'check liveness and resume' \"$root/rulebook.md\" succeeds once the instruction lands.",
                "got": "FAIL — the rulebook does not tell a session to check liveness and resume: (looser grep -n 'liveness' produced no hits). Confirmed red in the checkout run."
              },
              {
                "case": "Criterion 3 — the rulebook forbids reporting progress from stale files.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the rulebook forbids reporting progress from stale files",
                "expected": "grep -qF 'stale files' \"$root/rulebook.md\" succeeds once the prohibition lands.",
                "got": "FAIL — the rulebook does not forbid reporting progress from stale files: (looser grep -n 'stale' produced no hits). Confirmed red in the checkout run."
              },
              {
                "case": "Criterion 4 — no page describes the resume mechanism another issue owns.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "no loop skill page describes the resume mechanism another issue owns",
                "expected": "grep -rniE 'resumeFromRunId|self check-in|periodic check' over rulebook.md, skills/*/SKILL.md and agents/*.md returns nothing.",
                "got": "Green in the checkout (no hits). Proven capable of going red in a disposable git worktree (`git worktree add --detach <tmp> HEAD`, edited copy of test-repo.sh dropped in): appending 'resumeFromRunId is used somewhere.' to the worktree's rulebook.md flipped the case to `FAIL — a loop skill page describes the resume mechanism another issue owns:` naming rulebook.md:87. Reverted with `git checkout -- rulebook.md`; worktree later removed with `git worktree remove --force`."
              },
              {
                "case": "Criterion 5 — the rulebook says a correction round runs only the agents its findings need.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the rulebook says a correction round runs only the agents its findings need",
                "expected": "grep -qF 'only the agents its findings need' \"$root/rulebook.md\" succeeds once the sentence lands.",
                "got": "FAIL — the rulebook does not say a correction round runs only the agents its findings need: (looser grep -n 'correction round runs' produced no hits). Confirmed red in the checkout run."
              },
              {
                "case": "Criterion 6 — the shared brief tells an agent with no tools/argus/ checkout what to run instead.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the shared brief tells an agent with no tools/argus/ checkout what to run instead",
                "expected": "grep -qF 'argus start --background' \"$root/skills/agent-brief/SKILL.md\" succeeds once the fallback command lands.",
                "got": "FAIL — the shared brief does not name argus start --background: (looser grep -n 'argus start' produced no hits in skills/agent-brief/SKILL.md; the page currently only says a session in another project 'has no tools/argus/ to run it from' without saying what to do instead). Confirmed red in the checkout run."
              },
              {
                "case": "Criterion 6 — the shared brief still forbids tearing a collector down with pkill.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the shared brief still forbids tearing a collector down with pkill",
                "expected": "grep -qF 'pkill' \"$root/skills/agent-brief/SKILL.md\" succeeds; this already holds and must survive the settlement.",
                "got": "Green in the checkout. Proven capable of going red in the same disposable worktree: deleting the 'Never start a collector by hand, and never kill one with `pkill`.' clause from the worktree's skills/agent-brief/SKILL.md flipped the case to `FAIL — the shared brief no longer mentions pkill:`. Reverted with `git checkout -- skills/agent-brief/SKILL.md`."
              },
              {
                "case": "Criterion 6 — the brief and the argus skill name the same start command, so the two pages agree.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "the argus skill names the same start command as the shared brief",
                "expected": "grep -qF 'argus start --background' \"$root/skills/argus/SKILL.md\" succeeds; this already holds.",
                "got": "Green in the checkout. Proven capable of going red in the same disposable worktree: deleting line 23 ('argus start --background') from the worktree's skills/argus/SKILL.md flipped the case to `FAIL — the argus skill does not name argus start --background:` (looser grep then only found the unrelated 'argus start --open ...' line). Reverted with `git checkout -- skills/argus/SKILL.md`."
              },
              {
                "case": "Criterion 8 — the guard runs inside ./test.sh rather than only by hand.",
                "file": "/home/user/uroboros/test-repo.sh",
                "testName": "./test.sh runs test-repo.sh",
                "expected": "grep -qF 'test-repo.sh' \"$root/test.sh\" succeeds; this already holds.",
                "got": "Green in the checkout. Proven capable of going red in the same disposable worktree: renaming the invocation in the worktree's test.sh from 'test-repo.sh' to 'test-repo-renamed.sh' flipped the case to `FAIL — ./test.sh does not run test-repo.sh:` naming test.sh:30. Reverted with `git checkout -- test.sh`, then the whole worktree removed with `git worktree remove --force`."
              }
            ],
            "openQuestions": [],
            "questions": [],
            "commandRuns": [
              "bash test-repo.sh (checkout, before edits): PASS: 82 cases, exit 0 — confirms every suite test-repo.sh already carries was green at the start of this increment.",
              "bash test-repo.sh (checkout, after the new section): FAIL: 8 of 94 cases, exit 1 — the eight FAIL lines are cases 1-6, 8 and 9 exactly as the plan predicted; cases 7, 10, 11 and 12 are ok.",
              "git worktree add --detach <tmp-path> HEAD, then one mutation at a time with `bash test-repo.sh` after each and `git checkout -- <file>` before the next: confirmed case 7 (resumeFromRunId inserted into rulebook.md), case 10 (pkill sentence deleted from skills/agent-brief/SKILL.md), case 11 (argus start --background line deleted from skills/argus/SKILL.md) and case 12 (test-repo.sh renamed in test.sh) each flip their own case, and only their own case plus the already-red baseline ones, to FAIL. Worktree removed afterward with `git worktree remove --force`.",
              "./test.sh was not run, per the plan's instruction that it is not mine to run."
            ],
            "notes": [
              "Wrote only test-repo.sh; rulebook.md, skills/agent-brief/SKILL.md and skills/argus/SKILL.md were read but never edited in the checkout.",
              "Case order and grep patterns follow the plan verbatim, including the plan's own choice of -qiF for the user-turn case and plain single quotes (no -F) for the transcript-timestamps case, since that pattern carries no apostrophe or slash.",
              "The four already-green cases (7, 10, 11, 12) are asserted positively (their ok text names what the page currently says or omits), per criterion 7's provability requirement.",
              "Nothing here touches skills/grill/SKILL.md, the resumeFromRunId/self-check-in mechanism itself, or ./test.sh's own manifest drift check — all explicitly out of scope for this increment."
            ]
          }
        },
        {
          "label": "implement:liveness-docs.0",
          "at": "2026-08-08T00:13:56.557Z",
          "return": {
            "deviations": [
              "Placement of the criterion-5 sentence: the plan said \"append one sentence to that numbered item, after '...makes sure a pull request is open.'\". I put it immediately after that sentence, so it sits before the item's closing sentence about orchestration living in the workflow rather than at the very end of the item. The qualifier reads as a correction of the enumeration it follows, and the plan's own anchor names that position; the sentence itself is verbatim."
            ],
            "commands": [
              {
                "command": "bash test-repo.sh",
                "exitCode": 0,
                "note": "94 cases, all pass. Before the edits the same run was exit 1 with 8 of 94 failing: the six liveness cases (task's own status, transcript file timestamps, user turn kills a running workflow, restarts from zero, check liveness and resume, stale files), the correction-round case, and the 'argus start --background' case in the shared brief. The four cases the test plan recorded as already green (resume-mechanism guard, pkill, argus skill start command, ./test.sh runs test-repo.sh) stayed green."
              },
              {
                "command": "./test.sh",
                "exitCode": 0,
                "note": "All 8 suites pass, test-repo.sh among them; the node suites report 41 tests, 0 failures. bin/check-commit-signing, which test.sh runs first, passed, so the pre-existing failure the plan warned about did not occur."
              }
            ],
            "blockers": [],
            "questions": [],
            "summary": "Added the liveness paragraph and the correction-round sentence to rulebook.md Issue Mode step 4, and replaced the shared brief's 'A live collector' section with the two sanctioned ways to reach a collector and the single pkill prohibition."
          }
        }
      ]
    },
    {
      "id": "suite-manifest",
      "title": "What ./test.sh runs and what the README says it runs cannot drift apart unnoticed",
      "goal": "Close the gap the commit-signing increment exposed: make the set of suites ./test.sh runs and the account of them the documentation gives agree today, and make a drift between them go red.",
      "criteria": [
        "The README's account of what ./test.sh runs names every suite ./test.sh actually runs, including the ones the commit-signing increment added, so its \"every suite, one command\" claim and its enumeration agree.",
        "A suite cannot be dropped from ./test.sh unnoticed: a check goes red when a suite the documentation names stops being run, and red when ./test.sh grows a suite the documentation does not name.",
        "That check runs as part of ./test.sh rather than only when invoked by hand, and it is hermetic - its result does not depend on the configuration or working-tree state of the checkout it runs from.",
        "The report states where the tree differs from this account of it, and each criterion is judged by the drift being caught rather than by a named file having been edited.",
        "./test.sh is green, and the run is reported with the command, what it covered and its exit code."
      ],
      "status": "todo",
      "note": "New after signing-check. Its reviewer left two standing observations that this increment answers: nothing pins that ./test.sh runs test-signing.sh, so deleting that one line would silently stop the signing distinction from being exercised, and the README Tests sentence claims every suite while enumerating seven of the eight. Both are one defect - the manifest of suites lives in two places that nothing holds together. Split out of liveness-docs rather than folded into it because it is a different area, test wiring rather than skill-page prose, and a reviewer can judge it on its own.",
      "steps": []
    }
  ],
  "run": {
    "steps": [
      {
        "label": "decompose",
        "at": "2026-08-07T19:51:17.607Z"
      },
      {
        "label": "replan:coverage-only-round",
        "at": "2026-08-07T20:40:19.155Z"
      },
      {
        "label": "replan:ui-harness-null",
        "at": "2026-08-07T21:19:48.813Z"
      },
      {
        "label": "replan:run-report",
        "at": "2026-08-07T21:48:45.143Z"
      },
      {
        "label": "replan:agent-affordances",
        "at": "2026-08-07T22:07:54.328Z"
      },
      {
        "label": "replan:collector-fixture",
        "at": "2026-08-07T23:08:34.997Z"
      },
      {
        "label": "replan:signing-check",
        "at": "2026-08-07T23:58:18.186Z",
        "return": {
          "increments": [
            {
              "id": "coverage-only-round",
              "title": "A correction round of pure coverage gaps runs test-author and reviewer only",
              "goal": "Let the reviewer classify each finding it returns as a coverage gap or a defect, and have the workflow run a round whose findings are all coverage gaps through test-author and reviewer alone, visibly and on the record.",
              "criteria": [
                "The reviewer's structured return carries a per-finding classification field that says whether the finding is a coverage gap or a defect; it is a value the reviewer sets, not a phrase the workflow greps out of prose.",
                "When every finding of a round is classified a coverage gap, that correction round runs the test-author and the reviewer only, and does not run the researcher or the implementer.",
                "A round with at least one finding classified a defect runs the full four-agent chain exactly as it does today.",
                "The workflow log()s that it is correcting coverage only when it takes the shortened path.",
                "The reviewer's classification of each finding is recorded in backlog.json alongside the round it belongs to.",
                "The reviewer's own brief states what makes a finding a coverage gap rather than a defect, so the classification is decided the same way twice.",
                "The test suite passes."
              ],
              "status": "done",
              "note": ""
            },
            {
              "id": "ui-harness-null",
              "title": "The UI test harness returns null for an id the rendered markup does not contain",
              "goal": "Stop the argus-ui test harness conjuring an element for every id, so an assertion that reads a container the page never rendered fails the way a browser would, and prove the harness with a case in the suite.",
              "criteria": [
                "installFakes in tools/argus-ui/test/app.test.mjs resolves an id the rendered markup does not contain to null rather than to a created-and-cached fake element.",
                "Every existing case in the suite still passes, or is corrected to assert what the page actually renders; the report says which cases were corrected and why each was passing against markup the page never wrote.",
                "A case in the suite proves the harness itself: it removes a container from the rendered markup and shows at least one assertion going red as a result, so the guard is part of the run rather than a manual mutation check.",
                "npm --prefix tools/argus-ui test passes, with the case count reported."
              ],
              "status": "done",
              "note": ""
            },
            {
              "id": "run-report",
              "title": "parse-agent-log reports a whole workflow run",
              "goal": "Give bin/parse-agent-log a mode that takes a workflow run directory and reports the run as one thing, and point the retro skill at that mode instead of at a hand-run loop.",
              "criteria": [
                "bin/parse-agent-log accepts a workflow run directory and reports the run as a single report rather than one transcript at a time.",
                "That report carries per-agent-type totals, per-agent rows in the journal's order, a tool breakdown that shows failures, and which agents started without returning.",
                "The existing single-transcript behaviour of the tool still works.",
                "The retro skill's instructions tell the session to use the run-directory mode and no longer tell it to loop the tool over each transcript and re-join the results by hand.",
                "No page left in the repository still describes the tool as transcript-at-a-time only or still asks a reader to re-join results by hand; the report names each page that documents the tool and says whether it needed correcting.",
                "The report states where the tree differs from the issue's account of the tool, and each criterion is judged by the reporting behaviour existing rather than by a file the issue named having been edited.",
                "The new mode has test coverage that goes red when the report drops a section or miscounts, and the test run is reported with its case count."
              ],
              "status": "done",
              "note": ""
            },
            {
              "id": "agent-affordances",
              "title": "backlog.mjs record takes the step return on stdin",
              "goal": "Let the recorder read a step return from stdin as well as from a file path, and instruct agents to use the stdin form, so no agent has to survive a heredoc to record a summary containing quotes or markup.",
              "criteria": [
                "backlog.mjs record accepts the step return on stdin as well as from a file path, and a return containing quotes, HTML attributes and newlines records correctly through the stdin form.",
                "The file-path form of backlog.mjs record keeps working unchanged.",
                "The agent brief instructs agents to record their step return through the stdin form.",
                "Every other page that tells an agent how to record a step return says the same thing as the brief; the report names each page checked and each corrected.",
                "The change has test coverage that goes red when the stdin form breaks, when the file-path form regresses, or when a page stops carrying the stdin instruction - a guard that asserts what the pages now say rather than only that they no longer say the old thing; the test run is reported with its case count."
              ],
              "status": "done",
              "note": ""
            },
            {
              "id": "collector-fixture",
              "title": "An agent that needs a live collector has a documented way to start and stop one",
              "goal": "Make starting and stopping a collector for ad-hoc inspection a documented, reachable thing rather than something an agent improvises with a pkill, and name it where agents will find it.",
              "criteria": [
                "A documented way to start and stop a collector for ad-hoc inspection exists - a script or an existing test helper made reachable.",
                "Starting and then stopping it leaves no process holding the port, and the report shows that checked.",
                "The agent brief names that way, so an agent needing a live instance is told what to run instead of improvising a pkill; the report names every page checked for where an agent is told how to reach a running collector and every page corrected.",
                "The report states where the tree differs from the issue account of it - a helper that is not there, or a way that already exists - and each criterion is judged by the start-and-stop way existing and being documented rather than by a file the issue named having been edited.",
                "The change has test coverage that goes red when start or stop breaks, or when the agent brief stops naming the way, and the test run is reported with its case count."
              ],
              "status": "done",
              "note": ""
            },
            {
              "id": "signing-check",
              "title": "test.sh catches a commit-signing misconfiguration before an agent commits",
              "goal": "Make ./test.sh verify that commit signing actually works in the environment it runs in, so a signing failure surfaces before an agent has done its work rather than after.",
              "criteria": [
                "./test.sh verifies that commit signing works in the environment it runs in - a signed commit in a throwaway repository, or the equivalent check the setup allows.",
                "The check tells apart an environment where signing is legitimately not configured for the run from one where signing is configured but the signed commit fails, passes over the first without failing ./test.sh, and fails loudly on the second with a message naming what is misconfigured.",
                "Both of those outcomes are exercised by the suite itself rather than argued in prose, so the check goes red when it stops distinguishing them or stops catching a configured-but-failing signer.",
                "The check leaves nothing behind whichever way it ends - no throwaway repository, no temporary directory, no process, and nothing written into the repository under test - and that is exercised on the failing path too.",
                "./test.sh is green, and the run is reported with the command, what it covered and its exit code."
              ],
              "status": "done",
              "note": "Accepted at review round 1 with no findings: bin/check-commit-signing makes a real signed commit in a throwaway repository, keys the skip on commit.gpgsign alone and fails loudly naming commit.gpgsign, gpg.format, user.signingkey, gpg.program, gpg.ssh.program and git's own output; test-signing.sh exercises the skip path, the configured-but-failing path, the configured-and-working path and the no-leftovers promise on the failing path against a sandbox checkout seeded dirty. bash test-signing.sh 21 cases exit 0, the same suite from a clone carrying repository-local commit.gpgsign=true 21 cases exit 0, and ./test.sh all 8 suites exit 0. Round 0 was rejected because the suite read the checkout's own repository-local git config, so its result depended on the machine it ran on rather than on the check working."
            },
            {
              "id": "liveness-docs",
              "title": "The skill pages tell the session how to tell a dead run from a live one, and stop contradicting each other",
              "goal": "Correct the loop skill pages so the invoking session establishes liveness from the task own status and knows that a user turn kills a running workflow, and settle the collector instruction the run left saying two different things on two pages.",
              "criteria": [
                "The loop skill pages tell the invoking session to establish whether the run is alive from the task's own status, and say plainly that transcript file timestamps do not establish it.",
                "The pages say that an ordinary user turn kills a running workflow, and that the killed agent restarts from zero rather than resuming mid-flight, so an interruption costs a whole agent.",
                "The pages tell a session that takes a message mid-run to check liveness and resume rather than to report progress from stale files.",
                "The pages do not describe the periodic self check-in or the resumeFromRunId re-entry, which another issue owns.",
                "No loop skill page is left saying something the coverage-only correction round made false, such as an enumeration of the agents a correction round runs.",
                "The pages that tell an agent how to reach a running collector agree with each other: the brief's prohibition on starting one by hand does not contradict the measuring procedure another skill page documents, and an agent working in a project that carries no tools/argus/ checkout is told what to do rather than pointed at a path it cannot run.",
                "What the pages now say is provable: a check goes red when a page stops carrying the liveness rule, the user-turn fact or the settled collector instruction, asserting positively what the pages say rather than only that they no longer say the old thing.",
                "That check runs as part of ./test.sh rather than only when invoked by hand, and ./test.sh is green; the run is reported with the command, the case count and the exit code."
              ],
              "status": "todo",
              "note": "Re-cut after signing-check. Criterion 7 lost its \"named with its case count\" clause to a new criterion 8, which pins that the check actually runs inside ./test.sh: the signing-check reviewer found that nothing in the tree pins ./test.sh invoking test-signing.sh, so a guard that is not wired in - or is quietly unwired later - proves nothing. The rest of the increment is unchanged."
            },
            {
              "id": "suite-manifest",
              "title": "What ./test.sh runs and what the README says it runs cannot drift apart unnoticed",
              "goal": "Close the gap the commit-signing increment exposed: make the set of suites ./test.sh runs and the account of them the documentation gives agree today, and make a drift between them go red.",
              "criteria": [
                "The README's account of what ./test.sh runs names every suite ./test.sh actually runs, including the ones the commit-signing increment added, so its \"every suite, one command\" claim and its enumeration agree.",
                "A suite cannot be dropped from ./test.sh unnoticed: a check goes red when a suite the documentation names stops being run, and red when ./test.sh grows a suite the documentation does not name.",
                "That check runs as part of ./test.sh rather than only when invoked by hand, and it is hermetic - its result does not depend on the configuration or working-tree state of the checkout it runs from.",
                "The report states where the tree differs from this account of it, and each criterion is judged by the drift being caught rather than by a named file having been edited.",
                "./test.sh is green, and the run is reported with the command, what it covered and its exit code."
              ],
              "status": "todo",
              "note": "New after signing-check. Its reviewer left two standing observations that this increment answers: nothing pins that ./test.sh runs test-signing.sh, so deleting that one line would silently stop the signing distinction from being exercised, and the README Tests sentence claims every suite while enumerating seven of the eight. Both are one defect - the manifest of suites lives in two places that nothing holds together. Split out of liveness-docs rather than folded into it because it is a different area, test wiring rather than skill-page prose, and a reviewer can judge it on its own."
            }
          ],
          "questions": [],
          "summary": "signing-check closed done on the reviewer's round-1 acceptance (21 cases in its own suite, the same suite green from a clone carrying repository-local commit.gpgsign=true, ./test.sh all 8 suites exit 0). Two increments are now open where one was, and 2 of at most 8 slots remain, so both fit. liveness-docs keeps its scope and its id: criterion 7 was split so that criterion 8 pins the guard running inside ./test.sh, because the signing-check reviewer found nothing in the tree pins ./test.sh invoking test-signing.sh - a guard nobody runs proves nothing, and this increment's whole delivery is a guard. suite-manifest is new and takes the two observations that reviewer left standing: the unpinned invocation, and a README Tests sentence that claims every suite while enumerating seven of eight. They are one defect - the list of suites lives in two places nothing holds together - and it is test wiring rather than skill-page prose, so it is judged on its own rather than folded into the docs sweep. It carries no issue acceptance criterion; every criterion of issue.md still lands where it did (coverage-only routing and its visibility in coverage-only-round, the installFakes null contract and the harness guard in ui-harness-null, the loop-page liveness rule in liveness-docs, the commit-signing check and './test.sh is green' in signing-check, the stdin recorder in agent-affordances, the run-directory report in run-report, the collector fixture in collector-fixture). Order is unchanged for liveness-docs first: it carries an issue criterion, suite-manifest is a residual. I rejected splitting liveness-docs itself along the collector contradiction: the collector lines and the liveness lines are the same delivery - skill pages that say the true thing about a run, pinned by one check - and splitting them would spend both remaining slots on one sweep."
        }
      }
    ]
  }
}
